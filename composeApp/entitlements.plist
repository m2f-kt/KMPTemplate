<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "https://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!--
      Hardened-runtime entitlements for the packaged macOS .app.

      Compose Desktop signs the bundle with the hardened runtime enabled. A custom
      entitlementsFile REPLACES Compose Desktop's defaults wholesale — you MUST repeat these
      3 JVM keys (JIT, unsigned executable memory, library validation) or the bundled JVM
      won't launch.

      Add any app-specific entitlements (e.g. device access) BELOW these three keys.
    -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
</dict>
</plist>
