---
phase: 02-server-auth-users
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/core/database/src/main/kotlin/com/m2f/core/database/migrations/Migration.kt
  - server/core/database/src/main/kotlin/com/m2f/core/database/migrations/Migrations.kt
  - server/core/database/src/main/kotlin/com/m2f/core/database/migrations/MigrationRegistry.kt
  - server/core/config/src/main/kotlin/com/m2f/core/config/server/DomainError.kt
  - server/core/config/src/main/kotlin/com/m2f/core/config/server/Error.kt
  - server/core/config/src/main/kotlin/com/m2f/core/config/configuration/Env.kt
  - server/auth/build.gradle.kts
  - server/auth/src/main/kotlin/com/m2f/server/auth/Auth.kt
  - server/auth/src/main/kotlin/com/m2f/server/auth/tables/UsersTable.kt
  - server/auth/src/main/kotlin/com/m2f/server/auth/tables/RefreshTokensTable.kt
  - server/auth/src/main/kotlin/com/m2f/server/auth/repository/UserRepository.kt
  - server/auth/src/main/kotlin/com/m2f/server/auth/repository/RefreshTokenRepository.kt
  - server/auth/src/main/kotlin/com/m2f/server/auth/security/PasswordHasher.kt
  - server/auth/src/main/kotlin/com/m2f/server/auth/security/JwtTokenProvider.kt
  - server/auth/src/main/kotlin/com/m2f/server/auth/errors/AuthErrors.kt
  - server/auth/src/main/kotlin/com/m2f/server/auth/service/AuthService.kt
  - server/auth/src/main/kotlin/com/m2f/server/auth/routes/AuthRoutes.kt
  - server/auth/src/main/kotlin/com/m2f/server/auth/di/AuthModule.kt
  - server/src/main/kotlin/com/m2f/template/di/ServerModule.kt
  - server/src/main/kotlin/com/m2f/template/Application.kt
  - server/build.gradle.kts
autonomous: true

must_haves:
  truths:
    - "POST /api/auth/register with valid email/password/name returns 201 with accessToken, refreshToken, and expiresIn"
    - "POST /api/auth/register with invalid fields returns 422 with accumulated FieldError list (all errors at once, not fail-fast)"
    - "POST /api/auth/register with duplicate email returns 422 AUTH_USER_ALREADY_EXISTS"
    - "Password is stored bcrypt-hashed in the database, never in plaintext"
    - "Users and refresh_tokens tables are created by the migration system on startup"
  artifacts:
    - path: "server/auth/src/main/kotlin/com/m2f/server/auth/tables/UsersTable.kt"
      provides: "Exposed Table definition for users"
      contains: "object UsersTable"
    - path: "server/auth/src/main/kotlin/com/m2f/server/auth/tables/RefreshTokensTable.kt"
      provides: "Exposed Table definition for refresh_tokens"
      contains: "object RefreshTokensTable"
    - path: "server/auth/src/main/kotlin/com/m2f/server/auth/repository/UserRepository.kt"
      provides: "Database access for user CRUD"
      contains: "class UserRepository"
    - path: "server/auth/src/main/kotlin/com/m2f/server/auth/service/AuthService.kt"
      provides: "Registration business logic with Arrow Raise"
      contains: "suspend fun register"
    - path: "server/auth/src/main/kotlin/com/m2f/server/auth/routes/AuthRoutes.kt"
      provides: "POST /api/auth/register route"
      contains: "post(\"/register\")"
    - path: "server/auth/src/main/kotlin/com/m2f/server/auth/di/AuthModule.kt"
      provides: "Koin module wiring all auth dependencies"
      contains: "val authModule"
  key_links:
    - from: "server/auth/src/main/kotlin/com/m2f/server/auth/routes/AuthRoutes.kt"
      to: "server/auth/src/main/kotlin/com/m2f/server/auth/service/AuthService.kt"
      via: "Koin-injected AuthService called from conduit route handler"
      pattern: "authService\\.register"
    - from: "server/auth/src/main/kotlin/com/m2f/server/auth/service/AuthService.kt"
      to: "server/auth/src/main/kotlin/com/m2f/server/auth/repository/UserRepository.kt"
      via: "Constructor-injected repository for user persistence"
      pattern: "userRepository\\.findByEmail|userRepository\\.insert"
    - from: "server/src/main/kotlin/com/m2f/template/Application.kt"
      to: "server/auth/src/main/kotlin/com/m2f/server/auth/routes/AuthRoutes.kt"
      via: "authRoutes() called from routing block"
      pattern: "authRoutes"
---

<objective>
Build the foundation for server-side authentication: database tables for users and refresh tokens, repository layer, password hashing, JWT token generation, auth error types, registration endpoint with accumulated validation, and Koin DI wiring.

Purpose: Establishes the data layer, security utilities, and first working endpoint (register) that all subsequent auth plans build on. Also fixes two cross-module issues (Migration visibility and R2dbcDatabase Koin registration) that block feature module development.

Output: Working POST /api/auth/register endpoint with bcrypt password hashing, JWT token generation, accumulated validation errors, and database persistence via Exposed R2DBC.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-server-auth-users/02-RESEARCH.md
@.planning/phases/01-foundation-module-structure/01-02-SUMMARY.md
@.planning/phases/01-foundation-module-structure/01-03-SUMMARY.md
@server/core/config/src/main/kotlin/com/m2f/core/config/server/DomainError.kt
@server/core/config/src/main/kotlin/com/m2f/core/config/server/Error.kt
@server/core/config/src/main/kotlin/com/m2f/core/config/configuration/Env.kt
@server/core/config/src/main/kotlin/com/m2f/core/config/configuration/Configuration.kt
@server/core/config/src/main/kotlin/com/m2f/core/config/configuration/Module.kt
@server/core/database/src/main/kotlin/com/m2f/core/database/migrations/Migration.kt
@server/core/database/src/main/kotlin/com/m2f/core/database/migrations/Migrations.kt
@server/core/database/src/main/kotlin/com/m2f/core/database/migrations/MigrationRegistry.kt
@server/core/database/src/main/kotlin/com/m2f/core/database/Startup.kt
@server/core/security/src/main/kotlin/com/m2f/core/security/SecurityPlugin.kt
@server/src/main/kotlin/com/m2f/template/Application.kt
@server/src/main/kotlin/com/m2f/template/di/ServerModule.kt
@server/auth/build.gradle.kts
@server/build.gradle.kts
@core/models/src/commonMain/kotlin/com/m2f/template/models/AppError.kt
@core/models/src/commonMain/kotlin/com/m2f/template/models/dto/AuthDtos.kt
@core/models/src/commonMain/kotlin/com/m2f/template/models/validation/ValidationSupport.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Infrastructure fixes, tables, repositories, and security utilities</name>
  <files>
    server/core/database/src/main/kotlin/com/m2f/core/database/migrations/Migration.kt
    server/core/database/src/main/kotlin/com/m2f/core/database/migrations/Migrations.kt
    server/core/database/src/main/kotlin/com/m2f/core/database/migrations/MigrationRegistry.kt
    server/core/config/src/main/kotlin/com/m2f/core/config/server/DomainError.kt
    server/core/config/src/main/kotlin/com/m2f/core/config/server/Error.kt
    server/core/config/src/main/kotlin/com/m2f/core/config/configuration/Env.kt
    server/auth/build.gradle.kts
    server/auth/src/main/kotlin/com/m2f/server/auth/tables/UsersTable.kt
    server/auth/src/main/kotlin/com/m2f/server/auth/tables/RefreshTokensTable.kt
    server/auth/src/main/kotlin/com/m2f/server/auth/repository/UserRepository.kt
    server/auth/src/main/kotlin/com/m2f/server/auth/repository/RefreshTokenRepository.kt
    server/auth/src/main/kotlin/com/m2f/server/auth/security/PasswordHasher.kt
    server/auth/src/main/kotlin/com/m2f/server/auth/security/JwtTokenProvider.kt
    server/auth/src/main/kotlin/com/m2f/server/auth/errors/AuthErrors.kt
  </files>
  <action>
    **Step 1: Fix Migration visibility (cross-module blocker)**
    In `server/core/database/src/main/kotlin/com/m2f/core/database/migrations/Migration.kt`:
    - Change `internal interface Migration` to `interface Migration` (remove `internal`)
    - Change `internal object Migrations` to `object Migrations` (remove `internal`)
    - Change `internal object MigrationRegistry` in `MigrationRegistry.kt` to `object MigrationRegistry` (remove `internal`)
    - In `MigrationRegistry.kt`, add a public `fun register(migration: Migration)` method that delegates to `Migrations.register(migration)` so feature modules can register their own migrations

    **Step 2: Add forbidden() error helper**
    In `server/core/config/src/main/kotlin/com/m2f/core/config/server/Error.kt`:
    - Add a `forbidden()` helper function matching the existing pattern:
      ```kotlin
      suspend inline fun RoutingContext.forbidden(code: String, error: String): Unit =
          call.respond(HttpStatusCode.Forbidden, ErrorResponse(code = code, message = error))
      ```

    **Step 3: Add auth-specific DomainError subtypes**
    Create `server/auth/src/main/kotlin/com/m2f/server/auth/errors/AuthErrors.kt`:
    - `InvalidCredentials` -- implements DomainError, maps to AppError.Auth.InvalidCredentials(), responds with 401
    - `UserAlreadyExists` -- implements DomainError, maps to AppError.Auth.UserAlreadyExists(), responds with 422
    - `TokenExpired` -- implements DomainError, maps to AppError.Auth.TokenExpired(), responds with 401
    - `TokenInvalid` -- implements DomainError, maps to AppError.Auth.TokenInvalid(), responds with 401
    - `UserNotFound` -- implements DomainError, maps to AppError.User.NotFound(), responds with 404 (add `notFound()` helper to Error.kt)
    - `Forbidden` -- implements DomainError, maps to AppError.User.Forbidden(), responds with 403 via forbidden() helper
    - Each error class must implement `toAppError()` and `context(routingContext: RoutingContext) suspend fun respond()`
    - Also add a `notFound()` response helper to Error.kt:
      ```kotlin
      suspend inline fun RoutingContext.notFound(code: String, error: String): Unit =
          call.respond(HttpStatusCode.NotFound, ErrorResponse(code = code, message = error))
      ```

    **Step 4: Add token expiry to Env.Auth**
    In `server/core/config/src/main/kotlin/com/m2f/core/config/configuration/Env.kt`:
    - Add `accessTokenExpiry: Long` with default 900000 (15 min in ms), env var `JWT_ACCESS_EXPIRY`
    - Add `refreshTokenExpiry: Long` with default 604800000 (7 days in ms), env var `JWT_REFRESH_EXPIRY`

    **Step 5: Update server/auth build.gradle.kts**
    Replace the stub build file with full dependencies:
    ```kotlin
    plugins {
        id("server-module-convention")
    }
    group = "com.m2f.server"
    dependencies {
        implementation(projects.core.models)
        implementation(projects.server.core.config)
        implementation(projects.server.core.database)
        implementation(projects.server.core.security)
        implementation(libs.bundles.fp)
        implementation(libs.bundles.ktor.core)
        implementation(libs.jbcrypt)
        implementation(libs.bundles.di)
        testImplementation(libs.bundles.testing.server)
    }
    ```

    **Step 6: Create Exposed table definitions**
    Create `server/auth/src/main/kotlin/com/m2f/server/auth/tables/UsersTable.kt`:
    - `object UsersTable : Table("users")` with columns: `id` (uuid, PK, autoGenerate), `email` (varchar 255, uniqueIndex), `passwordHash` (varchar 255), `name` (varchar 100), `role` (varchar 50, default "USER"), `createdAt` (datetime, defaultExpression CurrentDateTime), `updatedAt` (datetime, defaultExpression CurrentDateTime)
    - Use `org.jetbrains.exposed.v1.core.Table`, `org.jetbrains.exposed.v1.datetime.datetime`, `org.jetbrains.exposed.v1.datetime.CurrentDateTime`
    - Use `uuid("id").autoGenerate()` for the id column (this auto-generates UUIDs in Exposed 1.0)

    Create `server/auth/src/main/kotlin/com/m2f/server/auth/tables/RefreshTokensTable.kt`:
    - `object RefreshTokensTable : Table("refresh_tokens")` with columns: `id` (uuid, PK, autoGenerate), `userId` (uuid, references UsersTable.id), `tokenHash` (varchar 512), `expiresAt` (datetime), `createdAt` (datetime, defaultExpression CurrentDateTime), `revoked` (bool, default false)

    **Step 7: Create repository layer**
    Create `server/auth/src/main/kotlin/com/m2f/server/auth/repository/UserRepository.kt`:
    - Constructor takes `R2dbcDatabase`
    - `suspend fun findByEmail(email: String): UserRecord?` -- uses `suspendTransaction(db = db) { UsersTable.select(UsersTable.columns).where { UsersTable.email eq email }.toList().singleOrNull()?.toUserRecord() }`
    - `suspend fun findById(id: UUID): UserRecord?` -- same pattern by id
    - `suspend fun insert(email: String, passwordHash: String, name: String, role: String): UUID` -- insert and return generated id
    - `suspend fun updateProfile(id: UUID, name: String?, email: String?): Boolean` -- update and return rowsUpdated > 0
    - Define `data class UserRecord(val id: UUID, val email: String, val passwordHash: String, val name: String, val role: String)` as internal data class (or in same file)
    - Use `.toList()` within `suspendTransaction` for R2DBC Flow collection (not `.collect {}`)
    - Extension function `ResultRow.toUserRecord()` to map from Exposed row

    Create `server/auth/src/main/kotlin/com/m2f/server/auth/repository/RefreshTokenRepository.kt`:
    - Constructor takes `R2dbcDatabase`
    - `suspend fun store(userId: UUID, tokenHash: String, expiresAt: LocalDateTime): UUID` -- insert, return id
    - `suspend fun findValidToken(tokenHash: String): RefreshTokenRecord?` -- select where tokenHash matches, not revoked, expiresAt > now
    - `suspend fun revokeByUserId(userId: UUID)` -- update all tokens for user, set revoked = true
    - `suspend fun revokeById(id: UUID)` -- revoke single token
    - Define `data class RefreshTokenRecord(val id: UUID, val userId: UUID, val tokenHash: String, val expiresAt: LocalDateTime, val revoked: Boolean)`

    **Step 8: Create security utilities**
    Create `server/auth/src/main/kotlin/com/m2f/server/auth/security/PasswordHasher.kt`:
    - `class PasswordHasher` with `suspend fun hash(password: String): String` wrapping `BCrypt.hashpw(password, BCrypt.gensalt(12))` in `withContext(Dispatchers.Default)` to avoid blocking the event loop
    - `suspend fun verify(password: String, hash: String): Boolean` wrapping `BCrypt.checkpw()` likewise

    Create `server/auth/src/main/kotlin/com/m2f/server/auth/security/JwtTokenProvider.kt`:
    - Constructor takes `Configuration`
    - Reads `env.auth.secret`, `env.auth.audience`, `env.auth.issuer`, `env.auth.accessTokenExpiry`, `env.auth.refreshTokenExpiry`
    - `fun generateAccessToken(userId: String, role: String): String` using `com.auth0.jwt.JWT.create()` with subject=userId, claim("role", role), audience, issuer, expiresAt
    - `fun generateRefreshToken(): String` returning `UUID.randomUUID().toString()`
    - `fun generateTokenPair(userId: String, role: String): Pair<AuthResponse, String>` returning the AuthResponse DTO AND the raw refresh token (the response goes to client, the raw token gets hashed for DB storage)
    - `fun getRefreshTokenExpiry(): Long` returning refreshTokenExpiry for computing DB expiry timestamp
    - Use SHA-256 hash of refresh token for storage: `fun hashRefreshToken(token: String): String` using `MessageDigest.getInstance("SHA-256")`
  </action>
  <verify>
    Run `./gradlew :server:auth:compileKotlin` from project root. All new files must compile without errors. Verify the Migration interface is no longer `internal` by confirming the auth module can import it.
  </verify>
  <done>
    UsersTable and RefreshTokensTable defined, UserRepository and RefreshTokenRepository compile with R2DBC suspendTransaction, PasswordHasher wraps BCrypt with coroutine dispatcher, JwtTokenProvider generates HMAC256 tokens via com.auth0.jwt, auth-specific DomainError subtypes map to AppError hierarchy, Migration interface is public.
  </done>
</task>

<task type="auto">
  <name>Task 2: AuthService, registration route, Koin wiring, and Application integration</name>
  <files>
    server/auth/src/main/kotlin/com/m2f/server/auth/service/AuthService.kt
    server/auth/src/main/kotlin/com/m2f/server/auth/routes/AuthRoutes.kt
    server/auth/src/main/kotlin/com/m2f/server/auth/di/AuthModule.kt
    server/auth/src/main/kotlin/com/m2f/server/auth/Auth.kt
    server/src/main/kotlin/com/m2f/template/di/ServerModule.kt
    server/src/main/kotlin/com/m2f/template/Application.kt
    server/build.gradle.kts
  </files>
  <action>
    **Step 1: Create AuthService with registration**
    Create `server/auth/src/main/kotlin/com/m2f/server/auth/service/AuthService.kt`:
    - Constructor takes `UserRepository`, `RefreshTokenRepository`, `PasswordHasher`, `JwtTokenProvider`
    - `context(raise: Raise<DomainError>) suspend fun register(request: RegisterRequest): AuthResponse`:
      1. Validate using `raise.withError(...)` and `zipOrAccumulate` to accumulate all field errors into an `IncorrectInput`. Map `NonEmptyList<FieldError>` to `IncorrectInput(errors.map { InvalidFieldImpl(it.field, nonEmptyListOf(it.message)) })` -- check the actual `InvalidField` data class in DomainError.kt to get the correct type name. The `IncorrectInput` takes `NonEmptyList<InvalidField>` where `InvalidField` has fields `field: String` and `errors: NonEmptyList<String>`. Since `InvalidField` might not exist as a separate class, check DomainError.kt -- it may be an inline data class or need to be created. Use the existing pattern from `IncorrectInput`.
      2. Check `userRepository.findByEmail(validEmail) == null`, else `raise(UserAlreadyExists())`
      3. Hash password via `passwordHasher.hash(validPassword)`
      4. Insert user via `userRepository.insert(validEmail, hash, validName, "USER")`
      5. Generate token pair via `tokenProvider.generateTokenPair(userId.toString(), "USER")`
      6. Hash refresh token and store via `refreshTokenRepository.store(userId, hashedToken, expiresAt)`
      7. Return the AuthResponse

    **IMPORTANT -- Arrow zipOrAccumulate integration:**
    The existing validators use `context(raise: Raise<FieldError>)`. To accumulate, use:
    ```kotlin
    raise.withError({ errors: NonEmptyList<FieldError> ->
        IncorrectInput(errors.map { InvalidField(it.field, nonEmptyListOf(it.message)) })
    }) {
        zipOrAccumulate(
            { validateEmail(request.email) },
            { validatePassword(request.password) },
            { validateName(request.name) }
        ) { email, password, name -> Triple(email, password, name) }
    }
    ```
    Check if `InvalidField` is a data class in DomainError.kt. If not (it's used as a constructor parameter type for `IncorrectInput`), look at the actual code. The `IncorrectInput` class takes `NonEmptyList<InvalidField>` -- find or create the `InvalidField` type. It likely needs to be a data class with `field: String` and `errors: NonEmptyList<String>`.

    **Step 2: Create AuthRoutes**
    Create `server/auth/src/main/kotlin/com/m2f/server/auth/routes/AuthRoutes.kt`:
    - `fun Route.authRoutes(authService: AuthService)` extension function
    - Define `route("/api/auth") { ... }` block
    - `post("/register")` using `conduit(HttpStatusCode.Created) { ... }`:
      - Parse body with `getModel<RegisterRequest>()`
      - Call `authService.register(request)`
    - Login, refresh, logout routes will be added in Plan 02-02. Just add the register route for now.

    **Step 3: Create migration registrations**
    Update `server/auth/src/main/kotlin/com/m2f/server/auth/Auth.kt` (replace the empty stub):
    - Create two migration classes: `CreateUsersTableMigration` (version "20260211000001") and `CreateRefreshTokensTableMigration` (version "20260211000002")
    - Each implements `Migration` interface from database module
    - `migrate()` calls `SchemaUtils.create(UsersTable)` and `SchemaUtils.create(RefreshTokensTable)` respectively
    - Add a top-level function `fun registerAuthMigrations()` that calls `MigrationRegistry.register(...)` for each migration

    **Step 4: Create AuthModule (Koin DI)**
    Create `server/auth/src/main/kotlin/com/m2f/server/auth/di/AuthModule.kt`:
    ```kotlin
    val authModule = module {
        single { PasswordHasher() }
        single { JwtTokenProvider(get<Configuration>()) }
        single { UserRepository(get<R2dbcDatabase>()) }
        single { RefreshTokenRepository(get<R2dbcDatabase>()) }
        single { AuthService(get(), get(), get(), get()) }
    }
    ```

    **Step 5: Wire into ServerModule and Application**
    Update `server/src/main/kotlin/com/m2f/template/di/ServerModule.kt`:
    - Import `authModule` from `com.m2f.server.auth.di`
    - Add `includes(authModule)` inside the module block

    Update `server/build.gradle.kts`:
    - Add `implementation(projects.server.auth)` to dependencies

    Update `server/src/main/kotlin/com/m2f/template/Application.kt`:
    - After `install(Koin)`, register the R2dbcDatabase instance in Koin: access the Koin instance and declare the database. Use `application.koin.declare(database)` after install, where `database` is the R2dbcDatabase from the context parameter. Since `install(Koin)` returns Unit, access Koin via `val koinApp = install(Koin) { modules(...) }` -- actually the Koin plugin installs itself and can be accessed via the Application extension. After `install(Koin)`, use `koin.declare(databaseInstance)` where the database comes from the context. The context parameter is `_: R2dbcDatabase` -- give it a name like `database: R2dbcDatabase` so it can be referenced:
      ```kotlin
      context(_: Configuration, database: R2dbcDatabase)
      fun Application.module() {
          install(Koin) {
              modules(configurationModule, serverModule)
          }
          // Register database in Koin for repository injection
          koin.declare(database)
          ...
      }
      ```
    - Call `registerAuthMigrations()` BEFORE `startDatabase()` in the `main()` function (so migrations are registered when the database starts). Actually, looking at the startup flow: `startDatabase()` calls `MigrationRegistry.registerMigrations()` then `Migrations.migrate(it)`. So the cleanest approach is to call `registerAuthMigrations()` from inside `MigrationRegistry.registerMigrations()`. But MigrationRegistry is in the database module which doesn't depend on server:auth. **Solution:** Call `registerAuthMigrations()` directly in `main()` before `startDatabase()`.
    - Add auth routes to routing: import and call `authRoutes(get())` inside the routing block, where `get()` retrieves AuthService from Koin. Use `val authService: AuthService by inject()` or `koin.get<AuthService>()`.

    **Step 6: Verify the full registration flow compiles**
    Ensure the entire dependency chain compiles: Application -> routing -> AuthRoutes -> AuthService -> Repositories -> Tables -> Database.
  </action>
  <verify>
    Run `./gradlew :server:compileKotlin` to verify the full server compiles. Then run `./gradlew :server:run` with Docker PostgreSQL up (docker compose up -d) and test with:
    ```
    curl -X POST http://localhost:8080/api/auth/register \
      -H "Content-Type: application/json" \
      -d '{"email":"test@example.com","password":"password123","name":"Test User"}'
    ```
    Expected: 201 response with `{"accessToken":"...","refreshToken":"...","expiresIn":900}`.
    Test validation:
    ```
    curl -X POST http://localhost:8080/api/auth/register \
      -H "Content-Type: application/json" \
      -d '{"email":"bad","password":"short","name":""}'
    ```
    Expected: 422 response with accumulated field errors for email, password, and name.
  </verify>
  <done>
    POST /api/auth/register returns 201 with JWT tokens for valid input, 422 with accumulated field errors for invalid input, 422 with AUTH_USER_ALREADY_EXISTS for duplicate email. All auth dependencies wired through Koin. R2dbcDatabase registered in Koin for repository injection. Auth migrations run on server startup creating users and refresh_tokens tables.
  </done>
</task>

</tasks>

<verification>
1. `./gradlew :server:auth:compileKotlin` passes -- auth module compiles independently
2. `./gradlew :server:compileKotlin` passes -- full server compiles with auth wired in
3. POST /api/auth/register with valid data returns 201 + AuthResponse JSON
4. POST /api/auth/register with invalid fields returns 422 + accumulated errors (multiple fields)
5. POST /api/auth/register with duplicate email returns 422 + AUTH_USER_ALREADY_EXISTS
6. Database has `users` and `refresh_tokens` tables after startup
7. No `internal` keyword on Migration interface or Migrations object
</verification>

<success_criteria>
Registration endpoint fully functional with accumulated validation, bcrypt password hashing, JWT token generation, and database persistence. All code uses Arrow Raise -- zero try/catch for domain errors. R2dbcDatabase available in Koin. Migration system accessible to feature modules.
</success_criteria>

<output>
After completion, create `.planning/phases/02-server-auth-users/02-01-SUMMARY.md`
</output>
