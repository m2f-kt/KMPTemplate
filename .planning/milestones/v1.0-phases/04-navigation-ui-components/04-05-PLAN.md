---
phase: 04-navigation-ui-components
plan: 05
type: execute
wave: 2
depends_on: ["04-02"]
files_modified:
  - app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/data/TerminalTable.kt
  - app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/data/TerminalList.kt
  - app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/display/TerminalKbd.kt
  - app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/display/TerminalAvatar.kt
  - app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/display/TerminalDivider.kt
  - composeApp/src/commonMain/kotlin/com/m2f/template/App.kt
autonomous: true

must_haves:
  truths:
    - "TerminalTable renders a header row and content rows with alternating/bordered styling from TerminalTheme"
    - "TerminalList renders a titled list with items in 4 states (Default, Hover, Selected, Disabled)"
    - "TerminalKbd, TerminalAvatar, and TerminalDivider render correctly styled display elements"
    - "App.kt wraps NavHost in TerminalTheme for consistent theming across all screens"
  artifacts:
    - path: "app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/data/TerminalTable.kt"
      provides: "TerminalTable and TerminalTableRow composables"
      contains: "TerminalTable"
    - path: "app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/data/TerminalList.kt"
      provides: "TerminalList and TerminalListItem with ListItemState enum"
      contains: "ListItemState"
    - path: "app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/display/TerminalKbd.kt"
      provides: "TerminalKbd keyboard shortcut display"
      contains: "TerminalKbd"
    - path: "app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/display/TerminalAvatar.kt"
      provides: "TerminalAvatar with initials display"
      contains: "TerminalAvatar"
    - path: "app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/display/TerminalDivider.kt"
      provides: "TerminalDivider horizontal line"
      contains: "TerminalDivider"
    - path: "composeApp/src/commonMain/kotlin/com/m2f/template/App.kt"
      provides: "App composable with TerminalTheme wrapping AppNavHost"
      contains: "TerminalTheme"
  key_links:
    - from: "composeApp/src/commonMain/kotlin/com/m2f/template/App.kt"
      to: "app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/theme/TerminalTheme.kt"
      via: "TerminalTheme { AppNavHost() }"
      pattern: "TerminalTheme"
---

<objective>
Implement data display components (table, list) and visual display components (kbd, avatar, divider), then integrate TerminalTheme into App.kt -- completing all 41 Pencil design system components.

Purpose: Data components (table, list) handle structured content display. Display components (kbd, avatar, divider) provide utility UI elements. Integrating TerminalTheme into App.kt ensures all screens render within the custom theme context.

Output: 5 component files completing the full 41-component library, plus App.kt updated with TerminalTheme.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-navigation-ui-components/04-RESEARCH.md
@.planning/phases/04-navigation-ui-components/04-02-SUMMARY.md
@composeApp/src/commonMain/kotlin/com/m2f/template/App.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement data components (Table, List)</name>
  <files>
    app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/data/TerminalTable.kt
    app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/data/TerminalList.kt
  </files>
  <action>
Create table and list components in package `com.m2f.template.designsystem.components.data`.

**IMPORTANT: Before implementing, use Pencil MCP tools.** Call `batch_get` on `terminal_design_system.pen` searching for "Table", "Table Row", "List", and "List Item" components. Call `get_screenshot` to see exact visual specs (cell padding, row borders, header styling, item states).

**TerminalTable.kt:**
```kotlin
@Composable
fun TerminalTable(
    headers: List<String>,
    modifier: Modifier = Modifier,
    content: @Composable ColumnScope.() -> Unit,
)
```
- Column layout with header row and content rows
- Header row: Row with BasicText in typography.xs, fontWeight.SemiBold, color textMuted, bottom border
- Each header cell gets equal weight (`Modifier.weight(1f)`) or configurable widths
- Overall container: border in border color, rounded corners (radius.md), surface background
- Cells padded with spacing.sm horizontal, spacing.xs vertical

```kotlin
@Composable
fun TerminalTableRow(
    cells: List<String>,
    modifier: Modifier = Modifier,
)
```
- Row with BasicText for each cell in typography.sm, color text
- Bottom border (borders.thin in border color) separating rows (except last)
- Cell weights matching header weights
- Same cell padding as header

**TerminalList.kt:**
```kotlin
enum class ListItemState { Default, Hover, Selected, Disabled }

@Composable
fun TerminalList(
    modifier: Modifier = Modifier,
    title: String? = null,
    count: Int? = null,
    content: @Composable ColumnScope.() -> Unit,
)
```
- Column layout with optional title (typography.sm, fontWeight.SemiBold) and count badge
- Content area: Column with list items
- Surface background, border, rounded corners (radius.md)
- Title row at top with optional count shown via TerminalBadge or simple text

```kotlin
@Composable
fun TerminalListItem(
    text: String,
    modifier: Modifier = Modifier,
    state: ListItemState = ListItemState.Default,
    subtitle: String? = null,
    onClick: (() -> Unit)? = null,
    leadingContent: (@Composable () -> Unit)? = null,
    trailingContent: (@Composable () -> Unit)? = null,
)
```
- State-based styling:
  - Default: transparent background, text color
  - Hover: inset background (applied on pointer hover via Modifier.hoverable or pointerInput)
  - Selected: accentMuted background, accent text
  - Disabled: text in textDim, opacity.medium, not clickable
- Row layout with optional leading/trailing content, text + optional subtitle
- Clickable when onClick provided and not Disabled
- Bottom border between items (borders.thin)
  </action>
  <verify>
Run `./gradlew :app:designsystem:compileKotlinJvm 2>&1 | tail -5` to verify compilation. Verify: `grep "ListItemState" app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/data/TerminalList.kt` confirms enum.
  </verify>
  <done>TerminalTable renders header + rows with borders; TerminalList renders titled list with 4 item states; all theme-driven; no Material3</done>
</task>

<task type="auto">
  <name>Task 2: Implement display components (Kbd, Avatar, Divider) and integrate TerminalTheme into App.kt</name>
  <files>
    app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/display/TerminalKbd.kt
    app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/display/TerminalAvatar.kt
    app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/display/TerminalDivider.kt
    composeApp/src/commonMain/kotlin/com/m2f/template/App.kt
  </files>
  <action>
Create 3 display components and integrate the theme into the app root.

**IMPORTANT: Before implementing, use Pencil MCP tools.** Call `batch_get` on `terminal_design_system.pen` searching for "Kbd", "Avatar", and "Divider" components. Call `get_screenshot` for visual reference.

**TerminalKbd.kt** (`com.m2f.template.designsystem.components.display`):
```kotlin
@Composable
fun TerminalKbd(
    text: String,
    modifier: Modifier = Modifier,
)
```
- Small inline element showing keyboard shortcut (e.g., "Cmd+K", "Ctrl+S")
- Box with: inset or surface background, border (borders.thin, border color), rounded corners (radius.sm)
- Tight padding (spacing.xs horizontal, ~2dp vertical)
- Text in typography.xs, color textMuted
- Slightly elevated appearance (via border + background contrast)

**TerminalAvatar.kt** (`com.m2f.template.designsystem.components.display`):
```kotlin
@Composable
fun TerminalAvatar(
    initials: String,
    modifier: Modifier = Modifier,
    size: Dp = 36.dp,
)
```
- Circular element showing 1-2 character initials
- Box with: accentMuted background, circular clip (RoundedCornerShape(50%)), size x size
- Text in typography.sm (or scaled based on size), fontWeight.SemiBold, color accent
- Centered content alignment

**TerminalDivider.kt** (`com.m2f.template.designsystem.components.display`):
```kotlin
@Composable
fun TerminalDivider(
    modifier: Modifier = Modifier,
)
```
- Horizontal line: `Box(Modifier.fillMaxWidth().height(borders.thin).background(border color))`
- Simple, full-width divider using border color from theme
- Optional: accept orientation parameter for vertical dividers

**App.kt update:**
Update the existing `App()` composable to wrap `AppNavHost()` with `TerminalTheme`:
```kotlin
KoinApplication(application = { modules(allAppModules) }) {
    TerminalTheme {
        AppNavHost()
    }
}
```
This ensures all navigation screens render within the TerminalTheme context, making `TerminalTheme.colors`, `.typography`, etc. available throughout the app.

Import `TerminalTheme` from `com.m2f.template.designsystem.theme.TerminalTheme`.

Also, if the placeholder screens in AppNavHost (from plan 04-01) use hardcoded colors or Material3 Text, update them to use `TerminalText` from the designsystem module and theme colors. This is optional cleanup but improves consistency.
  </action>
  <verify>
Run `./gradlew :composeApp:compileKotlinJvm 2>&1 | tail -5` to verify full app compiles with TerminalTheme integration. Run `./gradlew :composeApp:compileKotlinWasmJs 2>&1 | tail -5` to verify WASM target. Verify: `grep "TerminalTheme" composeApp/src/commonMain/kotlin/com/m2f/template/App.kt` confirms theme wrapping.
  </verify>
  <done>TerminalKbd, TerminalAvatar, TerminalDivider implemented; App.kt wraps NavHost in TerminalTheme; all 41 components complete; full app compiles on JVM and WASM</done>
</task>

</tasks>

<verification>
1. `./gradlew :app:designsystem:compileKotlinJvm` -- all component files compile
2. `./gradlew :composeApp:compileKotlinJvm` -- full app compiles with TerminalTheme integration
3. `./gradlew :composeApp:compileKotlinWasmJs` -- WASM target compiles
4. `grep "TerminalTheme" composeApp/src/commonMain/kotlin/com/m2f/template/App.kt` confirms theme wrapping
5. `grep -r "import.*material" app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/` returns 0 results (no Material3 anywhere in components)
6. Full component count check: `find app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components -name "*.kt" | wc -l` should be >= 15 (TerminalText + 14 component files across button, input, card, feedback, selection, data, display)
</verification>

<success_criteria>
- TerminalTable renders header + data rows with theme-styled borders and typography
- TerminalList renders titled list with 4 item states (Default, Hover, Selected, Disabled)
- TerminalKbd displays keyboard shortcuts with terminal styling
- TerminalAvatar shows circular initials avatar
- TerminalDivider renders horizontal divider line
- App.kt wraps AppNavHost in TerminalTheme composable
- All 41 Pencil components are implemented across 15 Kotlin files
- Full app compiles on JVM and WASM targets
- Zero Material3 imports in any design system component file
- 10 of 41 Pencil components covered in this plan (Table: 2, List: 5, Kbd: 1, Avatar: 1, Divider: 1)
- Cumulative: 13 (plan 03) + 18 (plan 04) + 10 (plan 05) = 41 total
</success_criteria>

<output>
After completion, create `.planning/phases/04-navigation-ui-components/04-05-SUMMARY.md`
</output>
