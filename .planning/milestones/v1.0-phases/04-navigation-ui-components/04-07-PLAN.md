---
phase: 04-navigation-ui-components
plan: 07
type: execute
wave: 2
depends_on: ["04-06"]
files_modified:
  - app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/selection/TerminalCheckbox.kt
  - app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/selection/TerminalSwitch.kt
  - app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/selection/TerminalRadio.kt
  - app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/data/TerminalTable.kt
  - app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/data/TerminalList.kt
  - app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/display/TerminalKbd.kt
  - app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/display/TerminalAvatar.kt
  - app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/display/TerminalDivider.kt
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Every selection component (Checkbox, Switch, Radio) has @Preview showing checked/unchecked and labeled/unlabeled states"
    - "Every data component (Table, List) has @Preview showing populated content with multiple rows/items"
    - "Every display component (Kbd, Avatar, Divider) has @Preview showing representative usage"
    - "All 17 designsystem component files now have at least one @Preview function"
  artifacts:
    - path: "app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/selection/TerminalCheckbox.kt"
      provides: "Checkbox state previews"
      contains: "@Preview"
    - path: "app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/selection/TerminalSwitch.kt"
      provides: "Switch state previews"
      contains: "@Preview"
    - path: "app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/selection/TerminalRadio.kt"
      provides: "Radio state previews"
      contains: "@Preview"
    - path: "app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/data/TerminalTable.kt"
      provides: "Table preview with sample data"
      contains: "@Preview"
    - path: "app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/data/TerminalList.kt"
      provides: "List preview with items in multiple states"
      contains: "@Preview"
    - path: "app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/display/TerminalKbd.kt"
      provides: "Kbd preview"
      contains: "@Preview"
    - path: "app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/display/TerminalAvatar.kt"
      provides: "Avatar preview"
      contains: "@Preview"
    - path: "app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/display/TerminalDivider.kt"
      provides: "Divider preview"
      contains: "@Preview"
  key_links:
    - from: "all component previews"
      to: "TerminalTheme"
      via: "TerminalTheme wrapper in each preview"
      pattern: "TerminalTheme.*\\{"
    - from: "all preview files"
      to: "org.jetbrains.compose.ui.tooling.preview.Preview"
      via: "import statement"
      pattern: "import org\\.jetbrains\\.compose\\.ui\\.tooling\\.preview\\.Preview"
---

<objective>
Add @Preview composables to selection, data, and display design system components, completing coverage across all 17 component files.

Purpose: Gap closure -- completes the preview coverage started in plan 04-06. After this plan, every designsystem component has IDE-previewable variants.
Output: 8 remaining component files updated with @Preview functions covering all states and variants.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-navigation-ui-components/04-06-SUMMARY.md
@app/designsystem/build.gradle.kts
@app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/theme/TerminalTheme.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @Preview functions to selection components (3 files)</name>
  <files>
    app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/selection/TerminalCheckbox.kt
    app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/selection/TerminalSwitch.kt
    app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/selection/TerminalRadio.kt
  </files>
  <action>
Add `@Preview` annotated composable functions to the END of each file. Import `org.jetbrains.compose.ui.tooling.preview.Preview`. Every preview MUST be wrapped in `TerminalTheme { ... }`. Use `Column` with `Arrangement.spacedBy(8.dp)` and `Modifier.padding(16.dp).background(TerminalTheme.colors.background)` for layout.

All preview functions must be `private`.

**TerminalCheckbox.kt** -- Add 1 preview:
- `TerminalCheckboxPreview`: Show 4 states in a Column:
  1. Unchecked without label: `TerminalCheckbox(checked = false, onCheckedChange = {})`
  2. Checked without label: `TerminalCheckbox(checked = true, onCheckedChange = {})`
  3. Checked with label: `TerminalCheckbox(checked = true, onCheckedChange = {}, label = "Accept terms")`
  4. Disabled unchecked with label: `TerminalCheckbox(checked = false, onCheckedChange = {}, label = "Disabled option", enabled = false)`

**TerminalSwitch.kt** -- Add 1 preview:
- `TerminalSwitchPreview`: Show 4 states in a Column:
  1. Off without label: `TerminalSwitch(checked = false, onCheckedChange = {})`
  2. On without label: `TerminalSwitch(checked = true, onCheckedChange = {})`
  3. On with label: `TerminalSwitch(checked = true, onCheckedChange = {}, label = "Dark mode")`
  4. Disabled off with label: `TerminalSwitch(checked = false, onCheckedChange = {}, label = "Disabled", enabled = false)`

**TerminalRadio.kt** -- Add 1 preview:
- `TerminalRadioPreview`: Show 4 states in a Column:
  1. Unselected without label: `TerminalRadio(selected = false, onClick = {})`
  2. Selected without label: `TerminalRadio(selected = true, onClick = {})`
  3. Selected with label: `TerminalRadio(selected = true, onClick = {}, label = "Option A")`
  4. Disabled unselected with label: `TerminalRadio(selected = false, onClick = {}, label = "Disabled", enabled = false)`
  </action>
  <verify>Run `./gradlew :app:designsystem:compileKotlinJvm` to verify compilation. Then `grep -c "@Preview" app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/selection/*.kt` should show 1 per file (3 total).</verify>
  <done>All 3 selection component files have @Preview functions showing checked/unchecked, labeled/unlabeled, and disabled states wrapped in TerminalTheme.</done>
</task>

<task type="auto">
  <name>Task 2: Add @Preview functions to data and display components (5 files)</name>
  <files>
    app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/data/TerminalTable.kt
    app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/data/TerminalList.kt
    app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/display/TerminalKbd.kt
    app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/display/TerminalAvatar.kt
    app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/display/TerminalDivider.kt
  </files>
  <action>
Add `@Preview` annotated composable functions to the END of each file. Import `org.jetbrains.compose.ui.tooling.preview.Preview`. Every preview MUST be wrapped in `TerminalTheme { ... }`. Use `Column` with `Arrangement.spacedBy(8.dp)` and `Modifier.padding(16.dp).background(TerminalTheme.colors.background)` for layout.

All preview functions must be `private`.

**TerminalTable.kt** -- Add 1 preview:
- `TerminalTablePreview`: Show a table with headers `listOf("Name", "Role", "Status")` and 3 rows of sample data using `TerminalTableRow`. The last row should have `showBottomBorder = false`. Example cells: `listOf("Alice", "Admin", "Active")`, `listOf("Bob", "User", "Pending")`, `listOf("Charlie", "User", "Inactive")`.

**TerminalList.kt** -- Add 1 preview:
- `TerminalListPreview`: Show a `TerminalList` with `title = "Team Members"` and `count = 4`. Add 4 `TerminalListItem` entries showing different `ListItemState` values:
  1. `text = "Alice"`, `subtitle = "Admin"`, `state = ListItemState.Default`
  2. `text = "Bob"`, `subtitle = "Editor"`, `state = ListItemState.Hover`
  3. `text = "Charlie"`, `subtitle = "Viewer"`, `state = ListItemState.Selected`
  4. `text = "Dave"`, `subtitle = "Locked"`, `state = ListItemState.Disabled`, `showBottomBorder = false`

**TerminalKbd.kt** -- Add 1 preview:
- `TerminalKbdPreview`: Show 3 keyboard shortcuts in a Row with `Arrangement.spacedBy(8.dp)`: `TerminalKbd("Cmd+K")`, `TerminalKbd("Ctrl+S")`, `TerminalKbd("Esc")`.

**TerminalAvatar.kt** -- Add 1 preview:
- `TerminalAvatarPreview`: Show 3 avatars in a Row with `Arrangement.spacedBy(8.dp)`: `TerminalAvatar("AB")`, `TerminalAvatar("JD", size = 48.dp)`, `TerminalAvatar("X")`.

**TerminalDivider.kt** -- Add 1 preview:
- `TerminalDividerPreview`: Show a Column with `TerminalText("Above")`, `TerminalDivider(modifier = Modifier.padding(vertical = 8.dp))`, `TerminalText("Below")`. Import TerminalText from `com.m2f.template.designsystem.components.TerminalText`.
  </action>
  <verify>Run `./gradlew :app:designsystem:compileKotlinJvm` to verify compilation. Then run `grep -rn "@Preview" app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/` and count -- should show at least 17 total @Preview annotations across all 17 component files (some files have 2 previews).</verify>
  <done>All 5 data and display component files have @Preview functions with representative content. Combined with plan 04-06 and Task 1, all 17 designsystem component files now have @Preview composables.</done>
</task>

</tasks>

<verification>
- `./gradlew :app:designsystem:compileKotlinJvm` passes with zero errors
- `./gradlew assemble` passes (all KMP targets still compile)
- `grep -rl "@Preview" app/designsystem/src/commonMain/kotlin/com/m2f/template/designsystem/components/` returns all 17 component files (TerminalText + 4 button/input/card + 4 feedback + 3 selection + 2 data + 3 display)
- Zero Material3 imports in any designsystem file
- Every @Preview function is wrapped in TerminalTheme
</verification>

<success_criteria>
All 17 designsystem component files have @Preview functions. The full project compiles on all KMP targets. The UAT gap "Design system components have @Preview composables" is fully closed.
</success_criteria>

<output>
After completion, create `.planning/phases/04-navigation-ui-components/04-07-SUMMARY.md`
</output>
