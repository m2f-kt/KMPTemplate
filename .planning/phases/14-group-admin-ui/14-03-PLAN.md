---
phase: 14-group-admin-ui
plan: 03
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/AdminPanelIntent.kt
  - app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/AdminPanelModel.kt
  - app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/AdminPanelMutation.kt
  - app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/AdminPanelEvent.kt
  - app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/AdminPanelViewModel.kt
  - app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/AdminPanelScreen.kt
  - app/admin/src/commonTest/kotlin/com/m2f/template/app/admin/AdminPanelViewModelTest.kt
  - composeApp/src/commonMain/kotlin/com/m2f/template/navigation/AppNavHost.kt
  - composeApp/src/commonMain/kotlin/com/m2f/template/di/AppModule.kt
autonomous: true

must_haves:
  truths:
    - "Admin can see their group name, slug, description, and member count on the admin panel"
    - "Admin can see a paginated list of group members with name, email, role, and join date"
    - "Admin can load more members via 'load more' action when hasMore is true"
    - "Admin panel shows loading state while fetching group info and members"
    - "Error from server (e.g. Forbidden) is displayed, not a crash"
    - "Admin can navigate to the register-member form from the admin panel"
  artifacts:
    - path: "app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/AdminPanelIntent.kt"
      provides: "User action types for admin panel"
      contains: "sealed interface AdminPanelIntent"
    - path: "app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/AdminPanelModel.kt"
      provides: "UI state with group info, member list, pagination"
      contains: "data class AdminPanelModel"
    - path: "app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/AdminPanelMutation.kt"
      provides: "State transition types"
      contains: "sealed interface AdminPanelMutation"
    - path: "app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/AdminPanelEvent.kt"
      provides: "One-shot navigation events"
      contains: "NavigateToRegisterMember"
    - path: "app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/AdminPanelViewModel.kt"
      provides: "ViewModel loading group + members with init-dispatching"
      contains: "SharingStarted.Eagerly"
    - path: "app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/AdminPanelScreen.kt"
      provides: "Admin panel UI with group info card + member table"
      contains: "fun AdminPanelScreen"
    - path: "app/admin/src/commonTest/kotlin/com/m2f/template/app/admin/AdminPanelViewModelTest.kt"
      provides: "ViewModel tests for loading, pagination, errors"
      contains: "class AdminPanelViewModelTest"
  key_links:
    - from: "AdminPanelViewModel"
      to: "sdk.getGroup(groupId)"
      via: "init-dispatched LoadAdminPanel intent"
      pattern: "sdk\\.getGroup"
    - from: "AdminPanelViewModel"
      to: "sdk.getMembers(groupId, cursor, limit)"
      via: "LoadAdminPanel and LoadMoreMembers intents"
      pattern: "sdk\\.getMembers"
    - from: "AppNavHost AdminPanelRoute"
      to: "AdminPanelViewModel"
      via: "koinViewModel + collectAsStateWithLifecycle"
      pattern: "koinViewModel<AdminPanelViewModel>"
    - from: "AdminPanelEvent.NavigateToRegisterMember"
      to: "navController.navigate(RegisterMemberRoute)"
      via: "event.collect in LaunchedEffect"
      pattern: "RegisterMemberRoute"
---

<objective>
Build the admin panel feature: MVI types, ViewModel that loads group info + paginated member list, screen composable, navigation wiring, and tests.

Purpose: This delivers requirements GRP-03 (view group info + members) and partially GRP-05 (admin-specific content). The admin panel is the core management screen from which admins view their group and navigate to member registration (Plan 04).

Output: Complete admin panel feature in `app:admin` module with ViewModel tests, wired into AppNavHost at the `AdminPanelRoute(groupId)` route.
</objective>

<execution_context>
@/Users/marc/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/marc/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-group-admin-ui/14-RESEARCH.md
@.planning/phases/14-group-admin-ui/14-01-SUMMARY.md
@.planning/phases/14-group-admin-ui/14-02-SUMMARY.md
@app/admin/build.gradle.kts
@app/dashboard/src/commonMain/kotlin/com/m2f/template/app/dashboard/DashboardViewModel.kt
@app/profile/src/commonMain/kotlin/com/m2f/template/app/profile/ProfileViewModel.kt
@app/profile/src/commonTest/kotlin/com/m2f/template/app/profile/ProfileViewModelTest.kt
@composeApp/src/commonMain/kotlin/com/m2f/template/navigation/AppNavHost.kt
@composeApp/src/commonMain/kotlin/com/m2f/template/navigation/Routes.kt
@composeApp/src/commonMain/kotlin/com/m2f/template/di/AppModule.kt
@core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/api/GroupApi.kt
@core/models/src/commonMain/kotlin/com/m2f/template/models/dto/GroupDtos.kt
@core/models/src/commonMain/kotlin/com/m2f/template/models/GroupRole.kt
@core/testing/src/commonMain/kotlin/com/m2f/template/core/testing/fakes/FakeGroupApiBuilder.kt
@core/testing/src/commonMain/kotlin/com/m2f/template/core/testing/fakes/FakeSdkBuilder.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: AdminPanel MVI types + ViewModel with init-dispatching</name>
  <files>
    app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/AdminPanelIntent.kt
    app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/AdminPanelModel.kt
    app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/AdminPanelMutation.kt
    app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/AdminPanelEvent.kt
    app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/AdminPanelViewModel.kt
  </files>
  <action>
    All files go in package `com.m2f.template.app.admin`.

    **AdminPanelIntent.kt:**
    ```kotlin
    sealed interface AdminPanelIntent {
        data class LoadAdminPanel(val groupId: String) : AdminPanelIntent
        data object LoadMoreMembers : AdminPanelIntent
        data object RegisterMemberClicked : AdminPanelIntent
    }
    ```

    **AdminPanelModel.kt:**
    ```kotlin
    data class AdminPanelModel(
        val groupId: String = "",
        val groupName: String = "",
        val groupSlug: String = "",
        val groupDescription: String = "",
        val memberCount: Int = 0,
        val members: List<MemberResponse> = emptyList(),
        val membersCursor: String? = null,
        val hasMoreMembers: Boolean = false,
        val isLoading: Boolean = false,
        val isLoadingMore: Boolean = false,
        val error: String? = null,
    )
    ```
    Import `MemberResponse` from `com.m2f.template.models.dto`.

    **AdminPanelMutation.kt:**
    ```kotlin
    sealed interface AdminPanelMutation {
        data class SetLoading(val loading: Boolean) : AdminPanelMutation
        data class SetLoadingMore(val loading: Boolean) : AdminPanelMutation
        data class SetGroupInfo(
            val groupId: String,
            val groupName: String,
            val groupSlug: String,
            val groupDescription: String,
            val memberCount: Int,
        ) : AdminPanelMutation
        data class SetMembers(
            val members: List<MemberResponse>,
            val cursor: String?,
            val hasMore: Boolean,
        ) : AdminPanelMutation
        data class AppendMembers(
            val members: List<MemberResponse>,
            val cursor: String?,
            val hasMore: Boolean,
        ) : AdminPanelMutation
        data class SetError(val error: String) : AdminPanelMutation
    }
    ```
    Import `MemberResponse` from `com.m2f.template.models.dto`.

    **AdminPanelEvent.kt:**
    ```kotlin
    sealed interface AdminPanelEvent {
        data class NavigateToRegisterMember(val groupId: String) : AdminPanelEvent
    }
    ```

    **AdminPanelViewModel.kt:**
    This ViewModel uses the **init-dispatching pattern** from ProfileViewModel. It receives `groupId` as a constructor parameter (provided via Koin assisted injection or SavedStateHandle — but since the existing pattern uses Sdk as sole constructor param, the groupId will be loaded from the route). Actually, looking at the existing pattern more carefully: ViewModels take only `Sdk` as a constructor param, and the route argument is dispatched as an intent.

    The ViewModel uses `SharingStarted.Eagerly` and force-inits `model`/`event` before dispatching `LoadAdminPanel` in init. But wait — the `groupId` isn't available at init time; it's passed as a route argument. So the init block does NOT dispatch — instead, the composable in AppNavHost dispatches `LoadAdminPanel(groupId)` after creation.

    Actually, revisit: ProfileViewModel dispatches `LoadProfile` in init because it doesn't need arguments. AdminPanelViewModel needs a `groupId`. Two approaches:
    - (A) Pass groupId via SavedStateHandle (Koin+Navigation integration) — complex, not used in the project
    - (B) Don't auto-dispatch in init; let the composable dispatch `LoadAdminPanel(groupId)` via LaunchedEffect

    **Use approach (B)** — simpler, matches how the project works. No Eagerly needed since there's no init dispatch. The composable will do:
    ```kotlin
    LaunchedEffect(Unit) { viewModel.take(AdminPanelIntent.LoadAdminPanel(groupId)) }
    ```

    ```kotlin
    class AdminPanelViewModel(
        private val sdk: Sdk,
    ) : MviViewModel<AdminPanelIntent, AdminPanelModel, AdminPanelMutation, AdminPanelEvent>(
        initialState = AdminPanelModel(),
    ) {

        override fun take(intent: AdminPanelIntent) {
            viewModelScope.launch {
                when (intent) {
                    is AdminPanelIntent.LoadAdminPanel -> handleLoadAdminPanel(intent.groupId)
                    is AdminPanelIntent.LoadMoreMembers -> handleLoadMoreMembers()
                    is AdminPanelIntent.RegisterMemberClicked -> {
                        val groupId = model.value.groupId
                        if (groupId.isNotBlank()) {
                            sendEvent(AdminPanelEvent.NavigateToRegisterMember(groupId))
                        }
                    }
                }
            }
        }

        private suspend fun handleLoadAdminPanel(groupId: String) {
            sendMutation(AdminPanelMutation.SetLoading(true))
            // Load group info
            sdk.getGroup(groupId).fold(
                ifLeft = { error ->
                    sendMutation(AdminPanelMutation.SetError(error.message))
                    sendMutation(AdminPanelMutation.SetLoading(false))
                },
                ifRight = { group ->
                    sendMutation(AdminPanelMutation.SetGroupInfo(
                        groupId = group.id,
                        groupName = group.name,
                        groupSlug = group.slug,
                        groupDescription = group.description,
                        memberCount = group.memberCount,
                    ))
                    // Load first page of members
                    sdk.getMembers(groupId, cursor = null, limit = 20).fold(
                        ifLeft = { error ->
                            sendMutation(AdminPanelMutation.SetError(error.message))
                            sendMutation(AdminPanelMutation.SetLoading(false))
                        },
                        ifRight = { page ->
                            sendMutation(AdminPanelMutation.SetMembers(
                                members = page.items,
                                cursor = page.cursor,
                                hasMore = page.hasMore,
                            ))
                            sendMutation(AdminPanelMutation.SetLoading(false))
                        },
                    )
                },
            )
        }

        private suspend fun handleLoadMoreMembers() {
            val current = model.value
            if (current.isLoadingMore || !current.hasMoreMembers || current.membersCursor == null) return
            sendMutation(AdminPanelMutation.SetLoadingMore(true))
            sdk.getMembers(current.groupId, cursor = current.membersCursor, limit = 20).fold(
                ifLeft = { error ->
                    sendMutation(AdminPanelMutation.SetError(error.message))
                    sendMutation(AdminPanelMutation.SetLoadingMore(false))
                },
                ifRight = { page ->
                    sendMutation(AdminPanelMutation.AppendMembers(
                        members = page.items,
                        cursor = page.cursor,
                        hasMore = page.hasMore,
                    ))
                    sendMutation(AdminPanelMutation.SetLoadingMore(false))
                },
            )
        }

        override suspend fun reduce(
            model: AdminPanelModel,
            mutation: AdminPanelMutation,
        ): AdminPanelModel = when (mutation) {
            is AdminPanelMutation.SetLoading -> model.copy(isLoading = mutation.loading)
            is AdminPanelMutation.SetLoadingMore -> model.copy(isLoadingMore = mutation.loading)
            is AdminPanelMutation.SetGroupInfo -> model.copy(
                groupId = mutation.groupId,
                groupName = mutation.groupName,
                groupSlug = mutation.groupSlug,
                groupDescription = mutation.groupDescription,
                memberCount = mutation.memberCount,
            )
            is AdminPanelMutation.SetMembers -> model.copy(
                members = mutation.members,
                membersCursor = mutation.cursor,
                hasMoreMembers = mutation.hasMore,
            )
            is AdminPanelMutation.AppendMembers -> model.copy(
                members = model.members + mutation.members,
                membersCursor = mutation.cursor,
                hasMoreMembers = mutation.hasMore,
            )
            is AdminPanelMutation.SetError -> model.copy(error = mutation.error)
        }
    }
    ```
    Import: `viewModelScope` from `androidx.lifecycle`, `MviViewModel` from `com.m2f.template.core.mvi`, `Sdk` from `com.m2f.template.sdk`, `kotlinx.coroutines.launch`.
  </action>
  <verify>
    Run `./gradlew :app:admin:compileKotlinMetadata` to verify the module compiles.
  </verify>
  <done>
    AdminPanelIntent has LoadAdminPanel(groupId), LoadMoreMembers, RegisterMemberClicked. AdminPanelModel contains group info fields, paginated member list, loading states, and error. AdminPanelViewModel loads group info + first page of members on LoadAdminPanel, supports LoadMoreMembers for pagination, and emits NavigateToRegisterMember event.
  </done>
</task>

<task type="auto">
  <name>Task 2: AdminPanelScreen + AppNavHost wiring + Koin registration + tests</name>
  <files>
    app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/AdminPanelScreen.kt
    app/admin/src/commonTest/kotlin/com/m2f/template/app/admin/AdminPanelViewModelTest.kt
    composeApp/src/commonMain/kotlin/com/m2f/template/navigation/AppNavHost.kt
    composeApp/src/commonMain/kotlin/com/m2f/template/di/AppModule.kt
  </files>
  <action>
    **AdminPanelScreen.kt** — Stateless composable following the existing pattern (DashboardScreen, ProfileScreen):
    ```kotlin
    @Composable
    fun AdminPanelScreen(
        state: AdminPanelModel,
        onLoadMore: () -> Unit,
        onRegisterMember: () -> Unit,
        onBack: () -> Unit,
        modifier: Modifier = Modifier,
    )
    ```
    Layout:
    - Top bar with back arrow (`<-` text button) and title `"> admin_panel"` using `TerminalText` with `typography.xxl.copy(fontWeight = FontWeight.Bold)`.
    - If `state.isLoading` and `state.members.isEmpty()`: show `TerminalText("// loading...")`.
    - If `state.error != null` and `state.members.isEmpty()`: show `TerminalCard` with error message using `CardVariant.Default` and `TerminalBadge(text = "error: ${state.error}", variant = BadgeVariant.Danger)`.
    - Group info card: `TerminalCard(title = state.groupName, description = "slug: ${state.groupSlug}")` containing:
      - `TerminalText` for description
      - `TerminalBadge(text = "${state.memberCount} members", variant = BadgeVariant.Info)`
    - Members section: `TerminalTable` with headers `["NAME", "EMAIL", "ROLE", "JOINED"]`:
      - Each member row: `TerminalTableRow` with `TerminalTableCell` for name, email, role badge (`TerminalBadge` with variant based on role — `BadgeVariant.Success` for Owner/Admin, `BadgeVariant.Default` for Member), joinedAt.
      - Handle empty `joinedAt` gracefully (display `"-"` if blank).
    - If `state.hasMoreMembers`: Show a `TerminalButton("load_more", onClick = onLoadMore)`. If `state.isLoadingMore`, show `TerminalText("// loading...")` instead.
    - Register member action: `TerminalButton("+ register_member", onClick = onRegisterMember)` placed below the member table.

    Use existing design system components from `app:designsystem` (`TerminalText`, `TerminalCard`, `TerminalTable`, `TerminalTableRow`, `TerminalTableCell`, `TerminalBadge`, `TerminalList`). Follow `DashboardScreen.kt` styling patterns (colors from `TerminalTheme.colors`, typography from `TerminalTheme.typography`).

    Wrap content in a `Column` with `Modifier.fillMaxSize().verticalScroll(rememberScrollState()).padding(32.dp)` for scrolling. Use `Arrangement.spacedBy(24.dp)`.

    **AppNavHost.kt** — Add the `AdminPanelRoute` composable block AFTER the existing `ProfileRoute` block and BEFORE `ForgotPasswordRoute`:
    ```kotlin
    composable<AdminPanelRoute> { backStackEntry ->
        val route = backStackEntry.toRoute<AdminPanelRoute>()
        val viewModel = koinViewModel<AdminPanelViewModel>()
        val state by viewModel.model.collectAsStateWithLifecycle()

        LaunchedEffect(Unit) {
            viewModel.take(AdminPanelIntent.LoadAdminPanel(route.groupId))
        }

        AdminPanelScreen(
            state = state,
            onLoadMore = { viewModel.take(AdminPanelIntent.LoadMoreMembers) },
            onRegisterMember = { viewModel.take(AdminPanelIntent.RegisterMemberClicked) },
            onBack = { navController.popBackStack() },
        )
        LaunchedEffect(Unit) {
            viewModel.event.collect { event ->
                when (event) {
                    is AdminPanelEvent.NavigateToRegisterMember -> {
                        navController.navigate(RegisterMemberRoute(groupId = event.groupId))
                    }
                }
            }
        }
    }
    ```
    Import `AdminPanelIntent`, `AdminPanelScreen`, `AdminPanelViewModel`, `AdminPanelEvent` from `com.m2f.template.app.admin`. Import `toRoute` if not already imported.

    **AppModule.kt** — Add `viewModelOf(::AdminPanelViewModel)` to the `appModule`:
    ```kotlin
    import com.m2f.template.app.admin.AdminPanelViewModel
    // ...
    val appModule = module {
        viewModelOf(::LoginViewModel)
        viewModelOf(::RegisterViewModel)
        viewModelOf(::ForgotPasswordViewModel)
        viewModelOf(::DashboardViewModel)
        viewModelOf(::ProfileViewModel)
        viewModelOf(::AdminPanelViewModel)
    }
    ```

    **AdminPanelViewModelTest.kt** — Following the exact test DSL from ProfileViewModelTest:
    ```kotlin
    class AdminPanelViewModelTest : ViewModelTest() {

        private val testGroup = GroupResponse(
            id = "g1", name = "Test Group", slug = "test-group",
            description = "A test group", createdBy = "u1", memberCount = 2,
            createdAt = "2026-01-01", updatedAt = "2026-01-01",
        )

        private val testMembers = PaginatedMemberResponse(
            items = listOf(
                MemberResponse(userId = "u1", email = "admin@test.com", name = "Admin User", role = GroupRole.Admin, joinedAt = "2026-01-01"),
                MemberResponse(userId = "u2", email = "member@test.com", name = "Member User", role = GroupRole.Member, joinedAt = "2026-01-02"),
            ),
            cursor = null,
            hasMore = false,
        )
    }
    ```

    Tests to write (all use `fakeSdk { group { ... } }` DSL):

    1. **`LoadAdminPanel loads group info and members`:**
       ```kotlin
       @Test
       fun `LoadAdminPanel loads group info and members`() {
           val sdk = fakeSdk {
               group {
                   getGroup { Either.Right(testGroup) }
                   getMembers { _, _, _ -> Either.Right(testMembers) }
               }
           }
           val viewModel = AdminPanelViewModel(sdk)
           viewModel.test {
               intent(AdminPanelIntent.LoadAdminPanel("g1"))
               model(AdminPanelModel(isLoading = true))
               model(AdminPanelModel(
                   isLoading = true,
                   groupId = "g1", groupName = "Test Group", groupSlug = "test-group",
                   groupDescription = "A test group", memberCount = 2,
               ))
               model(AdminPanelModel(
                   groupId = "g1", groupName = "Test Group", groupSlug = "test-group",
                   groupDescription = "A test group", memberCount = 2,
                   members = testMembers.items, membersCursor = null, hasMoreMembers = false,
               ))
               // SetLoading(false) conflated with above since isLoading is already false in default
               // Actually, the SetMembers mutation doesn't set isLoading. Then SetLoading(false) fires:
               model(AdminPanelModel(
                   isLoading = false,
                   groupId = "g1", groupName = "Test Group", groupSlug = "test-group",
                   groupDescription = "A test group", memberCount = 2,
                   members = testMembers.items, membersCursor = null, hasMoreMembers = false,
               ))
           }
       }
       ```
       **IMPORTANT NOTE on conflation**: Since SetGroupInfo changes many fields (model becomes distinct), it WILL emit. Then SetMembers also changes members (distinct again), so it WILL emit. Then SetLoading(false) only changes isLoading from `true` → the model at this point has `isLoading` from the initial SetLoading(true). Actually trace it:
       - Initial: `AdminPanelModel()` — `isLoading = false`
       - SetLoading(true) → `isLoading = true` — DISTINCT → emits
       - SetGroupInfo → `groupId = "g1", ..., isLoading = true` — DISTINCT → emits
       - SetMembers → `members = [...], isLoading = true` — DISTINCT → emits
       - SetLoading(false) → `isLoading = false` — DISTINCT → emits

       So the test should assert 4 model emissions after the intent. However, since this is a sync fake (no actual network delay), all mutations fire within a single `launch` coroutine tick. StateFlow may conflate intermediate states. The safest approach is to be aware that some intermediate emissions may be conflated and the test may only see a subset. For reliable testing, the test should expect the **final settled state** at minimum.

       Adjust the test to only assert states that represent meaningful transitions. Given the sync fake behavior, the most reliable test is:
       ```kotlin
       @Test
       fun `LoadAdminPanel loads group info and members`() {
           val sdk = fakeSdk {
               group {
                   getGroup { Either.Right(testGroup) }
                   getMembers { _, _, _ -> Either.Right(testMembers) }
               }
           }
           val viewModel = AdminPanelViewModel(sdk)
           viewModel.test {
               intent(AdminPanelIntent.LoadAdminPanel("g1"))
               model(AdminPanelModel(isLoading = true))
               model(AdminPanelModel(
                   groupId = "g1", groupName = "Test Group", groupSlug = "test-group",
                   groupDescription = "A test group", memberCount = 2,
                   members = testMembers.items, membersCursor = null, hasMoreMembers = false,
               ))
           }
       }
       ```
       This skips intermediate emissions that may be conflated. If the test fails because of additional emissions, add the intermediate states. Start with this minimal assertion set and adjust based on actual emissions.

    2. **`LoadAdminPanel with getGroup error shows error`:**
       ```kotlin
       @Test
       fun `LoadAdminPanel with getGroup error shows error`() {
           val sdk = fakeSdk {
               group {
                   getGroup { Either.Left(AppError.Group.Forbidden(message = "Access denied")) }
               }
           }
           val viewModel = AdminPanelViewModel(sdk)
           viewModel.test {
               intent(AdminPanelIntent.LoadAdminPanel("g1"))
               model(AdminPanelModel(isLoading = true))
               model(AdminPanelModel(error = "Access denied"))
           }
       }
       ```

    3. **`RegisterMemberClicked emits NavigateToRegisterMember`:**
       ```kotlin
       @Test
       fun `RegisterMemberClicked emits NavigateToRegisterMember`() {
           val sdk = fakeSdk {
               group {
                   getGroup { Either.Right(testGroup) }
                   getMembers { _, _, _ -> Either.Right(testMembers) }
               }
           }
           val viewModel = AdminPanelViewModel(sdk)
           viewModel.test {
               intent(AdminPanelIntent.LoadAdminPanel("g1"))
               model(AdminPanelModel(isLoading = true))
               model(AdminPanelModel(
                   groupId = "g1", groupName = "Test Group", groupSlug = "test-group",
                   groupDescription = "A test group", memberCount = 2,
                   members = testMembers.items, membersCursor = null, hasMoreMembers = false,
               ))
               intent(AdminPanelIntent.RegisterMemberClicked)
               event(AdminPanelEvent.NavigateToRegisterMember("g1"))
           }
       }
       ```

    4. **`LoadMoreMembers appends to member list`:**
       ```kotlin
       @Test
       fun `LoadMoreMembers appends to member list`() {
           val firstPage = PaginatedMemberResponse(
               items = listOf(testMembers.items.first()),
               cursor = "cursor-1",
               hasMore = true,
           )
           val secondPage = PaginatedMemberResponse(
               items = listOf(testMembers.items.last()),
               cursor = null,
               hasMore = false,
           )
           var callCount = 0
           val sdk = fakeSdk {
               group {
                   getGroup { Either.Right(testGroup) }
                   getMembers { _, _, _ ->
                       callCount++
                       if (callCount == 1) Either.Right(firstPage) else Either.Right(secondPage)
                   }
               }
           }
           val viewModel = AdminPanelViewModel(sdk)
           viewModel.test {
               intent(AdminPanelIntent.LoadAdminPanel("g1"))
               model(AdminPanelModel(isLoading = true))
               model(AdminPanelModel(
                   groupId = "g1", groupName = "Test Group", groupSlug = "test-group",
                   groupDescription = "A test group", memberCount = 2,
                   members = firstPage.items, membersCursor = "cursor-1", hasMoreMembers = true,
               ))
               intent(AdminPanelIntent.LoadMoreMembers)
               model(AdminPanelModel(
                   groupId = "g1", groupName = "Test Group", groupSlug = "test-group",
                   groupDescription = "A test group", memberCount = 2,
                   members = testMembers.items, membersCursor = null, hasMoreMembers = false,
                   isLoadingMore = false,
               ))
           }
       }
       ```

    Import: `arrow.core.Either`, `com.m2f.template.core.testing.ViewModelTest`, `com.m2f.template.core.testing.fakes.fakeSdk`, `com.m2f.template.core.testing.test`, `com.m2f.template.models.AppError`, `com.m2f.template.models.GroupRole`, `com.m2f.template.models.dto.*`, `kotlin.test.Test`.

    **CRITICAL testing note on StateFlow conflation:** If tests fail because of unexpected intermediate model emissions, add those intermediate states to the assertion chain. The sync fake may cause rapid-fire mutations that StateFlow conflates differently across JVM implementations. When in doubt, run the test once, observe the actual emission sequence, and adjust assertions accordingly.
  </action>
  <verify>
    Run `./gradlew :app:admin:compileKotlinMetadata` to verify admin module compiles.
    Run `./gradlew :app:admin:jvmTest` to verify all tests pass.
    Run `./gradlew :composeApp:compileKotlinMetadata` to verify AppNavHost compiles with new route.
  </verify>
  <done>
    AdminPanelScreen renders group info + member table using design system components. AppNavHost has `AdminPanelRoute` composable that creates AdminPanelViewModel, dispatches LoadAdminPanel(groupId), and handles NavigateToRegisterMember event. AdminPanelViewModel registered in Koin's appModule. At least 4 tests pass covering: successful load, error handling, register navigation, and pagination.
  </done>
</task>

</tasks>

<verification>
1. `./gradlew :app:admin:compileKotlinMetadata` — admin module compiles
2. `./gradlew :app:admin:jvmTest` — all AdminPanelViewModel tests pass
3. `./gradlew :composeApp:compileKotlinMetadata` — AppNavHost + AppModule compile
4. Verify AdminPanelModel has group info fields + paginated member list
5. Verify AdminPanelEvent.NavigateToRegisterMember carries groupId
6. Verify AdminPanelScreen is a stateless composable (no ViewModel injection)
</verification>

<success_criteria>
- AdminPanel MVI types (Intent/Model/Mutation/Event) follow the project's sealed interface pattern
- AdminPanelViewModel loads group info + paginated members via SDK
- AdminPanelViewModel handles LoadMoreMembers for cursor-based pagination
- AdminPanelScreen renders group card + member table using TerminalTheme design system
- AppNavHost wires AdminPanelRoute with koinViewModel, LaunchedEffect for intent dispatch, and event collection
- AdminPanelViewModel registered in AppModule
- 4+ tests pass covering load, error, pagination, and navigation
</success_criteria>

<output>
After completion, create `.planning/phases/14-group-admin-ui/14-03-SUMMARY.md`
</output>
