---
phase: 18.2-invitation-acceptance-flow
verified: 2026-02-25T19:30:00Z
status: passed
score: 5/5 must-haves verified
re_verification:
  previous_status: gaps_found
  previous_score: 4/5
  gaps_closed:
    - "Expired invitation shows 'Request new invitation' button as an actionable next step"
    - "Clicking 'Request new invitation' navigates user away from the dead-end expired screen"
    - "ROADMAP Success Criterion #2 text says 'Dashboard' not 'Admin Panel' (already fixed by Plan 04)"
    - "REQUIREMENTS.md traceability shows INVITE-02 and INVITE-04 as Phase 18.2 and Complete"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Open invitation link while not logged in"
    expected: "See invitation details (group name, inviter, role) with 'Log in to accept' and 'Create account to accept' buttons visible. No 'Accept Invitation' button."
    why_human: "Visual layout and button rendering cannot be verified programmatically"
  - test: "From invite screen, click 'Log in to accept', log in with existing account"
    expected: "After login, user is automatically added to the group and navigated to Dashboard (not Admin Panel)"
    why_human: "Requires real server and database to verify invitation acceptance and navigation"
  - test: "From invite screen, click 'Create account to accept', register new account"
    expected: "After registration, user is created AND linked to group, navigated to Dashboard"
    why_human: "Requires live server with InvitationService to verify transactional behavior"
  - test: "Open expired invitation link"
    expected: "Expired shows warning alert plus hint text ('contact the group administrator...') plus 'Request new invitation' secondary button. No accept/login/register buttons shown."
    why_human: "Requires database record with expired invitation"
  - test: "Click 'Request new invitation' button on expired invite screen"
    expected: "Navigates to Login screen; expired invite screen is cleared from back stack"
    why_human: "Requires live app session with expired invitation state"
  - test: "Open already-accepted invitation link"
    expected: "Shows info alert with no action buttons"
    why_human: "Requires database record in accepted state"
---

# Phase 18.2: Invitation Acceptance Flow Verification Report

**Phase Goal:** Unauthenticated users can accept invitations by logging in or registering with automatic group linking
**Verified:** 2026-02-25T19:30:00Z
**Status:** passed
**Re-verification:** Yes — after Plan 05 gap closure (previous status: gaps_found, score: 4/5)

## Goal Achievement

### Observable Truths

Truths are the ROADMAP.md Phase 18.2 Success Criteria (the authoritative contract).

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Unauthenticated user opens invite link → sees invitation details + Login/Register buttons (not Accept) | VERIFIED | `InviteAcceptScreen.kt:202` guards action buttons with `!state.isExpired && !state.isAlreadyAccepted && !state.acceptSuccess`; inside that block `state.isLoggedIn` branches to show either Accept (authenticated) or Login+Register (unauthenticated). `InviteAcceptViewModel.kt:45-46` sets `isLoggedIn` from `tokenStorage.getAccessToken() != null`. |
| 2 | User clicks Login → logs in with invitation token context → auto-linked to group → navigates to Dashboard | VERIFIED | `InviteAcceptIntent.GoToLogin` → `InviteAcceptEvent.NavigateToLogin(token)` → `AppNavHost.kt:367-370` navigates to `LoginRoute(invitationToken = event.token)` → `AppNavHost.kt:84-87` sets token via `LoginIntent.SetInvitationToken` → `LoginViewModel.handlePostLogin()` calls `sdk.acceptInvitation()` → `LoginEvent.NavigateToGroup` → `AppNavHost.kt:109-113` navigates to `DashboardRoute`. ROADMAP text already corrected to "Dashboard" by Plan 04. |
| 3 | User clicks Register → registers with invitation token → auto-linked to group → navigates to Dashboard | VERIFIED | `InviteAcceptIntent.GoToRegister` → `InviteAcceptEvent.NavigateToRegister(token)` → `AppNavHost.kt:372-375` navigates to `RegisterRoute(invitationToken = event.token)` → `AppNavHost.kt:125-128` sets token via `RegisterIntent.SetInvitationToken` → `RegisterViewModel.kt:72` includes `invitationToken = current.invitationToken` in `RegisterRequest` → server `AuthService.kt:97-99` calls `onRegistered` callback → `ServerModule.kt:52` calls `invitationService.acceptInvitation()`. `RegisterEvent.NavigateToDashboard` → `AppNavHost.kt:152-156` navigates to `DashboardRoute`. |
| 4 | Expired invitation shows error message with "Request new invitation" option (no accept/register allowed) | VERIFIED | `InviteAcceptScreen.kt:142-160`: expired block shows `invite_expired` TerminalAlert (Warning), `invite_expired_hint` TerminalText ("contact the group administrator..."), and a Secondary `TerminalButton` with `invite_request_new` text wired to `onRequestNewInvitation`. Action buttons are outside this block and guarded by `!state.isExpired` at line 202. `AppNavHost.kt:356` passes `onRequestNewInvitation = { viewModel.take(InviteAcceptIntent.RequestNewInvitation) }`. `AppNavHost.kt:377-381` handles `InviteAcceptEvent.RequestedNewInvitation` by navigating to `LoginRoute()` with `popUpTo<InviteAcceptRoute> { inclusive = true }`. |
| 5 | Already-accepted invitation shows appropriate error state | VERIFIED | `InviteAcceptScreen.kt:163-168` shows `invite_already_accepted` TerminalAlert (Info) when `state.isAlreadyAccepted == true`. Action buttons guarded by `!state.isAlreadyAccepted` at line 202. |

**Score:** 5/5 truths verified

---

## Required Artifacts (Plan 05 Gap Closure Focus)

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/InviteAcceptIntent.kt` | `RequestNewInvitation` data object | VERIFIED | Line 30: `data object RequestNewInvitation : InviteAcceptIntent` |
| `app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/InviteAcceptEvent.kt` | `RequestedNewInvitation` data object | VERIFIED | Line 25: `data object RequestedNewInvitation : InviteAcceptEvent` |
| `app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/InviteAcceptViewModel.kt` | Handler for `RequestNewInvitation` intent | VERIFIED | Lines 33-35: `is InviteAcceptIntent.RequestNewInvitation -> { sendEvent(InviteAcceptEvent.RequestedNewInvitation) }` |
| `app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/InviteAcceptScreen.kt` | `onRequestNewInvitation` callback + button/hint in expired block | VERIFIED | Line 57: param present. Lines 142-160: expired block contains alert, hint text, and Secondary button wired to callback. |
| `app/auth/src/commonMain/composeResources/values/strings.xml` | EN `invite_request_new` and `invite_expired_hint` strings | VERIFIED | Lines 155-156: both strings present |
| `app/auth/src/commonMain/composeResources/values-es/strings.xml` | ES `invite_request_new` and `invite_expired_hint` strings | VERIFIED | Lines 155-156: both strings present in Spanish |
| `composeApp/src/commonMain/kotlin/com/m2f/template/navigation/AppNavHost.kt` | `onRequestNewInvitation` callback + `RequestedNewInvitation` event handler | VERIFIED | Line 356: callback wired. Lines 377-381: event handler navigates to `LoginRoute()` with `popUpTo<InviteAcceptRoute> { inclusive = true }` |

---

## Key Link Verification (Plan 05 Gap Closure)

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `InviteAcceptScreen` expired block button | `InviteAcceptIntent.RequestNewInvitation` | `onRequestNewInvitation` callback | WIRED | `InviteAcceptScreen.kt:156`: `onClick = onRequestNewInvitation`; `AppNavHost.kt:356`: `onRequestNewInvitation = { viewModel.take(InviteAcceptIntent.RequestNewInvitation) }` |
| `InviteAcceptViewModel.RequestNewInvitation` handler | `InviteAcceptEvent.RequestedNewInvitation` | `sendEvent()` call | WIRED | `InviteAcceptViewModel.kt:33-35`: intent handler calls `sendEvent(InviteAcceptEvent.RequestedNewInvitation)` |
| `AppNavHost InviteAcceptEvent.RequestedNewInvitation` handler | `LoginRoute()` | `navController.navigate()` | WIRED | `AppNavHost.kt:377-381`: `navController.navigate(LoginRoute()) { popUpTo<InviteAcceptRoute> { inclusive = true } }` |

### Previously Verified Key Links (Regression Check — All Pass)

| From | To | Via | Status |
|------|----|-----|--------|
| `InviteAcceptViewModel` | `TokenStorage.getAccessToken()` | Null check in `handleLoadInvitation()` | WIRED |
| `InviteAcceptScreen` Login/Register buttons | `InviteAcceptIntent.GoToLogin/GoToRegister` | `onClick` callbacks | WIRED |
| `LoginViewModel.handlePostLogin()` | `sdk.acceptInvitation()` | Conditional call when `invitationToken != null` | WIRED |
| `RegisterViewModel.handleRegister()` | `RegisterRequest.invitationToken` | Field assignment | WIRED |
| `AppNavHost InviteAcceptRoute` | `LoginRoute(invitationToken=...)` / `RegisterRoute(invitationToken=...)` | NavigateToLogin/NavigateToRegister events | WIRED |
| `LoginEvent.NavigateToGroup` | `DashboardRoute` | AppNavHost event handler | WIRED |
| `RegisterEvent.NavigateToGroup` | `DashboardRoute` | AppNavHost event handler | WIRED |

---

## Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| INVITE-02 | 18.2-01, 18.2-02, 18.2-03 | Recipient can accept invite via token link — creates/activates account and joins group with specified role | SATISFIED | Login path: `sdk.acceptInvitation()` post-login links user to group. Register path: `RegisterRequest.invitationToken` → server `AuthService.onRegistered` → `InvitationService.acceptInvitation()`. REQUIREMENTS.md checkbox `[x]` with `-- Phase 18.2` annotation (line 56). Traceability table (line 141): Phase 18.2 / Complete. |
| INVITE-04 | 18.2-02 | Invite tokens expire after configurable duration and cannot be reused | SATISFIED | `InviteAcceptScreen.kt:142-160` shows expired alert + hint + "Request new invitation" button. Action buttons blocked by `!state.isExpired` guard at line 202. Expiry data from server `invitation.isExpired`. `isAlreadyAccepted` guard prevents reuse. REQUIREMENTS.md checkbox `[x]` with `-- Phase 18.2` annotation (line 58). Traceability table (line 143): Phase 18.2 / Complete. |

---

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `AppNavHost.kt` | ~405 | `// placeholder for now - will be wired per-platform.` in `DocumentsRoute` upload handler | Info | Unrelated to Phase 18.2 (Phase 19 territory). No impact on invitation acceptance flow. |

Zero anti-patterns detected in Phase 18.2 invitation acceptance code paths (including Plan 05 additions).

---

## Human Verification Required

### 1. Visual: Unauthenticated Invite Accept Screen

**Test:** Open an invitation link while not logged in
**Expected:** Invitation details (group name, inviter name, role) are visible with "$ login_to_accept()" and "$ create_account_to_accept()" buttons. No "Accept Invitation" button is shown.
**Why human:** Visual layout and button placement cannot be verified programmatically

### 2. End-to-End: Login with Invitation Token

**Test:** From invite screen, click "Log in to accept", log in with an existing account
**Expected:** After login, user is automatically added to the group and navigated to Dashboard
**Why human:** Requires live server and database to verify invitation acceptance and navigation

### 3. End-to-End: Register with Invitation Token

**Test:** From invite screen, click "Create account to accept", register a new account
**Expected:** After registration, user is created AND automatically linked to group, then navigated to Dashboard
**Why human:** Requires live server with InvitationService wired to verify transactional behavior

### 4. Expired Invitation State

**Test:** Open an expired invitation link
**Expected:** Warning alert "This invitation has expired." is shown, followed by dim hint text "// contact the group administrator to receive a new invitation", followed by a secondary "$ request_new_invite()" button. No login/register/accept buttons are visible.
**Why human:** Requires database record with expired invitation

### 5. "Request New Invitation" Navigation

**Test:** On expired invite screen, click "$ request_new_invite()"
**Expected:** Navigates to Login screen; expired invite screen is cleared from the back stack (back button does not return to it)
**Why human:** Requires live app session with expired invitation state to verify navigation and back-stack behavior

### 6. Already-Accepted Invitation State

**Test:** Open an already-accepted invitation link
**Expected:** Info alert with no action buttons visible
**Why human:** Requires database record in accepted state

---

## Re-verification Summary

### Gap Closed by Plan 05

**Root cause of gap:** ROADMAP Success Criterion #4 required a "Request new invitation" option on expired invitation screens, but Plans 01-04 implemented only the error alert with action button blocking — leaving users at a dead-end with no way forward.

**Fix applied (commits cb0e628 + 84b1ddc):**

1. `InviteAcceptIntent.RequestNewInvitation` — new data object intent
2. `InviteAcceptEvent.RequestedNewInvitation` — new data object event
3. `InviteAcceptViewModel` — intent handler emits the event
4. `InviteAcceptScreen` — `onRequestNewInvitation` callback parameter; expired block now renders hint text and Secondary button
5. EN + ES string resources — `invite_request_new` and `invite_expired_hint`
6. `AppNavHost` — callback wiring + event handler navigates to `LoginRoute()` with back-stack cleanup
7. `REQUIREMENTS.md` — INVITE-02 and INVITE-04 marked `[x]` Phase 18.2 / Complete
8. `ROADMAP.md` — Plan 05 marked `[x]` complete in plan list

### Documentation Status

- ROADMAP.md Phase 18.2 Success Criteria #2 and #3: already said "Dashboard" (fixed by Plan 04, confirmed by Plan 05)
- REQUIREMENTS.md INVITE-02 traceability: Phase 18.2 / Complete (line 141)
- REQUIREMENTS.md INVITE-04 traceability: Phase 18.2 / Complete (line 143)
- REQUIREMENTS.md checkboxes: both `[x]` with `-- Phase 18.2` annotation (lines 56, 58)

---

_Verified: 2026-02-25T19:30:00Z_
_Verifier: Claude (gsd-verifier)_
