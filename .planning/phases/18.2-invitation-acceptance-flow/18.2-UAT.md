---
status: diagnosed
phase: 18.2-invitation-acceptance-flow
source: [18.2-01-SUMMARY.md, 18.2-02-SUMMARY.md, 18.2-03-SUMMARY.md]
started: 2026-02-23T13:00:00Z
updated: 2026-02-23T13:30:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Unauthenticated user sees Login/Register buttons on invite screen
expected: Open an invitation link while not logged in. The InviteAcceptScreen shows the invitation details (group name, inviter) and displays "Log in to accept" and "Create account to accept" buttons — NOT an "Accept Invitation" button.
result: pass

### 2. Authenticated user sees Accept button on invite screen
expected: Open an invitation link while already logged in. The InviteAcceptScreen shows the invitation details and displays the "Accept Invitation" button (not Login/Register buttons).
result: pass

### 3. Login with invitation token auto-links to group
expected: From the invite screen (unauthenticated), click "Log in to accept" → login screen appears → log in with valid credentials → after login, you are automatically linked to the group and navigated to the Admin Panel (or dashboard).
result: issue
reported: "I've been navigated to the admin panel (which I should not be able to open as I've been invited as a regular member so admin panel is not a valid route for it) — shows 'error: You do not have permission to access this resource'"
severity: major

### 4. Register with invitation token auto-links to group
expected: From the invite screen (unauthenticated), click "Create account to accept" → register screen appears → register a new account → after registration, you are automatically linked to the group and navigated to the dashboard.
result: pass

### 5. Expired invitation shows error state
expected: Open an invitation link for an expired invitation. The screen shows the invitation details but with an error/warning message. The Login/Register/Accept buttons are NOT shown (no way to accept an expired invitation).
result: pass

### 6. Already-accepted invitation shows error state
expected: Open an invitation link that has already been accepted. The screen shows an appropriate message indicating the invitation was already used. No action buttons are shown.
result: pass

### 7. Login/Register without invitation still works normally
expected: Navigate to login or register directly (not from an invitation link). Login and register work exactly as before — no mention of invitations, normal dashboard navigation after auth.
result: pass

## Summary

total: 7
passed: 6
issues: 1
pending: 0
skipped: 0

## Gaps

- truth: "After login with invitation token, user is navigated to appropriate destination based on their role"
  status: diagnosed
  reason: "User reported: navigated to Admin Panel after login with invitation token, but invited as regular member — Admin Panel returns permission error"
  severity: major
  test: 3
  root_cause: "All 3 NavigateToGroup event handlers in AppNavHost.kt navigate to AdminPanelRoute instead of DashboardRoute. Invited users are regular members without admin permissions, so AdminPanelRoute returns a permission error. The navigation target should be DashboardRoute (the group dashboard) not AdminPanelRoute (admin-only)."
  artifacts:
    - path: "composeApp/src/commonMain/kotlin/com/m2f/template/navigation/AppNavHost.kt"
      issue: "Lines 104, 147, 356: LoginEvent.NavigateToGroup, RegisterEvent.NavigateToGroup, InviteAcceptEvent.NavigateToGroup all navigate to AdminPanelRoute(groupId=event.groupId)"
  missing:
    - "Change LoginEvent.NavigateToGroup handler (line 104) to navigate to DashboardRoute instead of AdminPanelRoute"
    - "Change RegisterEvent.NavigateToGroup handler (line 147) to navigate to DashboardRoute instead of AdminPanelRoute"
    - "Change InviteAcceptEvent.NavigateToGroup handler (line 356) to navigate to DashboardRoute instead of AdminPanelRoute"
  debug_session: "Root cause identified via code inspection of AppNavHost.kt — all NavigateToGroup handlers use AdminPanelRoute which requires admin permissions"
