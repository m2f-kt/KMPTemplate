---
phase: 18.2-invitation-acceptance-flow
plan: 04
subsystem: navigation
tags: [navigation, invitation-flow, gap-closure, dashboard-route]

# Dependency graph
requires:
  - phase: 18.2-invitation-acceptance-flow
    plan: 03
    provides: NavigateToGroup events in Login/Register/InviteAccept MVI layers, AppNavHost wiring
provides:
  - Correct NavigateToGroup navigation targets (DashboardRoute instead of AdminPanelRoute)
affects:
  - Invitation acceptance user experience (no more permission errors for regular members)

# Tech stack
added: []
patterns:
  - "Universal landing page pattern: DashboardRoute as default post-auth destination for all user roles"

# Key files
created: []
modified:
  - composeApp/src/commonMain/kotlin/com/m2f/template/navigation/AppNavHost.kt

# Decisions
key-decisions:
  - "DashboardRoute as universal post-invitation landing page — invited users are regular members without admin permissions, AdminPanelRoute causes permission errors"
  - "popUpTo(0) { inclusive = true } for InviteAcceptEvent.NavigateToGroup — cannot popUpTo DashboardRoute when navigating TO DashboardRoute"

# Metrics
duration: 2min
completed: 2026-02-25
tasks_completed: 1
tasks_total: 1
---

# Phase 18.2 Plan 04: NavigateToGroup DashboardRoute Fix Summary

All 3 NavigateToGroup handlers in AppNavHost.kt corrected from AdminPanelRoute to DashboardRoute -- invited users land on the universal dashboard instead of the admin panel they lack permissions for.

## Performance

- **Duration:** ~2 min (verification only -- code fix already applied)
- **Started:** 2026-02-25T18:45:53Z
- **Completed:** 2026-02-25T18:48:02Z
- **Tasks:** 1/1
- **Files modified:** 0 (fix already present in codebase)

## Accomplishments

- Verified all 3 NavigateToGroup handlers navigate to DashboardRoute:
  1. **LoginEvent.NavigateToGroup** (line 109): `navController.navigate(DashboardRoute)` with `popUpTo<LoginRoute> { inclusive = true }`
  2. **RegisterEvent.NavigateToGroup** (line 152): `navController.navigate(DashboardRoute)` with `popUpTo<LoginRoute> { inclusive = true }`
  3. **InviteAcceptEvent.NavigateToGroup** (line 361): `navController.navigate(DashboardRoute)` with `popUpTo(0) { inclusive = true }`
- Confirmed zero NavigateToGroup handlers reference AdminPanelRoute
- `./gradlew composeApp:compileKotlinWasmJs` passes (BUILD SUCCESSFUL)

## Task Commits

The code fix was already applied in a prior commit. No new code changes were needed.

- **Prior fix:** `320fe9e` (feat(19-05)) -- NavigateToGroup handlers were corrected from AdminPanelRoute to DashboardRoute as part of the documents UI module addition

## Files Verified

- `composeApp/src/commonMain/kotlin/com/m2f/template/navigation/AppNavHost.kt` -- All 3 NavigateToGroup handlers use DashboardRoute

## Decisions Made

- **DashboardRoute over AdminPanelRoute:** Invited users are regular members. AdminPanelRoute requires admin permissions, causing "permission denied" errors. DashboardRoute is the universal landing page accessible to all roles.
- **groupId not needed:** The server links the user to the group during invitation acceptance -- the dashboard shows their groups automatically.

## Deviations from Plan

### Pre-existing Fix

The code fix described in this plan was already applied in commit `320fe9e` (feat(19-05): add client SDK document API and documents UI module). The fix was bundled into that commit during Phase 19 execution. This plan execution verified the fix is correct and documented the closure.

No additional code changes were required.

## Verification

1. `./gradlew composeApp:compileKotlinWasmJs` -- PASSED (BUILD SUCCESSFUL in 13s)
2. NavigateToGroup handlers using DashboardRoute: 3/3
3. NavigateToGroup handlers using AdminPanelRoute: 0/3

## Self-Check: PASSED

All verification criteria met:
- [x] LoginEvent.NavigateToGroup -> DashboardRoute (not AdminPanelRoute)
- [x] RegisterEvent.NavigateToGroup -> DashboardRoute (not AdminPanelRoute)
- [x] InviteAcceptEvent.NavigateToGroup -> DashboardRoute (not AdminPanelRoute)
- [x] Code compiles without errors

---
*Phase: 18.2-invitation-acceptance-flow*
*Completed: 2026-02-25*
