---
phase: 18.2-invitation-acceptance-flow
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [composeApp/src/commonMain/kotlin/com/m2f/template/navigation/AppNavHost.kt]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "After login with invitation token, user is navigated to DashboardRoute (not AdminPanelRoute)"
    - "After register with invitation token, user is navigated to DashboardRoute (not AdminPanelRoute)"
    - "After accepting invitation while authenticated, user is navigated to DashboardRoute (not AdminPanelRoute)"
  artifacts:
    - path: "composeApp/src/commonMain/kotlin/com/m2f/template/navigation/AppNavHost.kt"
      provides: "Correct NavigateToGroup navigation targets"
      contains: "DashboardRoute"
  key_links:
    - from: "LoginEvent.NavigateToGroup handler"
      to: "DashboardRoute"
      via: "navController.navigate(DashboardRoute)"
      pattern: "NavigateToGroup.*DashboardRoute"
    - from: "RegisterEvent.NavigateToGroup handler"
      to: "DashboardRoute"
      via: "navController.navigate(DashboardRoute)"
      pattern: "NavigateToGroup.*DashboardRoute"
    - from: "InviteAcceptEvent.NavigateToGroup handler"
      to: "DashboardRoute"
      via: "navController.navigate(DashboardRoute)"
      pattern: "NavigateToGroup.*DashboardRoute"
---

<objective>
Fix NavigateToGroup navigation to use DashboardRoute instead of AdminPanelRoute.

Purpose: UAT Test 3 revealed that after login with invitation token, users are navigated to AdminPanelRoute — but invited users are regular members without admin permissions, causing a permission error. All 3 NavigateToGroup handlers in AppNavHost.kt must navigate to DashboardRoute instead.
Output: Fixed AppNavHost.kt with correct navigation targets.
</objective>

<execution_context>
@/Users/marc/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/marc/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18.2-invitation-acceptance-flow/18.2-UAT.md
@composeApp/src/commonMain/kotlin/com/m2f/template/navigation/AppNavHost.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix NavigateToGroup handlers to navigate to DashboardRoute</name>
  <files>composeApp/src/commonMain/kotlin/com/m2f/template/navigation/AppNavHost.kt</files>
  <action>
In AppNavHost.kt, change all 3 NavigateToGroup event handlers to navigate to DashboardRoute instead of AdminPanelRoute:

1. **LoginEvent.NavigateToGroup** (line ~104): Change `navController.navigate(AdminPanelRoute(groupId = event.groupId))` to `navController.navigate(DashboardRoute)`. Keep the `popUpTo<LoginRoute> { inclusive = true }` block.

2. **RegisterEvent.NavigateToGroup** (line ~147): Change `navController.navigate(AdminPanelRoute(groupId = event.groupId))` to `navController.navigate(DashboardRoute)`. Keep the `popUpTo<LoginRoute> { inclusive = true }` block.

3. **InviteAcceptEvent.NavigateToGroup** (line ~356): Change `navController.navigate(AdminPanelRoute(groupId = event.groupId))` to `navController.navigate(DashboardRoute)`. Keep the `popUpTo<DashboardRoute>()` block — but change it to `popUpTo(0) { inclusive = true }` since we're navigating TO DashboardRoute (can't popUpTo the same destination).

Why DashboardRoute and not AdminPanelRoute: Invited users are regular members. AdminPanelRoute requires admin permissions, causing a "permission denied" error. DashboardRoute is the universal landing page that all users can access.

Note: The groupId from the event is not needed for DashboardRoute navigation. The user has been linked to the group on the server side — the dashboard will show their groups.
  </action>
  <verify>
Run `./gradlew composeApp:compileKotlinWasmJs` to verify the code compiles without errors. Grep AppNavHost.kt for "AdminPanelRoute" in NavigateToGroup handlers — should find zero matches. Grep for "DashboardRoute" in NavigateToGroup handlers — should find 3 matches.
  </verify>
  <done>
All 3 NavigateToGroup event handlers in AppNavHost.kt navigate to DashboardRoute. No NavigateToGroup handler references AdminPanelRoute. Code compiles successfully.
  </done>
</task>

</tasks>

<verification>
1. `./gradlew composeApp:compileKotlinWasmJs` passes — no compile errors
2. All 3 NavigateToGroup handlers in AppNavHost.kt navigate to DashboardRoute
3. No NavigateToGroup handler navigates to AdminPanelRoute
</verification>

<success_criteria>
- LoginEvent.NavigateToGroup → DashboardRoute (not AdminPanelRoute)
- RegisterEvent.NavigateToGroup → DashboardRoute (not AdminPanelRoute)
- InviteAcceptEvent.NavigateToGroup → DashboardRoute (not AdminPanelRoute)
- Code compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/18.2-invitation-acceptance-flow/18.2-04-SUMMARY.md`
</output>
