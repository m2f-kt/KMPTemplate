---
status: diagnosed
phase: 21-group-invitations-profiles
source: [21-07-SUMMARY.md, 21-08-SUMMARY.md]
started: 2026-03-02T11:00:00Z
updated: 2026-03-02T11:15:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Spanish Role Badges in Invitation Table
expected: Switch to Spanish locale, open admin panel invitations table. Role column shows localized role names (Miembro, Admin, Propietario) instead of raw MEMBER/ADMIN/OWNER.
result: issue
reported: "items are still displaying in english while the rest of the content in spanish — ROL column shows MEMBER/OWNER raw strings"
severity: minor

### 2. Email Pre-fill on Register from Invitation
expected: Open an invitation link while logged out. Click Register. The email field should be pre-filled with the invitation's target email and the field should be disabled (cannot edit). Complete registration — the app navigates to the Dashboard showing the group you were invited to.
result: issue
reported: "email pre-fill works but the invitation did not get accepted after registration (on login all good)"
severity: major

### 3. Email Pre-fill on Login from Invitation
expected: Open an invitation link while logged out. Click Login. The email field should be pre-filled with the invitation's target email and the field should be disabled (cannot edit). Complete login — the invitation is applied and the app navigates to the Dashboard.
result: pass

### 4. Server Rejects Wrong-Email Invitation Acceptance
expected: Open an invitation link sent to user-a@test.com. Log in with a different account (user-b@test.com). The server rejects the invitation acceptance with an email mismatch error. The user sees an appropriate error message (not a generic error).
result: pass

### 5. Resend Invitation Deduplication
expected: In the admin panel, resend an expired or revoked invitation. After resending, the old invitation row should be removed (not just revoked). Only the new active invitation appears in the table for that email — no duplicates.
result: pass

### 6. Post-Registration Invitation Navigation
expected: Open an invitation link while logged out. Register a new account (using the pre-filled email). After registration, the app navigates to the Dashboard and the invited group is visible. The admin panel shows the invitation as Accepted and the user in the members list.
result: issue
reported: "don't work. user registers, invitation does not get accepted and the user also don't appear in the list of users for the group. second is that i see an inconsistency: another user is in the group while a pending invitation (I revoked and appears as revoked and i can resend invitation) is shown. if a user is in the group there should be only an accepted invitation that should not allow any other action."
severity: major

## Summary

total: 6
passed: 3
issues: 3
pending: 0
skipped: 0

## Gaps

- truth: "Role badges in Members table display in user's selected locale (Spanish shows Miembro/Propietario)"
  status: failed
  reason: "User reported: items are still displaying in english while the rest of the content in spanish — ROL column shows MEMBER/OWNER raw strings"
  severity: minor
  test: 1
  root_cause: "The InvitationsSection role rendering IS correctly localized (commit 578a5b0 fix works). The bug is in the Members table at AdminPanelScreen.kt line 277: TerminalBadge uses member.role.value which renders hardcoded uppercase English from GroupRole sealed class (OWNER/MEMBER/ADMIN). Same when-mapping to stringResource() is needed here."
  artifacts:
    - path: "app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/AdminPanelScreen.kt"
      issue: "Line 277: TerminalBadge(text = member.role.value) uses raw GroupRole.value — hardcoded English"
  missing:
    - "Replace member.role.value at line 277 with when(member.role) mapping to stringResource(Res.string.admin_role_owner/admin/member)"

- truth: "Invitation is accepted and user joins group after registering from invitation link"
  status: failed
  reason: "User reported: email pre-fill works but the invitation did not get accepted after registration (on login all good). Confirmed in test 6: user registers but doesn't appear in group members."
  severity: major
  test: 2
  root_cause: "Koin DI bug: authModule (AuthModule.kt line 28) registers AuthService with 4-arg constructor (onRegistered=null). serverModule (ServerModule.kt lines 41-59) tries to register a second AuthService WITH the onRegistered callback that calls invitationService.acceptInvitation(), but allowOverride is not enabled in Application.kt Koin setup (line 87-89). The first registration (from authModule via includes) wins, so the injected AuthService has onRegistered=null. When AuthService.register() reaches step 6 (line 97: if request.invitationToken != null && onRegistered != null), onRegistered is null so acceptInvitation is never called. Login works because LoginViewModel explicitly calls sdk.acceptInvitation() client-side."
  artifacts:
    - path: "server/auth/src/main/kotlin/com/m2f/server/auth/di/AuthModule.kt"
      issue: "Line 28: registers AuthService without onRegistered callback (4-arg constructor, defaults to null)"
    - path: "server/src/main/kotlin/com/m2f/template/di/ServerModule.kt"
      issue: "Lines 41-59: registers second AuthService WITH callback but override doesn't take effect"
    - path: "server/src/main/kotlin/com/m2f/template/Application.kt"
      issue: "Lines 87-89: Koin setup without allowOverride(true)"
    - path: "server/auth/src/main/kotlin/com/m2f/server/auth/service/AuthService.kt"
      issue: "Lines 96-99: onRegistered is null so invitation acceptance never fires during registration"
  missing:
    - "Remove single { AuthService(...) } from authModule (let serverModule be the sole registrar with the onRegistered callback)"
    - "OR add allowOverride(true) to Koin configuration in Application.kt"
    - "Add integration test for register-with-invitation flow to verify invitation is accepted"

- truth: "Invitation status is consistent with group membership — member in group shows no actionable invitation, stale invitations cleaned up"
  status: failed
  reason: "User reported: a user is in the group but their invitation shows as revoked with resend option available. If user is already in group, invitation should show Accepted with no further actions."
  severity: major
  test: 6
  root_cause: "No cross-check between members and invitations at any layer. InvitationRepository.findByGroupId() returns ALL invitation records unfiltered. InvitationService.listInvitations() maps them directly without checking membership. resendInvitation() doesn't check if email belongs to existing member. acceptInvitation() marks only the specific invitation used as accepted but doesn't clean up other invitations for the same email+group. UI renders actions based solely on invitation flags (isAccepted/isRevoked/isExpired) with no membership awareness."
  artifacts:
    - path: "server/groups/src/main/kotlin/com/m2f/server/groups/service/InvitationService.kt"
      issue: "listInvitations() (line 190-217): no membership cross-check. resendInvitation() (line 267-322): no membership guard. acceptInvitation() (line 140-183): doesn't clean up other invitations for same email+group."
    - path: "server/groups/src/main/kotlin/com/m2f/server/groups/repository/InvitationRepository.kt"
      issue: "findByGroupId() (line 101-107): returns all rows, no join with memberships"
    - path: "app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/AdminPanelScreen.kt"
      issue: "InvitationsSection: Revoke/Resend buttons shown based only on invitation flags, no membership awareness"
  missing:
    - "Server: In listInvitations(), cross-reference member emails and override status for existing members (or filter out stale invitations)"
    - "Server: In resendInvitation(), check if invitation email belongs to current member and reject with MemberAlreadyInGroup"
    - "Server: In acceptInvitation(), after adding member, delete/mark-accepted all other invitation records for same email+group"
    - "Add integration tests for membership-invitation consistency"
