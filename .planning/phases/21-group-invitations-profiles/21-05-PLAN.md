---
phase: 21-group-invitations-profiles
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/ErrorMapper.kt
  - core/models/src/commonMain/kotlin/com/m2f/template/models/localization/StringKey.kt
  - app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/InviteAcceptModel.kt
  - app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/InviteAcceptMutation.kt
  - app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/InviteAcceptViewModel.kt
  - app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/InviteAcceptScreen.kt
  - app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/StringKeyResolver.kt
  - app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/StringKeyResolver.kt
  - app/profile/src/commonMain/kotlin/com/m2f/template/app/profile/StringKeyResolver.kt
  - app/documents/src/commonMain/kotlin/com/m2f/template/app/documents/StringKeyResolver.kt
  - composeApp/src/commonMain/kotlin/com/m2f/template/localization/StringKeyResolver.kt
  - app/auth/src/commonMain/composeResources/values/strings.xml
  - app/auth/src/commonMain/composeResources/values-es/strings.xml
  - app/admin/src/commonMain/composeResources/values/strings.xml
  - app/admin/src/commonMain/composeResources/values-es/strings.xml
  - app/profile/src/commonMain/composeResources/values/strings.xml
  - app/profile/src/commonMain/composeResources/values-es/strings.xml
  - app/documents/src/commonMain/composeResources/values/strings.xml
  - composeApp/src/commonMain/composeResources/values/strings.xml
  - composeApp/src/commonMain/composeResources/values-es/strings.xml
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Revoked invitation shows specific 'this invitation has been revoked' message on InviteAcceptScreen"
    - "Expired invitation error from acceptInvitation is mapped to specific StringKey, not GENERIC_ERROR"
    - "Already-accepted invitation error from acceptInvitation is mapped to specific StringKey"
    - "InviteAcceptScreen shows revoked-state UI with warning alert and request-new-invitation button"
  artifacts:
    - path: "core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/ErrorMapper.kt"
      provides: "HTTP 410 Gone mapping to AppError.Client.ServerMapped"
      contains: "410"
    - path: "core/models/src/commonMain/kotlin/com/m2f/template/models/localization/StringKey.kt"
      provides: "INVITATION_REVOKED, INVITATION_EXPIRED, INVITATION_ALREADY_ACCEPTED, INVITATION_NOT_FOUND entries"
      contains: "INVITATION_REVOKED"
    - path: "app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/InviteAcceptModel.kt"
      provides: "isRevoked field on invitation model"
      contains: "isRevoked"
    - path: "app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/InviteAcceptScreen.kt"
      provides: "Revoked-state UI block"
      contains: "isRevoked"
  key_links:
    - from: "ErrorMapper.mapHttpError"
      to: "AppError.Client.ServerMapped"
      via: "410 status code mapping"
      pattern: "410.*ServerMapped"
    - from: "StringKey.fromCode"
      to: "INVITATION_REVOKED"
      via: "code-to-enum lookup"
      pattern: "INVITATION_REVOKED"
    - from: "InviteAcceptViewModel.handleLoadInvitation"
      to: "InviteAcceptMutation.SetInvitationDetails.isRevoked"
      via: "invitation.isRevoked DTO field"
      pattern: "isRevoked"
---

<objective>
Fix the revoked invitation error propagation chain: map HTTP 410 in ErrorMapper, add invitation error entries to StringKey enum (with exhaustive when updates across 5 modules), add isRevoked to InviteAcceptModel/Mutation/ViewModel, and add revoked-state UI to InviteAcceptScreen.

Purpose: Close UAT gap 3 — revoked invitation shows specific error message instead of generic error.
Output: Full error propagation from server 410 → SDK → ViewModel → UI with localized strings.
</objective>

<execution_context>
@/Users/marc/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/marc/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/revoked-invitation-error-message.md
@core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/ErrorMapper.kt
@core/models/src/commonMain/kotlin/com/m2f/template/models/localization/StringKey.kt
@app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/InviteAcceptModel.kt
@app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/InviteAcceptMutation.kt
@app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/InviteAcceptViewModel.kt
@app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/InviteAcceptScreen.kt
@app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/StringKeyResolver.kt
@app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/StringKeyResolver.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: ErrorMapper 410 mapping + StringKey invitation entries + string resources</name>
  <files>
    core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/ErrorMapper.kt
    core/models/src/commonMain/kotlin/com/m2f/template/models/localization/StringKey.kt
    app/auth/src/commonMain/composeResources/values/strings.xml
    app/auth/src/commonMain/composeResources/values-es/strings.xml
    app/admin/src/commonMain/composeResources/values/strings.xml
    app/admin/src/commonMain/composeResources/values-es/strings.xml
    app/profile/src/commonMain/composeResources/values/strings.xml
    app/profile/src/commonMain/composeResources/values-es/strings.xml
    app/documents/src/commonMain/composeResources/values/strings.xml
    composeApp/src/commonMain/composeResources/values/strings.xml
    composeApp/src/commonMain/composeResources/values-es/strings.xml
    app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/StringKeyResolver.kt
    app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/StringKeyResolver.kt
    app/profile/src/commonMain/kotlin/com/m2f/template/app/profile/StringKeyResolver.kt
    app/documents/src/commonMain/kotlin/com/m2f/template/app/documents/StringKeyResolver.kt
    composeApp/src/commonMain/kotlin/com/m2f/template/localization/StringKeyResolver.kt
  </files>
  <action>
This task fixes 3 layers of the error propagation chain. Each sub-step MUST be done in order because later steps depend on earlier ones.

**Step 1: Add HTTP 410 mapping to ErrorMapper.kt**

In `mapHttpError()`, add a case for 410 before the `in 500..599` branch (after the 422 case). Map 410 to `AppError.Client.ServerMapped` to preserve the server's error code and message (same treatment as 422):

```kotlin
410 -> AppError.Client.ServerMapped(
    code = errorResponse?.code ?: "INVITATION_ERROR",
    message = errorResponse?.message ?: "Resource is no longer available",
)
```

Also update the KDoc comment on `mapHttpError` to include: `- 410 -> [AppError.Client.ServerMapped] (preserves server's error code/message, e.g. revoked invitation)`.

**Step 2: Add invitation error entries to StringKey enum**

In `StringKey.kt`, add 4 new entries in a new `// Invitation errors` section, BEFORE the `// Generic fallback` section (before `GENERIC_ERROR`):

```kotlin
// Invitation errors
INVITATION_REVOKED("INVITATION_REVOKED"),
INVITATION_EXPIRED("INVITATION_EXPIRED"),
INVITATION_ALREADY_ACCEPTED("INVITATION_ALREADY_ACCEPTED"),
INVITATION_NOT_FOUND("INVITATION_NOT_FOUND"),
```

These codes match what the server sends in the `ErrorResponse.code` field.

**Step 3: Add invitation error string resources to ALL module string files**

Add the following 4 string entries to the Error Messages section of EACH module's `values/strings.xml` (before the `<!-- Generic fallback -->` comment). Add them as a new `<!-- Invitation errors -->` section:

English strings (for ALL modules' `values/strings.xml`):
```xml
<!-- Invitation errors -->
<string name="error_invitation_revoked">This invitation has been revoked</string>
<string name="error_invitation_expired">This invitation has expired</string>
<string name="error_invitation_already_accepted">This invitation has already been accepted</string>
<string name="error_invitation_not_found">Invitation not found</string>
```

Spanish strings (for ALL modules' `values-es/strings.xml` — skip modules without `values-es/strings.xml` like `app/documents`):
```xml
<!-- Invitation errors -->
<string name="error_invitation_revoked">Esta invitación ha sido revocada</string>
<string name="error_invitation_expired">Esta invitación ha expirado</string>
<string name="error_invitation_already_accepted">Esta invitación ya ha sido aceptada</string>
<string name="error_invitation_not_found">Invitación no encontrada</string>
```

The modules to update:
- `app/auth/src/commonMain/composeResources/values/strings.xml` + `values-es/strings.xml`
- `app/admin/src/commonMain/composeResources/values/strings.xml` + `values-es/strings.xml`
- `app/profile/src/commonMain/composeResources/values/strings.xml` + `values-es/strings.xml`
- `app/documents/src/commonMain/composeResources/values/strings.xml` (EN only — no ES file exists)
- `composeApp/src/commonMain/composeResources/values/strings.xml` + `values-es/strings.xml`

NOTE: `app/dashboard` does NOT have a StringKeyResolver, so it does not need invitation error strings. Only add them to modules that have a StringKeyResolver.

**Step 4: Update ALL 5 StringKeyResolver files with exhaustive when branches**

Add 4 new branches to the exhaustive `when` block in EACH of the 5 `StringKeyResolver.kt` files. Place them in a new `// Invitation errors` section before the `// Generic fallback` section.

Each StringKeyResolver uses its own module's `Res.string.*` import. Add the corresponding imports and when branches:

For each module, add these 4 imports (adjusting the import prefix per module):
```kotlin
import template.app.{module}.generated.resources.error_invitation_revoked
import template.app.{module}.generated.resources.error_invitation_expired
import template.app.{module}.generated.resources.error_invitation_already_accepted
import template.app.{module}.generated.resources.error_invitation_not_found
```

And add these when branches:
```kotlin
// Invitation errors
StringKey.INVITATION_REVOKED -> stringResource(Res.string.error_invitation_revoked, *args)
StringKey.INVITATION_EXPIRED -> stringResource(Res.string.error_invitation_expired, *args)
StringKey.INVITATION_ALREADY_ACCEPTED -> stringResource(Res.string.error_invitation_already_accepted, *args)
StringKey.INVITATION_NOT_FOUND -> stringResource(Res.string.error_invitation_not_found, *args)
```

The 5 files and their import prefixes:
1. `app/auth/.../StringKeyResolver.kt` → `template.app.auth.generated.resources.*`
2. `app/admin/.../StringKeyResolver.kt` → `template.app.admin.generated.resources.*`
3. `app/profile/.../StringKeyResolver.kt` → `template.app.profile.generated.resources.*`
4. `app/documents/.../StringKeyResolver.kt` → `template.app.documents.generated.resources.*`
5. `composeApp/.../StringKeyResolver.kt` → `template.composeapp.generated.resources.*` (NOTE: composeApp uses lowercase `composeapp` in generated resources path)

CRITICAL: All 5 must be updated or compilation will fail due to exhaustive `when` on the enum.
  </action>
  <verify>`./gradlew :core:sdk:compileKotlinJvm :core:models:compileKotlinJvm :app:auth:compileKotlinJvm :app:admin:compileKotlinJvm :app:profile:compileKotlinJvm :app:documents:compileKotlinJvm :composeApp:compileKotlinJvm` — all pass</verify>
  <done>ErrorMapper maps 410 to ServerMapped, StringKey has 4 invitation entries, all 5 StringKeyResolvers have matching branches, all string resources exist in EN/ES</done>
</task>

<task type="auto">
  <name>Task 2: Add isRevoked to InviteAccept MVI chain + revoked UI block</name>
  <files>
    app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/InviteAcceptModel.kt
    app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/InviteAcceptMutation.kt
    app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/InviteAcceptViewModel.kt
    app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/InviteAcceptScreen.kt
    app/auth/src/commonMain/composeResources/values/strings.xml
    app/auth/src/commonMain/composeResources/values-es/strings.xml
  </files>
  <action>
Thread the `isRevoked` field from the server's `InvitationResponse` DTO through the MVI chain to the UI.

**Step 1: Add `isRevoked` to InviteAcceptModel**

Add `val isRevoked: Boolean = false` to the `InviteAcceptModel` data class, after `isAlreadyAccepted`:
```kotlin
val isExpired: Boolean = false,
val isAlreadyAccepted: Boolean = false,
val isRevoked: Boolean = false,
```

**Step 2: Add `isRevoked` to SetInvitationDetails mutation**

Add `val isRevoked: Boolean` parameter to `InviteAcceptMutation.SetInvitationDetails`:
```kotlin
data class SetInvitationDetails(
    val groupName: String,
    val inviterName: String,
    val role: String,
    val email: String,
    val isExpired: Boolean,
    val isAlreadyAccepted: Boolean,
    val isRevoked: Boolean,
) : InviteAcceptMutation
```

**Step 3: Update InviteAcceptViewModel**

In `handleLoadInvitation()`, pass `isRevoked = invitation.isRevoked` to the `SetInvitationDetails` mutation:
```kotlin
InviteAcceptMutation.SetInvitationDetails(
    groupName = invitation.groupName,
    inviterName = invitation.inviterName,
    role = invitation.role,
    email = invitation.email,
    isExpired = invitation.isExpired,
    isAlreadyAccepted = invitation.isAccepted,
    isRevoked = invitation.isRevoked,
)
```

In `reduce()`, update the `SetInvitationDetails` case to include `isRevoked`:
```kotlin
is InviteAcceptMutation.SetInvitationDetails -> model.copy(
    groupName = mutation.groupName,
    inviterName = mutation.inviterName,
    role = mutation.role,
    email = mutation.email,
    isExpired = mutation.isExpired,
    isAlreadyAccepted = mutation.isAlreadyAccepted,
    isRevoked = mutation.isRevoked,
)
```

**Step 4: Add invite_revoked string resources**

Add to `app/auth/.../values/strings.xml` (in the `Invite Accept Screen` section, after `invite_already_accepted`):
```xml
<string name="invite_revoked">This invitation has been revoked by the administrator.</string>
<string name="invite_revoked_hint">// contact the group administrator to receive a new invitation</string>
```

Add to `app/auth/.../values-es/strings.xml` (same location):
```xml
<string name="invite_revoked">Esta invitación ha sido revocada por el administrador.</string>
<string name="invite_revoked_hint">// contacta al administrador del grupo para recibir una nueva invitación</string>
```

**Step 5: Add revoked-state UI block to InviteAcceptScreen**

In `InviteAcceptScreen.kt`, add a revoked-state block AFTER the expired block (around line 160) and BEFORE the already-accepted block. This matches the same pattern as the expired block:

```kotlin
// Revoked alert
if (state.isRevoked) {
    TerminalAlert(
        message = stringResource(Res.string.invite_revoked),
        variant = AlertVariant.Error,
    )
    Spacer(modifier = Modifier.height(8.dp))
    TerminalText(
        text = stringResource(Res.string.invite_revoked_hint),
        style = typography.sm,
        color = colors.textDim,
    )
    Spacer(modifier = Modifier.height(8.dp))
    TerminalButton(
        text = stringResource(Res.string.invite_request_new),
        onClick = onRequestNewInvitation,
        modifier = Modifier.fillMaxWidth(),
        variant = ButtonVariant.Secondary,
    )
}
```

Also add the string resource imports for `invite_revoked` and `invite_revoked_hint` at the top of the file.

**Step 6: Update action button guard**

In the action buttons condition (around line 202), add `!state.isRevoked` alongside the existing guards:
```kotlin
if (!state.isExpired && !state.isAlreadyAccepted && !state.isRevoked && !state.acceptSuccess) {
```

This prevents showing Accept/Login/Register buttons for revoked invitations.
  </action>
  <verify>`./gradlew :app:auth:compileKotlinJvm :composeApp:compileKotlinJvm` passes</verify>
  <done>InviteAcceptScreen shows revoked-state UI (Error alert + hint + request-new button) when invitation is revoked, action buttons hidden for revoked state</done>
</task>

</tasks>

<verification>
- `./gradlew :core:sdk:compileKotlinJvm` passes (ErrorMapper compiles)
- `./gradlew :core:models:compileKotlinJvm` passes (StringKey compiles)
- `./gradlew :app:auth:compileKotlinJvm` passes (InviteAccept MVI + StringKeyResolver compile)
- `./gradlew :app:admin:compileKotlinJvm` passes (StringKeyResolver compiles)
- `./gradlew :app:profile:compileKotlinJvm` passes (StringKeyResolver compiles)
- `./gradlew :app:documents:compileKotlinJvm` passes (StringKeyResolver compiles)
- `./gradlew :composeApp:compileKotlinJvm` passes (full app compiles)
</verification>

<success_criteria>
1. HTTP 410 Gone is mapped to AppError.Client.ServerMapped in ErrorMapper (preserving server's INVITATION_REVOKED code)
2. StringKey enum has INVITATION_REVOKED, INVITATION_EXPIRED, INVITATION_ALREADY_ACCEPTED, INVITATION_NOT_FOUND entries
3. All 5 StringKeyResolver files compile with exhaustive when branches for new entries
4. InviteAcceptModel carries isRevoked, ViewModel maps it from DTO, Screen shows revoked alert UI
5. Action buttons (Accept/Login/Register) are hidden when invitation is revoked
6. String resources exist in all modules (EN + ES where applicable)
</success_criteria>

<output>
After completion, create `.planning/phases/21-group-invitations-profiles/21-05-SUMMARY.md`
</output>
