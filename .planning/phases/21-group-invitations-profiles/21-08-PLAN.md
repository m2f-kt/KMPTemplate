---
phase: 21-group-invitations-profiles
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/AdminPanelScreen.kt
  - app/admin/src/commonMain/composeResources/values/strings.xml
  - app/admin/src/commonMain/composeResources/values-es/strings.xml
  - composeApp/src/commonMain/kotlin/com/m2f/template/navigation/Routes.kt
  - composeApp/src/commonMain/kotlin/com/m2f/template/navigation/AppNavHost.kt
  - app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/InviteAcceptEvent.kt
  - app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/InviteAcceptViewModel.kt
  - app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/LoginModel.kt
  - app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/LoginIntent.kt
  - app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/LoginViewModel.kt
  - app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/LoginScreen.kt
  - app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/RegisterModel.kt
  - app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/RegisterIntent.kt
  - app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/RegisterViewModel.kt
  - app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/RegisterScreen.kt
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Invitation role badges display localized text (Member/Miembro, Admin, Owner/Propietario) instead of raw MEMBER/ADMIN/OWNER"
    - "Login screen pre-fills and locks the email field when navigated from an invitation"
    - "Register screen pre-fills and locks the email field when navigated from an invitation"
  artifacts:
    - path: "app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/AdminPanelScreen.kt"
      provides: "Localized role badge text via when-mapping on invitation.role string"
    - path: "app/admin/src/commonMain/composeResources/values/strings.xml"
      provides: "EN role translations (admin_role_member, admin_role_admin, admin_role_owner)"
    - path: "app/admin/src/commonMain/composeResources/values-es/strings.xml"
      provides: "ES role translations"
    - path: "composeApp/src/commonMain/kotlin/com/m2f/template/navigation/Routes.kt"
      provides: "invitationEmail parameter on LoginRoute and RegisterRoute"
    - path: "app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/LoginModel.kt"
      provides: "invitationEmail field"
    - path: "app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/RegisterModel.kt"
      provides: "invitationEmail field"
  key_links:
    - from: "InviteAcceptViewModel.GoToLogin"
      to: "InviteAcceptEvent.NavigateToLogin"
      via: "passes email alongside token"
      pattern: "NavigateToLogin.*email"
    - from: "AppNavHost LoginRoute composable"
      to: "LoginIntent.SetInvitationEmail"
      via: "LaunchedEffect sets email from route param"
      pattern: "SetInvitationEmail"
    - from: "AdminPanelScreen invitation.role"
      to: "stringResource(Res.string.admin_role_*)"
      via: "when mapping on role value"
      pattern: "admin_role_member|admin_role_admin|admin_role_owner"
---

<objective>
Fix two client-side invitation UX issues from GAP-UAT tests #1 and #2.

Purpose: (1) Role badges in the admin panel invitations table show raw server strings (MEMBER/OWNER) — they need to display localized text. (2) Login/Register screens don't pre-fill or lock the email from the invitation, allowing users to log in or register with a different email than the invitation target.

Output: Localized role badges in admin panel, email pre-fill and lock in Login/Register when navigating from an invitation.
</objective>

<execution_context>
@/Users/marc/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/marc/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-group-invitations-profiles/21-GAP-UAT.md
@.planning/phases/21-group-invitations-profiles/21-06-SUMMARY.md

@app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/AdminPanelScreen.kt
@app/admin/src/commonMain/composeResources/values/strings.xml
@app/admin/src/commonMain/composeResources/values-es/strings.xml
@composeApp/src/commonMain/kotlin/com/m2f/template/navigation/Routes.kt
@composeApp/src/commonMain/kotlin/com/m2f/template/navigation/AppNavHost.kt
@app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/InviteAcceptEvent.kt
@app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/InviteAcceptViewModel.kt
@app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/LoginModel.kt
@app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/LoginIntent.kt
@app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/LoginViewModel.kt
@app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/LoginScreen.kt
@app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/RegisterModel.kt
@app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/RegisterIntent.kt
@app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/RegisterViewModel.kt
@app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/RegisterScreen.kt
@core/models/src/commonMain/kotlin/com/m2f/template/models/GroupRole.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Localize role badges in admin panel invitation table</name>
  <files>
    app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/AdminPanelScreen.kt
    app/admin/src/commonMain/composeResources/values/strings.xml
    app/admin/src/commonMain/composeResources/values-es/strings.xml
  </files>
  <action>
    1. **EN strings.xml** (app/admin values/) — Add 3 role translation entries after the existing invitation strings:
       ```xml
       <string name="admin_role_member">Member</string>
       <string name="admin_role_admin">Admin</string>
       <string name="admin_role_owner">Owner</string>
       ```

    2. **ES strings.xml** (app/admin values-es/) — Add the Spanish equivalents:
       ```xml
       <string name="admin_role_member">Miembro</string>
       <string name="admin_role_admin">Admin</string>
       <string name="admin_role_owner">Propietario</string>
       ```

    3. **AdminPanelScreen.kt** — Replace line 574 (`TerminalTableCell(text = invitation.role, secondary = true)`) with a localized role mapping. Create a helper function or inline `when` that maps the raw role string to a string resource:
       ```kotlin
       TerminalTableCell(
           text = when (invitation.role.uppercase()) {
               "OWNER" -> stringResource(Res.string.admin_role_owner)
               "ADMIN" -> stringResource(Res.string.admin_role_admin)
               else -> stringResource(Res.string.admin_role_member)
           },
           secondary = true,
       )
       ```
       Add the necessary `Res.string.admin_role_*` imports if not auto-resolved. The `invitation.role` field is a plain String from InvitationResponse DTO containing values like "MEMBER", "ADMIN", "OWNER" matching GroupRole.value.

    NOTE: This is an admin-module-only change. No StringKey or StringKeyResolver updates needed — these are UI display strings, not error codes.
  </action>
  <verify>
    `./gradlew :app:admin:compileKotlinJvm` passes. Verify AdminPanelScreen.kt no longer references `invitation.role` directly as text content.
  </verify>
  <done>
    Invitation role badges display "Member"/"Admin"/"Owner" in English and "Miembro"/"Admin"/"Propietario" in Spanish instead of raw MEMBER/ADMIN/OWNER strings.
  </done>
</task>

<task type="auto">
  <name>Task 2: Pre-fill and lock email in Login/Register when navigating from invitation</name>
  <files>
    composeApp/src/commonMain/kotlin/com/m2f/template/navigation/Routes.kt
    composeApp/src/commonMain/kotlin/com/m2f/template/navigation/AppNavHost.kt
    app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/InviteAcceptEvent.kt
    app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/InviteAcceptViewModel.kt
    app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/LoginModel.kt
    app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/LoginIntent.kt
    app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/LoginViewModel.kt
    app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/LoginScreen.kt
    app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/RegisterModel.kt
    app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/RegisterIntent.kt
    app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/RegisterViewModel.kt
    app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/RegisterScreen.kt
  </files>
  <action>
    This task threads the invitation email from the InviteAcceptScreen through to Login/Register screens, pre-filling the email field and disabling it so the user cannot change it.

    **A. Routes — Add invitationEmail parameter:**

    1. **Routes.kt** — Add `invitationEmail: String? = null` parameter to both `LoginRoute` and `RegisterRoute`:
       ```kotlin
       @Serializable
       data class LoginRoute(val invitationToken: String? = null, val invitationEmail: String? = null)

       @Serializable
       data class RegisterRoute(val invitationToken: String? = null, val invitationEmail: String? = null)
       ```

    **B. InviteAccept — Pass email with navigation events:**

    2. **InviteAcceptEvent.kt** — Add `email` parameter to `NavigateToLogin` and `NavigateToRegister`:
       ```kotlin
       data class NavigateToLogin(val token: String, val email: String) : InviteAcceptEvent
       data class NavigateToRegister(val token: String, val email: String) : InviteAcceptEvent
       ```

    3. **InviteAcceptViewModel.kt** — Update GoToLogin and GoToRegister handlers (lines 27-32) to pass the invitation email from the model:
       ```kotlin
       is InviteAcceptIntent.GoToLogin -> {
           sendEvent(InviteAcceptEvent.NavigateToLogin(
               token = model.value.token,
               email = model.value.email ?: "",
           ))
       }
       is InviteAcceptIntent.GoToRegister -> {
           sendEvent(InviteAcceptEvent.NavigateToRegister(
               token = model.value.token,
               email = model.value.email ?: "",
           ))
       }
       ```

    **C. Login — Add invitation email support:**

    4. **LoginModel.kt** — Add `invitationEmail: String? = null` field. This is the locked email from the invitation (distinct from `email` which is the editable input). When `invitationEmail` is set, the email field should be pre-filled and locked:
       ```kotlin
       data class LoginModel(
           val email: String = "",
           val password: String = "",
           val rememberMe: Boolean = false,
           val isLoading: Boolean = false,
           val emailError: StringKey? = null,
           val passwordError: StringKey? = null,
           val serverError: StringKey? = null,
           val invitationToken: String? = null,
           val invitationEmail: String? = null,
           val isAcceptingInvitation: Boolean = false,
       )
       ```

    5. **LoginIntent.kt** — Add `SetInvitationEmail` intent:
       ```kotlin
       data class SetInvitationEmail(val email: String?) : LoginIntent
       ```

    6. **LoginViewModel.kt** — Add mutation handling for `SetInvitationEmail`. When invitationEmail is set, also set the `email` field to match (pre-fill):
       - In `take()`, add: `is LoginIntent.SetInvitationEmail -> { sendMutation(LoginMutation.SetInvitationEmail(intent.email)); if (intent.email != null) sendMutation(LoginMutation.SetEmail(intent.email)) }`
       - Add `LoginMutation.SetInvitationEmail` (or reuse an existing pattern). In `reduce()`, handle: `is LoginMutation.SetInvitationEmail -> model.copy(invitationEmail = mutation.email)`
       - Check LoginMutation.kt and add `data class SetInvitationEmail(val email: String?) : LoginMutation` if it doesn't exist

    7. **LoginScreen.kt** — Pass `enabled` parameter to the email TerminalInput. The email field should be disabled when `state.invitationEmail != null`:
       - Find the email TerminalInput (around line 455-462) and add `enabled = state.invitationEmail == null && !state.isLoading`
       - This applies to ALL screen size variants (mobile, tablet, desktop). Search for ALL occurrences of `onEmailChange` callback usage and ensure the `enabled` prop is passed consistently. There are multiple composable overloads for different screen sizes — the actual TerminalInput is in the `LoginFormContent` composable (the inner one). Make sure the `enabled` state reaches it. The `state` object is already passed through.

    **D. Register — Add invitation email support (same pattern):**

    8. **RegisterModel.kt** — Add `invitationEmail: String? = null` field.

    9. **RegisterIntent.kt** — Add `SetInvitationEmail` intent:
       ```kotlin
       data class SetInvitationEmail(val email: String?) : RegisterIntent
       ```

    10. **RegisterViewModel.kt** — Handle `SetInvitationEmail`:
        - In `take()`: `is RegisterIntent.SetInvitationEmail -> { sendMutation(RegisterMutation.SetInvitationEmail(intent.email)); if (intent.email != null) sendMutation(RegisterMutation.SetEmail(intent.email)) }`
        - Add `RegisterMutation.SetInvitationEmail` if needed. In `reduce()`: `is RegisterMutation.SetInvitationEmail -> model.copy(invitationEmail = mutation.email)`
        - Check RegisterMutation.kt and add the mutation class if it doesn't exist

    11. **RegisterScreen.kt** — Same as LoginScreen: find the email TerminalInput (around line 559-566 in the `RegisterFormContent` composable) and add `enabled = state.invitationEmail == null && !state.isLoading`. Apply to all screen size overloads.

    **E. AppNavHost — Thread email from route to ViewModel:**

    12. **AppNavHost.kt** — In the `composable<LoginRoute>` block (around line 80-117), update the existing LaunchedEffect that sets the invitation token to also set the email:
        ```kotlin
        LaunchedEffect(route.invitationToken, route.invitationEmail) {
            route.invitationToken?.let { token ->
                viewModel.take(LoginIntent.SetInvitationToken(token))
            }
            route.invitationEmail?.let { email ->
                viewModel.take(LoginIntent.SetInvitationEmail(email))
            }
        }
        ```

    13. **AppNavHost.kt** — Same for `composable<RegisterRoute>` (around line 119-160):
        ```kotlin
        LaunchedEffect(route.invitationToken, route.invitationEmail) {
            route.invitationToken?.let { token ->
                viewModel.take(RegisterIntent.SetInvitationToken(token))
            }
            route.invitationEmail?.let { email ->
                viewModel.take(RegisterIntent.SetInvitationEmail(email))
            }
        }
        ```

    14. **AppNavHost.kt** — In the `composable<InviteAcceptRoute>` block, update event handlers for NavigateToLogin and NavigateToRegister to pass the email via route params:
        - Find `is InviteAcceptEvent.NavigateToLogin` → change to: `navController.navigate(LoginRoute(invitationToken = event.token, invitationEmail = event.email))`
        - Find `is InviteAcceptEvent.NavigateToRegister` → change to: `navController.navigate(RegisterRoute(invitationToken = event.token, invitationEmail = event.email))`

    NOTE: Check LoginMutation.kt and RegisterMutation.kt for existing mutation classes and add the new ones following the same sealed interface pattern.
  </action>
  <verify>
    `./gradlew :composeApp:compileKotlinJvm :app:auth:compileKotlinJvm` passes. Verify:
    - LoginRoute and RegisterRoute have invitationEmail parameter
    - LoginModel and RegisterModel have invitationEmail field
    - InviteAcceptEvent.NavigateToLogin and NavigateToRegister have email parameter
  </verify>
  <done>
    When navigating from InviteAcceptScreen to Login or Register, the invitation email is passed via route params, pre-fills the email field, and disables editing. Users cannot log in or register with a different email than the invitation target.
  </done>
</task>

</tasks>

<verification>
- `./gradlew :app:admin:compileKotlinJvm :app:auth:compileKotlinJvm :composeApp:compileKotlinJvm` succeeds
- AdminPanelScreen role badges use stringResource() instead of raw invitation.role
- LoginRoute and RegisterRoute have invitationEmail parameter
- Email TerminalInput is disabled when invitationEmail is set
- InviteAcceptEvent passes email alongside token
</verification>

<success_criteria>
1. Admin panel invitation table shows "Member"/"Miembro" (not "MEMBER"), "Admin" (both), "Owner"/"Propietario" (not "OWNER") based on locale
2. Navigating from invitation to Login pre-fills the email and disables the field
3. Navigating from invitation to Register pre-fills the email and disables the field
4. Regular login/register (without invitation) is unaffected — email field remains editable
</success_criteria>

<output>
After completion, create `.planning/phases/21-group-invitations-profiles/21-08-SUMMARY.md`
</output>
