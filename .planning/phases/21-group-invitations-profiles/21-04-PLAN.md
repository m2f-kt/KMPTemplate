---
phase: 21-group-invitations-profiles
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app/admin/src/commonMain/composeResources/values-es/strings.xml
  - app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/RegisterViewModel.kt
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Admin panel invitation table displays in Spanish when locale is set to Spanish"
    - "Register with invitation token navigates to dashboard (server auto-links, client navigates correctly)"
  artifacts:
    - path: "app/admin/src/commonMain/composeResources/values-es/strings.xml"
      provides: "Spanish translations for 17 invitation/revoke keys"
      contains: "admin_invitations_title"
    - path: "app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/RegisterViewModel.kt"
      provides: "Post-registration invitation handling"
      contains: "getInvitation"
  key_links:
    - from: "values-es/strings.xml"
      to: "InvitationsSection composable"
      via: "Compose resource system locale fallback"
      pattern: "admin_invitations_"
    - from: "RegisterViewModel.handleRegister"
      to: "sdk.getInvitation"
      via: "post-registration groupId lookup"
      pattern: "getInvitation.*NavigateToGroup"
---

<objective>
Fix two UAT gaps: (1) Spanish translations missing for invitation strings causing English fallback, and (2) Register with invitation token always navigating to dashboard instead of the joined group.

Purpose: Close UAT gaps 1 and 2 from Phase 21 verification.
Output: Spanish translations complete, register-with-invite navigates correctly.
</objective>

<execution_context>
@/Users/marc/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/marc/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/invitations-spanish-strings.md
@.planning/debug/invite-post-auth-flow.md
@app/admin/src/commonMain/composeResources/values/strings.xml
@app/admin/src/commonMain/composeResources/values-es/strings.xml
@app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/RegisterViewModel.kt
@app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/RegisterEvent.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Spanish translations for 17 invitation string keys</name>
  <files>app/admin/src/commonMain/composeResources/values-es/strings.xml</files>
  <action>
Add the 17 missing Spanish translations to `values-es/strings.xml`. Insert them between the `admin_invite_done` entry (line 33) and `admin_no_group_title` (line 35), matching the position of the English block in `values/strings.xml` (lines 38-55).

Add this block as a new section `<!-- Admin Pending Invitations Section -->`:

```xml
<!-- Admin Pending Invitations Section -->
<string name="admin_invitations_title">Invitaciones Pendientes</string>
<string name="admin_invitations_none">Sin invitaciones</string>
<string name="admin_invitations_email">CORREO</string>
<string name="admin_invitations_role">ROL</string>
<string name="admin_invitations_status">ESTADO</string>
<string name="admin_invitations_actions">ACCIONES</string>
<string name="admin_invitations_accepted">Aceptada</string>
<string name="admin_invitations_revoked">Revocada</string>
<string name="admin_invitations_expired">Expirada</string>
<string name="admin_invitations_expires_days">Expira en %1$d días</string>
<string name="admin_invitations_expires_tomorrow">Expira mañana</string>
<string name="admin_invitations_expires_today">Expira hoy</string>
<string name="admin_revoke_title">Revocar Invitación</string>
<string name="admin_revoke_confirm">¿Revocar invitación a %1$s?</string>
<string name="admin_revoke_cancel">Cancelar</string>
<string name="admin_revoke_submit">Revocar</string>
<string name="admin_revoke_button">Revocar</string>
```

Ensure the key names match the English file EXACTLY (they are referenced by `Res.string.*` in InvitationsSection composable).
  </action>
  <verify>`./gradlew :app:admin:compileKotlinJvm` passes (verifies resource compilation)</verify>
  <done>All 17 invitation string keys exist in both `values/strings.xml` and `values-es/strings.xml` with Spanish translations</done>
</task>

<task type="auto">
  <name>Task 2: Fix RegisterViewModel post-registration invitation handling</name>
  <files>app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/RegisterViewModel.kt</files>
  <action>
In `RegisterViewModel.handleRegister()`, after successful registration (the `ifRight` branch at line 91), add post-registration invitation handling. Currently it always emits `RegisterEvent.NavigateToDashboard`.

Replace the success handler to:
1. Check if `current.invitationToken` is non-null and non-blank
2. If YES: call `sdk.getInvitation(current.invitationToken)` to retrieve the invitation details (which include `groupId`). The server already accepted the invitation during registration, so this is just a lookup — NOT calling `acceptInvitation` (which is not idempotent and would fail with `InvitationAlreadyAccepted`).
3. On `getInvitation` success: emit `RegisterEvent.NavigateToGroup(invitation.groupId)` — this event variant already exists in the sealed interface but is never emitted.
4. On `getInvitation` failure (any reason): fall back to `RegisterEvent.NavigateToDashboard` — the user IS registered and IS in the group (server handled it), just the navigation is best-effort.
5. If NO invitation token: emit `RegisterEvent.NavigateToDashboard` as before.

The key insight: `acceptInvitation` is NOT idempotent (raises `InvitationAlreadyAccepted`), so use `getInvitation` instead — it's a read-only call that returns the `InvitationResponse` DTO with `groupId`.

Important: The `InvitationResponse` DTO field for the group ID is `groupId` (a String).

The code should look like:
```kotlin
ifRight = {
    val token = current.invitationToken
    if (!token.isNullOrBlank()) {
        // Server already accepted invitation during registration.
        // Use getInvitation (read-only) to retrieve groupId for navigation.
        // Do NOT call acceptInvitation — it's not idempotent.
        sdk.getInvitation(token).fold(
            ifLeft = {
                // User IS in the group (server handled it), just navigate to dashboard
                sendEvent(RegisterEvent.NavigateToDashboard)
            },
            ifRight = { invitation ->
                sendEvent(RegisterEvent.NavigateToGroup(invitation.groupId))
            },
        )
    } else {
        sendEvent(RegisterEvent.NavigateToDashboard)
    }
},
```
  </action>
  <verify>`./gradlew :app:auth:compileKotlinJvm` passes</verify>
  <done>RegisterViewModel emits NavigateToGroup(groupId) after registration with invitation token, falls back to NavigateToDashboard on failure or no token</done>
</task>

</tasks>

<verification>
- `./gradlew :app:admin:compileKotlinJvm` passes (Spanish resources compile)
- `./gradlew :app:auth:compileKotlinJvm` passes (RegisterViewModel compiles)
- `./gradlew :composeApp:compileKotlinJvm` passes (full app compiles)
</verification>

<success_criteria>
1. Spanish translation file contains all 17 invitation string keys with correct Spanish text
2. RegisterViewModel.handleRegister() checks invitationToken on success, calls getInvitation to retrieve groupId, and emits NavigateToGroup
3. All modules compile successfully
</success_criteria>

<output>
After completion, create `.planning/phases/21-group-invitations-profiles/21-04-SUMMARY.md`
</output>
