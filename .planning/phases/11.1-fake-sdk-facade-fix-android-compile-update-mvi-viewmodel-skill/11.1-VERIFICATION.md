---
phase: 11.1-fake-sdk-facade-fix-android-compile-update-mvi-viewmodel-skill
verified: 2026-02-18T17:00:00Z
status: passed
score: 8/8 must-haves verified
gaps: []
---

# Phase 11.1: Fake SDK Facade / Fix Android Compile / Update MVI ViewModel Skill — Verification Report

**Phase Goal:** Fix Android compilation blocker, create FakeSdkBuilder composing sub-API fakers into a parent-level fakeSdk {} DSL, migrate LoginViewModel to use Sdk as dependency, and update the mvi-viewmodel skill to document the canonical pattern.
**Verified:** 2026-02-18
**Status:** passed — all must-haves verified
**Re-verification:** Yes — stale sendStatement in source-patterns.md fixed (commit f846f8a)

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|---------|
| 1 | Android target compiles with kotlin-test-junit dependency | VERIFIED | `core/testing/build.gradle.kts` lines 40-42: `androidMain.dependencies { api(libs.kotlin.testJunit) }` |
| 2 | FakeSdkBuilder composes FakeAuthApiBuilder and FakeUserApiBuilder into a fake Sdk | VERIFIED | `FakeSdkBuilder.kt`: `auth {}` and `user {}` DSL functions delegate to sub-builders; `build()` calls `Sdk(authApi = authApiBuilder.build(), userApi = userApiBuilder.build())` |
| 3 | fakeSdk {} top-level DSL function creates a configured Sdk instance | VERIFIED | `FakeSdkBuilder.kt` line 48: `fun fakeSdk(block: FakeSdkBuilder.() -> Unit = {}): Sdk = FakeSdkBuilder().apply(block).build()` |
| 4 | sendStatement is removed from MviViewModel (actual source) | VERIFIED | `MviViewModel.kt` contains only `sendEvent` and `sendMutation`; grep across all .kt files returns zero hits for `sendStatement` |
| 4b | sendStatement is removed from skill reference documentation | VERIFIED | Fixed in commit f846f8a — `source-patterns.md` no longer contains `sendStatement` |
| 5 | LoginViewModel takes Sdk as its only dependency | VERIFIED | `LoginViewModel.kt` line 12-14: `class LoginViewModel(private val sdk: Sdk) : ViewModel()` |
| 6 | LoginViewModelTest uses fakeSdk { auth { ... } } to construct dependencies | VERIFIED | All 3 tests use `fakeSdk { auth { ... } }`; zero references to `fakeAuthApi` in test file |
| 7 | Koin resolves LoginViewModel with Sdk injected from the DI graph | VERIFIED | `AppModule.kt`: `viewModelOf(::LoginViewModel)`; `SdkModule.kt`: `single { Sdk(authApi = get(), userApi = get()) }`; chain: `allAppModules -> sharedModule -> sdkModule` |
| 8 | mvi-viewmodel skill documents Sdk dependency and fakeSdk {} as mandatory | VERIFIED | `SKILL.md` lines 146-149: prescriptive rules with "ALWAYS", "Do NOT" language; `source-patterns.md` includes FakeSdkBuilder source and LoginViewModelTest reference |

**Score:** 8/8 truths verified

---

## Required Artifacts

### Plan 01 Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `core/testing/build.gradle.kts` | Android kotlin-test-junit dependency | VERIFIED | `androidMain.dependencies { api(libs.kotlin.testJunit) }` present at lines 40-42 |
| `core/testing/src/commonMain/kotlin/com/m2f/template/core/testing/fakes/FakeSdkBuilder.kt` | FakeSdkBuilder class and fakeSdk DSL function | VERIFIED | 49-line file; `@FakeSDKDsl class FakeSdkBuilder`, `fun auth()`, `fun user()`, `internal fun build()`, `fun fakeSdk()` |
| `core/mvi/src/commonMain/kotlin/com/m2f/template/core/mvi/MviViewModel.kt` | MviViewModel without sendStatement | VERIFIED | 49 lines; only `sendEvent` and `sendMutation` protected methods |

### Plan 02 Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/LoginViewModel.kt` | LoginViewModel with Sdk dependency | VERIFIED | `class LoginViewModel(private val sdk: Sdk)`, calls `sdk.login(...)` |
| `app/auth/src/commonTest/kotlin/com/m2f/template/app/auth/LoginViewModelTest.kt` | Tests using fakeSdk DSL | VERIFIED | 3 tests; all use `fakeSdk { auth { ... } }` or `fakeSdk()`; `import com.m2f.template.core.testing.fakes.fakeSdk` present |
| `composeApp/src/commonMain/kotlin/com/m2f/template/di/AppModule.kt` | Koin wiring with viewModelOf(::LoginViewModel) | VERIFIED | Line 16: `viewModelOf(::LoginViewModel)` |
| `.claude/skills/mvi-viewmodel/SKILL.md` | Updated skill with Sdk dependency and fakeSdk patterns | VERIFIED | "Dependency faking" section mandates Sdk, prohibits direct fakeAuthApi/fakeUserApi; test examples use fakeSdk |
| `.claude/skills/mvi-viewmodel/references/source-patterns.md` | FakeSdkBuilder source and LoginViewModelTest reference | VERIFIED (with gap) | Contains FakeSdkBuilder source (lines 213-267) and LoginViewModelTest reference; BUT MviViewModel snapshot at line 59 still includes removed sendStatement |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `FakeSdkBuilder.kt` | `com.m2f.template.sdk.Sdk` | Direct constructor call `Sdk(authApi = ..., userApi = ...)` | VERIFIED | `build()` calls `Sdk(authApi = authApiBuilder.build(), userApi = userApiBuilder.build())` — plan deviation (no subclass, direct construction) confirmed correct |
| `FakeSdkBuilder` | `FakeAuthApiBuilder, FakeUserApiBuilder` | `fun auth(init: FakeAuthApiBuilder.() -> Unit)` and `fun user(init: FakeUserApiBuilder.() -> Unit)` | VERIFIED | Both builders composed; `authApiBuilder.init()` and `userApiBuilder.init()` delegate correctly |
| `LoginViewModel.kt` | `com.m2f.template.sdk.Sdk` | Constructor injection `LoginViewModel(sdk: Sdk)` | VERIFIED | `class LoginViewModel(private val sdk: Sdk)` with `sdk.login(...)` call |
| `LoginViewModelTest.kt` | `FakeSdkBuilder.kt` | `import com.m2f.template.core.testing.fakes.fakeSdk` | VERIFIED | Import present; `fakeSdk { ... }` used in all 3 tests |
| `AppModule.kt` | `LoginViewModel` | `viewModelOf(::LoginViewModel)` resolves Sdk from sdkModule | VERIFIED | Chain: `allAppModules -> sharedModule -> sdkModule -> single { Sdk(authApi=get(), userApi=get()) }` |

---

## Requirements Coverage

The plan uses phase-local requirement IDs (FIX-ANDROID, FAKE-SDK, etc.) that are not present in REQUIREMENTS.md. REQUIREMENTS.md uses a different ID scheme (MVI-*, TEST-*). These are planning-level tracking IDs, not REQUIREMENTS.md IDs. The contribution to REQUIREMENTS.md requirements is:

| REQUIREMENTS.md ID | Description | Phase 11.1 Contribution | Status |
|--------------------|-------------|------------------------|--------|
| TEST-03 | SDK API classes extracted to interfaces for fake substitution | FakeSdkBuilder creates fake Sdk from real interface-backed sub-fakers | Contributes |
| TEST-04 | Hand-written fake implementations exist for SDK contracts | FakeSdkBuilder composing FakeAuthApiBuilder + FakeUserApiBuilder | Contributes |
| TEST-05 | Shared test fixtures and utilities available across modules | fakeSdk {} DSL available from core:testing for all ViewModel tests | Contributes |
| MVI-04 | ViewModels injectable via Koin | LoginViewModel migrated; Koin auto-resolves Sdk | Contributes |

Note: REQUIREMENTS.md traceability table does not list phase 11.1 (it was a sub-phase fix, not originally planned). No orphaned requirements found.

---

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `.claude/skills/mvi-viewmodel/references/source-patterns.md` | 59 | `sendStatement` still present in embedded MviViewModel code snapshot — stale reference documentation | Warning | Future Claude sessions reading source-patterns.md will see a conflicting MviViewModel signature with `sendStatement`. The skill documentation should match the actual source. |

No blocking anti-patterns found in production or test code.

---

## Human Verification Required

None. All items were verifiable programmatically.

---

## Gaps Summary

One documentation inconsistency was found:

The `sendStatement` method was correctly removed from `MviViewModel.kt` (the actual source file), confirmed by:
- `MviViewModel.kt` contains only `sendEvent` and `sendMutation`
- `grep -rn "sendStatement" **/*.kt` returns zero results across all Kotlin source files
- Git commit `06550c2` explicitly removed sendStatement

However, the mvi-viewmodel skill's reference file `source-patterns.md` still contains the pre-removal MviViewModel code snapshot, including `sendStatement` at line 59:

```kotlin
protected suspend fun sendStatement(statement: Either<Event, Mutation>) {
    pipeline.emit(statement)
}
```

This is a stale documentation issue. The fix is a one-line deletion in source-patterns.md (remove the `sendStatement` method block from the MviViewModel code snippet).

This is classified as a **warning** (not a blocker for production/test code), but should be fixed to prevent future confusion when Claude sessions use source-patterns.md as an authoritative reference.

---

## Commit Verification

All claimed commits exist and contain the expected changes:

| Commit | Plan | Description | Verified |
|--------|------|-------------|---------|
| `06550c2` | 01 | Android compile fix + sendStatement removal | YES — modifies `build.gradle.kts` (+3 lines) and `MviViewModel.kt` (-4 lines) |
| `0a66c57` | 01 | FakeSdkBuilder creation | YES — adds `FakeSdkBuilder.kt` (49 lines) |
| `a212fa6` | 02 | LoginViewModel migration to Sdk | YES — modifies `LoginViewModel.kt` (6 lines changed) |
| `20612ba` | 02 | LoginViewModelTest fakeSdk migration | YES — modifies `LoginViewModelTest.kt` (15 insertions, 11 deletions) |
| `0dd5fd6` | 02 | Skill documentation update | YES — adds SKILL.md (251 lines) and source-patterns.md (526 lines) |

---

_Verified: 2026-02-18_
_Verifier: Claude (gsd-verifier)_
