---
phase: 11-testing-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/api/AuthApi.kt
  - core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/api/AuthApiImpl.kt
  - core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/api/UserApi.kt
  - core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/api/UserApiImpl.kt
  - core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/Sdk.kt
  - core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/di/SdkModule.kt
autonomous: true
requirements: [TEST-03]

must_haves:
  truths:
    - "AuthApi is an interface; AuthApiImpl is the concrete implementation"
    - "UserApi is an interface; UserApiImpl is the concrete implementation"
    - "Sdk facade class implements all interfaces via by delegation"
    - "Koin binds AuthApiImpl and UserApiImpl to their interface types"
    - "All existing code that injects AuthApi/UserApi continues to compile and resolve via Koin"
  artifacts:
    - path: "core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/api/AuthApi.kt"
      provides: "AuthApi interface with 6 suspend methods returning Either<AppError, T>"
      contains: "interface AuthApi"
    - path: "core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/api/AuthApiImpl.kt"
      provides: "AuthApi implementation with HttpClient and TokenStorage"
      contains: "class AuthApiImpl"
    - path: "core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/api/UserApi.kt"
      provides: "UserApi interface with 3 suspend methods returning Either<AppError, T>"
      contains: "interface UserApi"
    - path: "core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/api/UserApiImpl.kt"
      provides: "UserApi implementation with HttpClient"
      contains: "class UserApiImpl"
    - path: "core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/Sdk.kt"
      provides: "Sdk facade delegating to all API interfaces"
      contains: "class Sdk"
    - path: "core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/di/SdkModule.kt"
      provides: "Updated Koin bindings with interface types"
      contains: "single<AuthApi>"
  key_links:
    - from: "core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/di/SdkModule.kt"
      to: "AuthApiImpl, UserApiImpl"
      via: "single<AuthApi> { AuthApiImpl(...) }"
      pattern: "single<AuthApi>"
    - from: "app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/LoginViewModel.kt"
      to: "core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/api/AuthApi.kt"
      via: "Constructor injection of AuthApi interface"
      pattern: "private val authApi: AuthApi"
---

<objective>
Extract interfaces from SDK API classes (AuthApi, UserApi), rename implementations with Impl suffix, create Sdk facade with delegation, and update Koin bindings.

Purpose: Enable fake substitution in tests by making SDK API classes injectable via interfaces rather than concrete classes. This is the foundation for TEST-03 and unblocks the fake builder DSL in Plan 03.

Output: AuthApi and UserApi as interfaces, AuthApiImpl and UserApiImpl as implementations, Sdk facade, updated SdkModule Koin bindings.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/api/AuthApi.kt
@core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/api/UserApi.kt
@core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/Sdk.kt
@core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/di/SdkModule.kt
@core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/AuthInterceptor.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract SDK interfaces and rename implementations</name>
  <files>
    core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/api/AuthApi.kt
    core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/api/AuthApiImpl.kt
    core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/api/UserApi.kt
    core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/api/UserApiImpl.kt
  </files>
  <action>
    **Per locked decision: interface gets the clean name, concrete implementation gets Impl suffix.**

    **AuthApi extraction:**
    1. Replace the contents of `AuthApi.kt` with an interface named `AuthApi` containing the 6 public method signatures:
       - `suspend fun register(request: RegisterRequest): Either<AppError, AuthResponse>`
       - `suspend fun login(request: LoginRequest, rememberMe: Boolean = true): Either<AppError, AuthResponse>`
       - `suspend fun refresh(request: RefreshTokenRequest): Either<AppError, AuthResponse>`
       - `suspend fun forgotPassword(request: ForgotPasswordRequest): Either<AppError, Unit>`
       - `suspend fun resetPassword(request: ResetPasswordRequest): Either<AppError, Unit>`
       - `suspend fun logout(): Either<AppError, Unit>`
       Keep the KDoc. Only import what the interface needs (arrow.core.Either, model DTOs, AppError).

    2. Create `AuthApiImpl.kt` in the same package. Move the current class body here as `class AuthApiImpl(private val client: HttpClient, private val tokenStorage: TokenStorage) : AuthApi`. Keep ALL existing implementation code unchanged -- the only changes are the class name and the `: AuthApi` interface.

    **UserApi extraction:**
    3. Replace the contents of `UserApi.kt` with an interface named `UserApi` containing 3 method signatures:
       - `suspend fun getProfile(): Either<AppError, UserResponse>`
       - `suspend fun updateProfile(request: UpdateProfileRequest): Either<AppError, UserResponse>`
       - `suspend fun getUserById(id: String): Either<AppError, UserResponse>`
       Keep the KDoc.

    4. Create `UserApiImpl.kt` in the same package. Move the current class body here as `class UserApiImpl(private val client: HttpClient) : UserApi`. Keep ALL existing implementation code unchanged.

    **IMPORTANT:** The existing imports in consumers (LoginViewModel, RegisterViewModel, ForgotPasswordViewModel, ProfileViewModel) import `com.m2f.template.sdk.api.AuthApi` and `com.m2f.template.sdk.api.UserApi`. Since the interface keeps the same name and package, NO consumer changes are needed.
  </action>
  <verify>
    Run `./gradlew :core:sdk:compileKotlinJvm` -- must compile with no errors.
    Run `./gradlew :app:auth:compileKotlinJvm :app:profile:compileKotlinJvm :app:dashboard:compileKotlinJvm` -- all consumer modules must compile without changes.
  </verify>
  <done>AuthApi.kt is an interface with 6 methods, AuthApiImpl.kt is the implementation. UserApi.kt is an interface with 3 methods, UserApiImpl.kt is the implementation. All consumer modules compile unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Create Sdk facade and update Koin bindings</name>
  <files>
    core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/Sdk.kt
    core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/di/SdkModule.kt
  </files>
  <action>
    **Per locked decision: Sdk class implements all interfaces via Kotlin `by` delegation. Koin provides each *ApiImpl binding; Sdk delegates to them.**

    1. **Sdk.kt** -- Replace the empty package declaration with:
       ```kotlin
       class Sdk(
           private val authApi: AuthApi,
           private val userApi: UserApi,
       ) : AuthApi by authApi, UserApi by userApi
       ```
       Import `com.m2f.template.sdk.api.AuthApi` and `com.m2f.template.sdk.api.UserApi`.

    2. **SdkModule.kt** -- Update Koin bindings to use explicit interface types:
       - `single<AuthApi> { AuthApiImpl(client = get(), tokenStorage = get()) }` (was `single { AuthApi(...) }`)
       - `single<UserApi> { UserApiImpl(client = get()) }` (was `single { UserApi(...) }`)
       - Add: `single { Sdk(authApi = get(), userApi = get()) }` for optional facade access
       - Update imports: add `AuthApiImpl`, `UserApiImpl`, and `Sdk`.
       - Keep `AuthInterceptor` and `createApiClient` bindings unchanged.

    **CRITICAL:** The `single<AuthApi>` type parameter is essential. Without it, Koin registers `AuthApiImpl` but consumers inject `AuthApi`, causing `NoBeanDefFoundException` at runtime.
  </action>
  <verify>
    Run `./gradlew :core:sdk:compileKotlinJvm` -- must compile.
    Run `./gradlew :composeApp:compileKotlinJvm` -- full app compilation must succeed (Koin graph checked at compile time for type mismatches in Koin DSL).
    Verify the Koin module has `single<AuthApi>` and `single<UserApi>` type-qualified bindings by reading the updated SdkModule.kt.
  </verify>
  <done>Sdk facade exists with delegation to AuthApi and UserApi. SdkModule binds AuthApiImpl to AuthApi interface type and UserApiImpl to UserApi interface type. Full project compiles.</done>
</task>

</tasks>

<verification>
1. `./gradlew :core:sdk:compileKotlinJvm` succeeds
2. `./gradlew :app:auth:compileKotlinJvm :app:profile:compileKotlinJvm` succeeds (consumers unchanged)
3. `AuthApi.kt` contains `interface AuthApi` (not `class`)
4. `UserApi.kt` contains `interface UserApi` (not `class`)
5. `SdkModule.kt` contains `single<AuthApi> { AuthApiImpl` and `single<UserApi> { UserApiImpl`
6. `Sdk.kt` contains `class Sdk` with `AuthApi by authApi, UserApi by userApi`
</verification>

<success_criteria>
- AuthApi and UserApi are interfaces; AuthApiImpl and UserApiImpl are concrete classes implementing them
- Sdk facade delegates to all interfaces via `by`
- Koin bindings use interface type qualifiers (`single<AuthApi>`)
- All existing consumer modules compile without modification
- No Koin NoBeanDefFoundException -- interface bindings resolve correctly
</success_criteria>

<output>
After completion, create `.planning/phases/11-testing-infrastructure/11-01-SUMMARY.md`
</output>
