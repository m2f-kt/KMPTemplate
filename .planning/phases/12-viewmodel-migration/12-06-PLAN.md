---
phase: 12-viewmodel-migration
plan: 06
type: execute
wave: 4
depends_on: [12-01, 12-02, 12-03, 12-04, 12-05]
files_modified:
  - app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/LoginState.kt
  - app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/RegisterState.kt
  - app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/ForgotPasswordState.kt
  - app/profile/src/commonMain/kotlin/com/m2f/template/app/profile/ProfileState.kt
  - app/dashboard/src/commonMain/kotlin/com/m2f/template/app/dashboard/DashboardState.kt
autonomous: true
requirements: [MVI-05]

must_haves:
  truths:
    - "No *State.kt files remain in app/auth, app/profile, or app/dashboard"
    - "No imports of LoginState, RegisterState, ForgotPasswordState, ProfileState, or DashboardState exist in the codebase"
    - "All 5 ViewModels, Screens, and AppNavHost exclusively use the new Model types"
    - "Full project compiles and all tests pass"
  artifacts: []
  key_links: []
---

<objective>
Remove all legacy State data classes and verify the complete migration compiles and tests pass.

Purpose: After all 5 ViewModels are migrated (Plans 01-05), the old *State.kt files are dead code. This plan removes them and does a final full verification that the migration is complete and clean.
Output: No legacy State files remain, full project builds and tests pass.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-viewmodel-migration/12-RESEARCH.md
@.planning/phases/12-viewmodel-migration/12-01-SUMMARY.md
@.planning/phases/12-viewmodel-migration/12-02-SUMMARY.md
@.planning/phases/12-viewmodel-migration/12-03-SUMMARY.md
@.planning/phases/12-viewmodel-migration/12-04-SUMMARY.md
@.planning/phases/12-viewmodel-migration/12-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete legacy State files and verify full project builds and tests pass</name>
  <files>
    app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/LoginState.kt
    app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/RegisterState.kt
    app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/ForgotPasswordState.kt
    app/profile/src/commonMain/kotlin/com/m2f/template/app/profile/ProfileState.kt
    app/dashboard/src/commonMain/kotlin/com/m2f/template/app/dashboard/DashboardState.kt
  </files>
  <action>
    **Step 1: Verify no remaining references** to the old State classes anywhere in the codebase:
    - Search for `LoginState` in all .kt files -- should only appear in LoginState.kt itself
    - Search for `RegisterState` -- only in RegisterState.kt itself
    - Search for `ForgotPasswordState` -- only in ForgotPasswordState.kt itself
    - Search for `ProfileState` -- only in ProfileState.kt itself
    - Search for `DashboardState` -- only in DashboardState.kt itself
    - If ANY references exist outside the State files themselves, fix them first (they should have been migrated in Plans 01-05)

    **Step 2: Delete the 5 State files:**
    - `rm app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/LoginState.kt`
    - `rm app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/RegisterState.kt`
    - `rm app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/ForgotPasswordState.kt`
    - `rm app/profile/src/commonMain/kotlin/com/m2f/template/app/profile/ProfileState.kt`
    - `rm app/dashboard/src/commonMain/kotlin/com/m2f/template/app/dashboard/DashboardState.kt`

    **Step 3: Full project compilation verification:**
    - `./gradlew compileCommonMainKotlinMetadata` -- all modules compile
    - Verify no "unresolved reference" errors

    **Step 4: Full test suite:**
    - `./gradlew :app:auth:allTests` -- Login, Register, ForgotPassword tests pass
    - `./gradlew :app:profile:allTests` -- Profile tests pass
    - `./gradlew :app:dashboard:allTests` -- Dashboard tests pass
  </action>
  <verify>
    Run `./gradlew compileCommonMainKotlinMetadata` -- entire project compiles.
    Run `./gradlew :app:auth:allTests :app:profile:allTests :app:dashboard:allTests` -- all ViewModel tests pass.
    Verify with `grep -r "LoginState\|RegisterState\|ForgotPasswordState\|ProfileState\|DashboardState" --include="*.kt" app/ composeApp/` returns nothing.
  </verify>
  <done>All 5 legacy *State.kt files deleted, no remaining references, full project compiles and all ViewModel tests pass.</done>
</task>

</tasks>

<verification>
1. `./gradlew compileCommonMainKotlinMetadata` passes -- full project compiles
2. `./gradlew :app:auth:allTests :app:profile:allTests :app:dashboard:allTests` passes -- all VM tests pass
3. No *State.kt files in app/auth, app/profile, or app/dashboard
4. grep for old State class names returns empty
5. All 5 ViewModels exclusively use MviViewModel with Model types
</verification>

<success_criteria>
- Zero *State.kt files remain in feature modules
- Zero references to old State classes in any .kt file
- Full project compiles without errors
- All ViewModel tests pass across all targets
- Phase 12 migration is complete and clean
</success_criteria>

<output>
After completion, create `.planning/phases/12-viewmodel-migration/12-06-SUMMARY.md`
</output>
