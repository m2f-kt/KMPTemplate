---
phase: 15-localization
plan: 12
type: execute
wave: 1
depends_on: []
files_modified:
  - composeApp/src/wasmJsMain/kotlin/com/m2f/template/localization/AppLocale.wasmJs.kt
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "WASM target compiles without js() expression error"
  artifacts:
    - path: "composeApp/src/wasmJsMain/kotlin/com/m2f/template/localization/AppLocale.wasmJs.kt"
      provides: "Platform locale detection for WASM"
      contains: "private fun navigatorLanguage"
  key_links: []
---

<objective>
Fix WASM compilation error: js() call must be the sole expression in a top-level function body.

Purpose: Kotlin/Wasm 2.3.10 requires js("...") to be the ENTIRE and SOLE expression of a top-level function body — no method chaining (.toString().take(2)) is allowed on the same line. The previous fix (15-11) extracted js() from an elvis expression but still chains methods on it.
Output: AppLocale.wasmJs.kt compiles on wasmJs target.
</objective>

<execution_context>
@/Users/marc/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/marc/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/debug/wasm-js-call-compile-error.md
@composeApp/src/wasmJsMain/kotlin/com/m2f/template/localization/AppLocale.wasmJs.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split js() into its own top-level function</name>
  <files>composeApp/src/wasmJsMain/kotlin/com/m2f/template/localization/AppLocale.wasmJs.kt</files>
  <action>
Replace the current `browserLanguage()` function with two separate functions:

1. `private fun navigatorLanguage(): JsString = js("navigator.language")` — This function body contains ONLY the js() call. Nothing else. JsString is the Kotlin/Wasm interop type returned by js() string expressions.

2. `private fun browserLanguage(): String = navigatorLanguage().toString().take(2)` — This function calls navigatorLanguage() and chains .toString().take(2) on the result.

The key constraint: js() must be the ONLY expression in the function body. No chaining, no wrapping, no surrounding code.

Import `kotlin.js.JsString` if not auto-imported (it's in the kotlin.js package available in wasmJsMain).
  </action>
  <verify>
Run `./gradlew composeApp:wasmJsBrowserDistribution` (or `./gradlew composeApp:compileKotlinWasmJs` for faster check). Must compile without the "Calls to 'js(code)' must be a single expression" error.
  </verify>
  <done>WASM target compiles successfully. No js() expression error at AppLocale.wasmJs.kt.</done>
</task>

</tasks>

<verification>
- `./gradlew composeApp:compileKotlinWasmJs` succeeds with exit code 0
- No "Calls to 'js(code)' must be a single expression" errors in build output
</verification>

<success_criteria>
WASM/JS target compiles without errors. The js() call is isolated in its own top-level function body.
</success_criteria>

<output>
After completion, create `.planning/phases/15-localization/15-12-SUMMARY.md`
</output>
