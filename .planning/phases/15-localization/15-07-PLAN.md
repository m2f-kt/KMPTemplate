---
phase: 15-localization
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - app/admin/build.gradle.kts
  - app/admin/src/commonMain/composeResources/values/strings.xml
  - app/admin/src/commonMain/composeResources/values-es/strings.xml
  - app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/StringKeyResolver.kt
  - app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/AdminPanelScreen.kt
  - app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/RegisterMemberScreen.kt
  - app/dashboard/build.gradle.kts
  - app/dashboard/src/commonMain/composeResources/values/strings.xml
  - app/dashboard/src/commonMain/composeResources/values-es/strings.xml
  - app/dashboard/src/commonMain/kotlin/com/m2f/template/app/dashboard/StringKeyResolver.kt
  - app/dashboard/src/commonMain/kotlin/com/m2f/template/app/dashboard/DashboardScreen.kt
  - app/dashboard/src/commonMain/kotlin/com/m2f/template/app/dashboard/DashboardSidebar.kt
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "AdminPanel screen displays all UI strings via stringResource(Res.string.*) instead of hardcoded English"
    - "RegisterMember screen displays error messages via resolveStringKey() instead of raw StringKey.code"
    - "Dashboard screen and sidebar display all UI strings via stringResource(Res.string.*) instead of hardcoded English"
    - "Admin and Dashboard screen strings update when user switches locale to Spanish"
  artifacts:
    - path: "app/admin/src/commonMain/composeResources/values/strings.xml"
      provides: "Per-module English string resources for admin screens"
      contains: "<resources>"
    - path: "app/admin/src/commonMain/composeResources/values-es/strings.xml"
      provides: "Per-module Spanish string resources for admin screens"
      contains: "<resources>"
    - path: "app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/StringKeyResolver.kt"
      provides: "Local resolveStringKey bridge for admin module"
      contains: "@Composable"
    - path: "app/dashboard/src/commonMain/composeResources/values/strings.xml"
      provides: "Per-module English string resources for dashboard screens"
      contains: "<resources>"
    - path: "app/dashboard/src/commonMain/composeResources/values-es/strings.xml"
      provides: "Per-module Spanish string resources for dashboard screens"
      contains: "<resources>"
    - path: "app/dashboard/src/commonMain/kotlin/com/m2f/template/app/dashboard/StringKeyResolver.kt"
      provides: "Local resolveStringKey bridge for dashboard module (even though dashboard currently has no error fields, for pattern consistency and future-proofing)"
      contains: "@Composable"
  key_links:
    - from: "AdminPanelScreen.kt"
      to: "Res.string.*"
      via: "stringResource(Res.string.admin_*) calls"
      pattern: "stringResource\\(Res\\.string\\.admin_"
    - from: "RegisterMemberScreen.kt"
      to: "StringKeyResolver.kt"
      via: "resolveStringKey(fieldError) and resolveStringKey(serverError)"
      pattern: "resolveStringKey\\("
    - from: "DashboardScreen.kt"
      to: "Res.string.*"
      via: "stringResource(Res.string.dashboard_*) calls"
      pattern: "stringResource\\(Res\\.string\\.dashboard_"
    - from: "DashboardSidebar.kt"
      to: "Res.string.*"
      via: "stringResource(Res.string.*) for brand and logout"
      pattern: "stringResource\\(Res\\.string\\."
---

<objective>
Localize the Admin and Dashboard module screens by adding per-module Compose resource infrastructure and replacing all hardcoded English strings with stringResource() calls, following the exact auth module pattern from Plan 02.

Purpose: Close the verification gap — admin and dashboard screen strings don't update on locale switch because they're hardcoded English. RegisterMember shows raw StringKey.code instead of localized error text.
Output: Admin (AdminPanel + RegisterMember) and Dashboard (DashboardScreen + DashboardSidebar) screens fully localized with per-module strings.xml (en + es) and resolveStringKey bridges.
</objective>

<execution_context>
@/Users/marc/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/marc/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-localization/15-02-SUMMARY.md

Reference files (the auth module pattern to replicate):
@app/auth/build.gradle.kts
@app/auth/src/commonMain/composeResources/values/strings.xml
@app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/StringKeyResolver.kt

Source strings to copy from:
@composeApp/src/commonMain/composeResources/values/strings.xml (lines 81-101 for dashboard, 102-108 for nav, 131-163 for admin/register_member, 179-231 for errors)
@composeApp/src/commonMain/composeResources/values-es/strings.xml (same line ranges)

Files to modify:
@app/admin/build.gradle.kts
@app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/AdminPanelScreen.kt
@app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/RegisterMemberScreen.kt
@app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/AdminPanelModel.kt
@app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/RegisterMemberModel.kt
@app/dashboard/build.gradle.kts
@app/dashboard/src/commonMain/kotlin/com/m2f/template/app/dashboard/DashboardScreen.kt
@app/dashboard/src/commonMain/kotlin/com/m2f/template/app/dashboard/DashboardSidebar.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add compose resources to admin + dashboard modules and create per-module strings + StringKeyResolvers</name>
  <files>
    app/admin/build.gradle.kts
    app/admin/src/commonMain/composeResources/values/strings.xml
    app/admin/src/commonMain/composeResources/values-es/strings.xml
    app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/StringKeyResolver.kt
    app/dashboard/build.gradle.kts
    app/dashboard/src/commonMain/composeResources/values/strings.xml
    app/dashboard/src/commonMain/composeResources/values-es/strings.xml
    app/dashboard/src/commonMain/kotlin/com/m2f/template/app/dashboard/StringKeyResolver.kt
  </files>
  <action>
    **ADMIN MODULE**

    **Step 1: Add compose.components.resources to app/admin/build.gradle.kts**
    Add `implementation(compose.components.resources)` to the `commonMain.dependencies` block, right after `implementation(compose.foundation)`. Note: admin module does NOT currently have `compose.foundation` — add it after the existing `implementation(compose.runtime)` line. Actually, check the existing deps — admin has `compose.runtime` but look at auth's pattern; auth has `compose.foundation` + `compose.components.resources`. Just add `implementation(compose.components.resources)` after the last compose dependency line.

    **Step 2: Create admin module English strings.xml**
    Create `app/admin/src/commonMain/composeResources/values/strings.xml` containing:
    - All admin_* strings from composeApp (lines 132-146): admin_title, admin_back, admin_loading, admin_error_title, admin_error_description, admin_member_count, admin_slug_prefix, admin_load_more, admin_register_member_button, admin_table_name, admin_table_email, admin_table_role, admin_table_joined
    - All register_member_* strings from composeApp (lines 149-163): register_member_title, register_member_back, register_member_first_name_label, register_member_first_name_placeholder, register_member_last_name_label, register_member_last_name_placeholder, register_member_email_label, register_member_email_placeholder, register_member_password_label, register_member_password_placeholder, register_member_role_label, register_member_role_member, register_member_role_admin, register_member_button, register_member_button_loading
    - ALL 32 error_* strings from composeApp (lines 179-231) for the resolveStringKey bridge

    **Step 3: Create admin module Spanish strings.xml**
    Create `app/admin/src/commonMain/composeResources/values-es/strings.xml` with the same structure — Spanish translations for all admin_*, register_member_*, and error_* strings from composeApp values-es.

    **Step 4: Create admin StringKeyResolver.kt**
    Create `app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/StringKeyResolver.kt` — copy from auth module, change:
    - Package: `com.m2f.template.app.admin`
    - All `template.app.auth.generated.resources.*` imports → `template.app.admin.generated.resources.*`
    - Keep `internal fun`, keep exhaustive when body identical.

    **DASHBOARD MODULE**

    **Step 5: Add compose.components.resources to app/dashboard/build.gradle.kts**
    Add `implementation(compose.components.resources)` to `commonMain.dependencies`, same position as admin.

    **Step 6: Create dashboard module English strings.xml**
    Create `app/dashboard/src/commonMain/composeResources/values/strings.xml` containing:
    - All dashboard_* strings from composeApp (lines 82-100): dashboard_system_overview, dashboard_nodes_active, dashboard_active_processes, dashboard_deployment_status, dashboard_deployment_subtitle, dashboard_deployment_build, dashboard_deployment_tests, dashboard_deployment_deploy, dashboard_recent_activity, dashboard_under_construction, dashboard_status_pending, dashboard_logout, dashboard_table_pid, dashboard_table_name, dashboard_table_cpu, dashboard_table_memory, dashboard_table_status
    - All nav_* strings from composeApp (lines 103-108): nav_dashboard, nav_processes, nav_logs, nav_deployments, nav_admin, nav_settings
    - Common brand strings needed by sidebar: common_brand_prompt (">_"), common_brand_name ("terminal")
    - ALL 32 error_* strings from composeApp (lines 179-231) — even though dashboard currently doesn't display errors, include them for pattern consistency and future-proofing, since StringKeyResolver's exhaustive when requires them

    **Step 7: Create dashboard module Spanish strings.xml**
    Create `app/dashboard/src/commonMain/composeResources/values-es/strings.xml` with the same structure — Spanish translations.

    **Step 8: Create dashboard StringKeyResolver.kt**
    Create `app/dashboard/src/commonMain/kotlin/com/m2f/template/app/dashboard/StringKeyResolver.kt` — copy from auth module, change:
    - Package: `com.m2f.template.app.dashboard`
    - All `template.app.auth.generated.resources.*` imports → `template.app.dashboard.generated.resources.*`
  </action>
  <verify>
    Run both in sequence:
    1. `./gradlew :app:admin:compileKotlinMetadata` — should compile cleanly
    2. `./gradlew :app:dashboard:compileKotlinMetadata` — should compile cleanly
  </verify>
  <done>
    Both admin and dashboard modules have compose.components.resources dependency, per-module strings.xml (en + es), and local resolveStringKey bridges that compile.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace all hardcoded strings in AdminPanelScreen, RegisterMemberScreen, DashboardScreen, and DashboardSidebar</name>
  <files>
    app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/AdminPanelScreen.kt
    app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/RegisterMemberScreen.kt
    app/dashboard/src/commonMain/kotlin/com/m2f/template/app/dashboard/DashboardScreen.kt
    app/dashboard/src/commonMain/kotlin/com/m2f/template/app/dashboard/DashboardSidebar.kt
  </files>
  <action>
    **ADMIN PANEL SCREEN (AdminPanelScreen.kt)**

    Add imports:
    ```
    import org.jetbrains.compose.resources.stringResource
    import template.app.admin.generated.resources.Res
    import template.app.admin.generated.resources.*
    ```

    Replace hardcoded strings (using exact line references from current file):
    - `"<-"` (line 65, button text) → `stringResource(Res.string.admin_back)`
    - `"> admin_panel"` (line 69) → `stringResource(Res.string.admin_title)`
    - `"// loading..."` (line 78) → `stringResource(Res.string.admin_loading)`
    - `"error"` (line 88, title param) → `stringResource(Res.string.admin_error_title)`
    - `"// failed to load admin panel"` (line 89, description param) → `stringResource(Res.string.admin_error_description)`
    - `"error: ${state.error.code}"` (line 93, badge text) → `"error: ${resolveStringKey(state.error)}"` — BUT better: use `stringResource(Res.string.admin_error_title) + ": " + resolveStringKey(state.error)`. Actually, since the badge currently shows `"error: ${state.error.code}"`, replace with `resolveStringKey(state.error)` only (the TerminalCard above already shows "error" as title). So the badge text becomes: `resolveStringKey(state.error)`
    - `"slug: ${state.groupSlug}"` (line 104, description param) → `stringResource(Res.string.admin_slug_prefix, state.groupSlug)`
    - `"${state.memberCount} members"` (line 116, badge text) → `stringResource(Res.string.admin_member_count, state.memberCount)`
    - `listOf("NAME", "EMAIL", "ROLE", "JOINED")` (line 126, headers) → `listOf(stringResource(Res.string.admin_table_name), stringResource(Res.string.admin_table_email), stringResource(Res.string.admin_table_role), stringResource(Res.string.admin_table_joined))`
    - `"// loading..."` (line 155) → `stringResource(Res.string.admin_loading)`
    - `"load_more"` (line 161) → `stringResource(Res.string.admin_load_more)`
    - `"+ register_member"` (line 169) → `stringResource(Res.string.admin_register_member_button)`

    **REGISTER MEMBER SCREEN (RegisterMemberScreen.kt)**

    Add imports:
    ```
    import org.jetbrains.compose.resources.stringResource
    import template.app.admin.generated.resources.Res
    import template.app.admin.generated.resources.*
    ```
    Note: RegisterMemberScreen is in the admin module, so it uses `template.app.admin.generated.resources`.

    Replace hardcoded strings:
    - `"<-"` (line 72, button text) → `stringResource(Res.string.register_member_back)`
    - `"> register_member"` (line 76) → `stringResource(Res.string.register_member_title)`
    - `"first_name"` (line 87, label) → `stringResource(Res.string.register_member_first_name_label)`
    - `"John"` (line 88, placeholder) → `stringResource(Res.string.register_member_first_name_placeholder)`
    - `firstNameError?.code` (line 90, errorMessage) → `firstNameError?.let { resolveStringKey(it) }`  (CRITICAL: replace .code with resolveStringKey)
    - `"last_name"` (line 98, label) → `stringResource(Res.string.register_member_last_name_label)`
    - `"Doe"` (line 99, placeholder) → `stringResource(Res.string.register_member_last_name_placeholder)`
    - `lastNameError?.code` (line 101, errorMessage) → `lastNameError?.let { resolveStringKey(it) }`
    - `"email"` (line 109, label) → `stringResource(Res.string.register_member_email_label)`
    - `"user@example.com"` (line 110, placeholder) → `stringResource(Res.string.register_member_email_placeholder)`
    - `emailError?.code` (line 112, errorMessage) → `emailError?.let { resolveStringKey(it) }`
    - `"password"` (line 120, label) → `stringResource(Res.string.register_member_password_label)`
    - `"min. 8 characters"` (line 121, placeholder) → `stringResource(Res.string.register_member_password_placeholder)`
    - `passwordError?.code` (line 123, errorMessage) → `passwordError?.let { resolveStringKey(it) }`
    - `"role"` (line 129, text) → `stringResource(Res.string.register_member_role_label)`
    - `"Member"` (line 135, badge text) → `stringResource(Res.string.register_member_role_member)`
    - `"Admin"` (line 143, badge text) → `stringResource(Res.string.register_member_role_admin)`
    - `state.serverError.code` (line 156, badge text) → `resolveStringKey(state.serverError)`  (CRITICAL: replace .code with resolveStringKey)
    - `if (state.isLoading) "$ registering..." else "register"` (line 163) → `if (state.isLoading) stringResource(Res.string.register_member_button_loading) else stringResource(Res.string.register_member_button)`

    **DASHBOARD SCREEN (DashboardScreen.kt)**

    Add imports:
    ```
    import org.jetbrains.compose.resources.stringResource
    import template.app.dashboard.generated.resources.Res
    import template.app.dashboard.generated.resources.*
    ```

    Replace hardcoded strings. Note: many strings appear in BOTH desktop and mobile layouts. Replace ALL occurrences:
    - `"$ system_overview"` (lines 199 and 298) → `stringResource(Res.string.dashboard_system_overview)`
    - `"[4 nodes active]"` (line 304) → `stringResource(Res.string.dashboard_nodes_active)`
    - `"active_processes"` (lines 369 and 508) → `stringResource(Res.string.dashboard_active_processes)`
    - `"deployment_status"` (line 540, title param) → `stringResource(Res.string.dashboard_deployment_status)`
    - `"// pipeline progress"` (line 541, description param) → `stringResource(Res.string.dashboard_deployment_subtitle)`
    - `"Build"` (line 547, label param) → `stringResource(Res.string.dashboard_deployment_build)`
    - `"Tests"` (line 551, label param) → `stringResource(Res.string.dashboard_deployment_tests)`
    - `"Deploy"` (line 555, label param) → `stringResource(Res.string.dashboard_deployment_deploy)`
    - `"recent_activity"` (line 563) → `stringResource(Res.string.dashboard_recent_activity)`
    - `"// under construction"` (lines 409 and 438, description param) → `stringResource(Res.string.dashboard_under_construction)`
    - `"status: pending"` (lines 417 and 446, badge text) → `stringResource(Res.string.dashboard_status_pending)`
    - `listOf("PID", "NAME", "CPU", "MEMORY", "STATUS")` (line 514) → `listOf(stringResource(Res.string.dashboard_table_pid), stringResource(Res.string.dashboard_table_name), stringResource(Res.string.dashboard_table_cpu), stringResource(Res.string.dashboard_table_memory), stringResource(Res.string.dashboard_table_status))`

    For mobile brand header (lines 265-271):
    - `">_"` (line 265) → `stringResource(Res.string.common_brand_prompt)`
    - `"terminal"` (line 270) → `stringResource(Res.string.common_brand_name)`

    For placeholder content nav titles like `"> processes"`, `"> logs"`, `"> deployments"`, `"> settings"` — these use the nav_* string resources. Replace:
    - `"> processes"` (lines 153, 323) → `"> " + stringResource(Res.string.nav_processes)` OR keep as-is since the `>` prefix is UI chrome. Better approach: use `stringResource(Res.string.nav_processes)` and prepend "> " in the PlaceholderContent composable. Actually, looking more carefully, PlaceholderContent receives `title` param and strips the `> ` prefix for the card title. The simplest correct approach: pass the nav label from stringResource and add "> " prefix at call site. Replace each:
      - `PlaceholderContent(title = "> processes", ...)` → `PlaceholderContent(title = "> " + stringResource(Res.string.nav_processes), ...)`
      - Same for "logs", "deployments", "settings" — use nav_logs, nav_deployments, nav_settings
      - `else ->` branch with `"> ${state.selectedNavItem}"` can stay dynamic
    - Mobile versions (lines 323, 326, 329): same pattern with MobilePlaceholderContent

    **DASHBOARD SIDEBAR (DashboardSidebar.kt)**

    Add imports:
    ```
    import org.jetbrains.compose.resources.stringResource
    import template.app.dashboard.generated.resources.Res
    import template.app.dashboard.generated.resources.*
    ```

    Replace hardcoded strings:
    - `">_"` (line 91) → `stringResource(Res.string.common_brand_prompt)`
    - `"terminal"` (line 96) → `stringResource(Res.string.common_brand_name)`
    - `"$ logout"` (line 171) → `stringResource(Res.string.dashboard_logout)`
    - The nav items built in lines 105-112 use hardcoded labels like `"dashboard"`, `"processes"`, `"logs"`, `"deployments"`, `"admin"`, `"settings"`. Replace:
      ```kotlin
      val navItems = buildList {
          add("dashboard" to stringResource(Res.string.nav_dashboard))
          add("processes" to stringResource(Res.string.nav_processes))
          add("logs" to stringResource(Res.string.nav_logs))
          add("deployments" to stringResource(Res.string.nav_deployments))
          if (isAdmin) add("admin" to stringResource(Res.string.nav_admin))
          add("settings" to stringResource(Res.string.nav_settings))
      }
      ```
      The `key` (first element) stays as the English ID for routing logic; only the `label` (second element, displayed in SidebarNavItem) uses stringResource.
  </action>
  <verify>
    1. Run `./gradlew :app:admin:compileKotlinMetadata` — must compile cleanly
    2. Run `./gradlew :app:dashboard:compileKotlinMetadata` — must compile cleanly
    3. Run `grep -r '\.code' app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/AdminPanelScreen.kt app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/RegisterMemberScreen.kt` — should return ZERO matches
    4. Run `grep -c 'stringResource' app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/AdminPanelScreen.kt` — should return 10+
    5. Run `grep -c 'stringResource' app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/RegisterMemberScreen.kt` — should return 14+
    6. Run `grep -c 'resolveStringKey' app/admin/src/commonMain/kotlin/com/m2f/template/app/admin/RegisterMemberScreen.kt` — should return 5 (4 field errors + 1 server error)
    7. Run `grep -c 'stringResource' app/dashboard/src/commonMain/kotlin/com/m2f/template/app/dashboard/DashboardScreen.kt` — should return 15+
    8. Run `grep -c 'stringResource' app/dashboard/src/commonMain/kotlin/com/m2f/template/app/dashboard/DashboardSidebar.kt` — should return 8+
  </verify>
  <done>
    AdminPanelScreen, RegisterMemberScreen, DashboardScreen, and DashboardSidebar have zero hardcoded English strings — all UI text uses stringResource(Res.string.*) and all error display uses resolveStringKey(). Strings will update when user switches locale.
  </done>
</task>

</tasks>

<verification>
1. `./gradlew :app:admin:compileKotlinMetadata && ./gradlew :app:dashboard:compileKotlinMetadata` compiles cleanly
2. No hardcoded English strings remain in AdminPanelScreen.kt, RegisterMemberScreen.kt, DashboardScreen.kt, DashboardSidebar.kt
3. All `.code` usages (raw StringKey enum name display) replaced with resolveStringKey() calls
4. Per-module strings.xml exists in both values/ and values-es/ directories for both modules
5. StringKeyResolver.kt in both modules has exhaustive when over all 32 StringKey entries
</verification>

<success_criteria>
Admin and Dashboard screens render all UI text via stringResource(Res.string.*) and all error messages via resolveStringKey(). When a user switches locale from English to Spanish, all admin panel, register member, dashboard, and sidebar strings update to Spanish translations. The gap "User can switch locale at runtime and all UI strings update without app restart" is fully closed.
</success_criteria>

<output>
After completion, create `.planning/phases/15-localization/15-07-SUMMARY.md`
</output>
