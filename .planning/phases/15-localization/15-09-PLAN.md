---
phase: 15-localization
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - app/profile/src/commonMain/kotlin/com/m2f/template/app/profile/ProfileSidebar.kt
  - app/profile/src/commonMain/kotlin/com/m2f/template/app/profile/tier/FreeTierContent.kt
  - app/profile/src/commonMain/kotlin/com/m2f/template/app/profile/tier/PaidTierContent.kt
  - app/profile/src/commonMain/kotlin/com/m2f/template/app/profile/tier/PremiumTierContent.kt
  - app/profile/src/commonMain/kotlin/com/m2f/template/app/profile/tier/AdminTierContent.kt
  - app/profile/src/commonMain/kotlin/com/m2f/template/app/profile/tier/PowerAdminTierContent.kt
  - app/dashboard/src/commonMain/kotlin/com/m2f/template/app/dashboard/DashboardBottomNav.kt
  - app/dashboard/src/commonMain/kotlin/com/m2f/template/app/dashboard/DashboardMockData.kt
  - app/dashboard/src/commonMain/kotlin/com/m2f/template/app/dashboard/DashboardScreen.kt
  - app/profile/src/commonMain/composeResources/values/strings.xml
  - app/profile/src/commonMain/composeResources/values-es/strings.xml
  - app/dashboard/src/commonMain/composeResources/values/strings.xml
  - app/dashboard/src/commonMain/composeResources/values-es/strings.xml
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "ProfileSidebar nav items, brand, logout, and footer text all come from stringResource()"
    - "All 5 TierContent files use stringResource() for every visible text"
    - "DashboardBottomNav tab labels come from stringResource()"
    - "Dashboard metric card labels (UPTIME, REQUESTS, etc.) come from string resources"
  artifacts:
    - path: "app/profile/src/commonMain/kotlin/com/m2f/template/app/profile/ProfileSidebar.kt"
      provides: "Localized sidebar navigation"
    - path: "app/dashboard/src/commonMain/kotlin/com/m2f/template/app/dashboard/DashboardBottomNav.kt"
      provides: "Localized bottom navigation"
    - path: "app/dashboard/src/commonMain/kotlin/com/m2f/template/app/dashboard/DashboardMockData.kt"
      provides: "String resource keys for mock data labels"
  key_links:
    - from: "ProfileSidebar.kt"
      to: "profile/values/strings.xml"
      via: "stringResource(Res.string.*)"
    - from: "DashboardBottomNav.kt"
      to: "dashboard/values/strings.xml"
      via: "stringResource(Res.string.*)"
---

<objective>
Wire all hardcoded composable strings to stringResource() and add corresponding string resource entries for ProfileSidebar, all 5 TierContent files, DashboardBottomNav, and DashboardMockData.

Purpose: UAT tests 3, 5, 6 revealed that 7+ Kotlin files have 100% hardcoded strings with zero stringResource() calls. Even after Plan 08 fixes the XML values, these composables would still show hardcoded English text.

Output: All composables use stringResource() for visible text; all new string keys added to EN and ES strings.xml.
</objective>

<execution_context>
@/Users/marc/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/marc/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-localization/15-UAT.md
@.planning/phases/15-localization/15-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire ProfileSidebar.kt and add string resources</name>
  <files>
    app/profile/src/commonMain/kotlin/com/m2f/template/app/profile/ProfileSidebar.kt
    app/profile/src/commonMain/composeResources/values/strings.xml
    app/profile/src/commonMain/composeResources/values-es/strings.xml
  </files>
  <action>
    Read ProfileSidebar.kt fully to understand all hardcoded strings. Then:

    1. Add import for `org.jetbrains.compose.resources.stringResource` and the generated `Res` class (follow the import pattern used in ProfileScreen.kt in the same module — look for existing stringResource imports).

    2. Replace ALL hardcoded strings with stringResource() calls. Specifically:

    BRAND ROW:
    - ">_" → stringResource(Res.string.sidebar_brand_prompt) (or reuse common_brand_prompt if already exists)
    - "profile" → stringResource(Res.string.sidebar_brand_name)

    LOGOUT:
    - "$ logout" → stringResource(Res.string.sidebar_logout)

    NAV ITEMS PER TIER (these are in when/if branches returning list of nav items):
    For Free tier: "profile", "preferences", "billing"
    For Paid tier: + "team access", "analytics", "export"  
    For Premium tier: + "webhooks", "API keys", "priority support"
    For Admin tier: "user management", "groups", "permissions", "audit log", "org settings", "admin tools", "reports"
    For PowerAdmin tier: "user directory", "admin identity", "access matrix", "platform analytics", "monitoring", "alerts", "system status", "configuration", "maintenance", "system logs", "danger zone"

    Each needs a unique string resource key. Use pattern: sidebar_nav_{tier}_{item} 
    E.g.: sidebar_nav_free_profile, sidebar_nav_free_preferences, sidebar_nav_paid_team_access, sidebar_nav_poweradmin_user_directory, etc.

    IMPORTANT: Since the nav items are created in a list builder (not directly in a @Composable), you may need to make the nav item label a string resource ID (Res.string.*) and resolve it at the call site with stringResource(). Look at how the existing code structures nav items — if they're data classes with String labels, you'll need to change the approach. The composable function that renders the nav item label should call stringResource().

    If the nav items are rendered in a LazyColumn or forEach with a composable lambda, you CAN use stringResource() inside that lambda.

    FOOTER CONTENT PER TIER:
    - Free: "> upgrade available", "// unlock team access, webhooks, and more"  
    - Paid: "> premium available", "// get priority support, API keys, webhooks"
    - Premium: "✓ premium active", "// all features unlocked"

    Add corresponding string keys.

    3. Add ALL new string keys to profile values/strings.xml (English) AND values-es/strings.xml (Spanish).

    English values should be human-readable (e.g., "User Directory", "Team Access").
    Spanish values should be proper translations (e.g., "Directorio de Usuarios", "Acceso de Equipo").

    CRITICAL: Ensure the Kotlin code compiles. Check that the Res.string.* references match exactly the XML key names. In Compose Multiplatform Resources, the generated accessor replaces hyphens with underscores and follows the XML `name` attribute.
  </action>
  <verify>Verify the file compiles by checking stringResource imports are correct and all Res.string.* references have matching XML entries in both EN and ES strings.xml. Count hardcoded strings remaining — should be zero visible text.</verify>
  <done>ProfileSidebar.kt has zero hardcoded visible text strings. All nav items, brand, logout, and footer content use stringResource(). Both EN and ES strings.xml have all new keys.</done>
</task>

<task type="auto">
  <name>Task 2: Wire TierContent files and DashboardBottomNav/MockData with string resources</name>
  <files>
    app/profile/src/commonMain/kotlin/com/m2f/template/app/profile/tier/FreeTierContent.kt
    app/profile/src/commonMain/kotlin/com/m2f/template/app/profile/tier/PaidTierContent.kt
    app/profile/src/commonMain/kotlin/com/m2f/template/app/profile/tier/PremiumTierContent.kt
    app/profile/src/commonMain/kotlin/com/m2f/template/app/profile/tier/AdminTierContent.kt
    app/profile/src/commonMain/kotlin/com/m2f/template/app/profile/tier/PowerAdminTierContent.kt
    app/dashboard/src/commonMain/kotlin/com/m2f/template/app/dashboard/DashboardBottomNav.kt
    app/dashboard/src/commonMain/kotlin/com/m2f/template/app/dashboard/DashboardMockData.kt
    app/dashboard/src/commonMain/kotlin/com/m2f/template/app/dashboard/DashboardScreen.kt
    app/profile/src/commonMain/composeResources/values/strings.xml
    app/profile/src/commonMain/composeResources/values-es/strings.xml
    app/dashboard/src/commonMain/composeResources/values/strings.xml
    app/dashboard/src/commonMain/composeResources/values-es/strings.xml
  </files>
  <action>
    This is a large task. Read each file first, then make changes systematically.

    === DASHBOARD BOTTOM NAV ===
    DashboardBottomNav.kt has hardcoded tab labels in a buildList block:
    ```
    add(BottomTab("dashboard", "~", "home"))
    add(BottomTab("processes", "#", "procs"))
    add(BottomTab("logs", "$", "logs"))
    add(BottomTab("admin", "@", "admin"))
    add(BottomTab("settings", "%", "config"))
    ```
    
    The BottomTab labels ("home", "procs", "logs", "admin", "config") need to come from string resources. Since BottomTab is a data class created outside a @Composable context, you have two approaches:
    
    PREFERRED APPROACH: If the tab labels are rendered inside a @Composable (e.g., in a Text() composable), move the stringResource() call to the rendering site. The BottomTab data class can store a string resource key identifier (e.g., the Res.string reference as StringResource) and resolve it at render time.

    Add to dashboard strings.xml:
    - bottom_nav_home → EN: "Home", ES: "Inicio"
    - bottom_nav_processes → EN: "Processes", ES: "Procesos"
    - bottom_nav_logs → EN: "Logs", ES: "Registros"
    - bottom_nav_admin → EN: "Admin", ES: "Admin"
    - bottom_nav_settings → EN: "Settings", ES: "Ajustes"

    === DASHBOARD MOCK DATA ===
    DashboardMockData.kt has hardcoded metric labels: "UPTIME", "REQUESTS", "AVG LATENCY", "ERROR RATE".
    Activity titles: "deploy_v2.4.1", "high_memory_alert", "ssl_cert_renewed", "auto_scaling_up".

    For metrics: Change the MetricItem to store a StringResource (from Compose Multiplatform Resources) instead of a String for the label. Then in the composable that renders MetricCard (in DashboardScreen.kt), call stringResource() on the metric's label resource.

    Add to dashboard strings.xml:
    - metric_uptime → EN: "UPTIME", ES: "DISPONIBILIDAD"
    - metric_requests → EN: "REQUESTS", ES: "SOLICITUDES"
    - metric_avg_latency → EN: "AVG LATENCY", ES: "LATENCIA PROM."
    - metric_error_rate → EN: "ERROR RATE", ES: "TASA DE ERROR"

    For activity titles, add:
    - activity_deploy → EN: "Deploy v2.4.1", ES: "Despliegue v2.4.1"
    - activity_memory_alert → EN: "High memory alert", ES: "Alerta de memoria alta"
    - activity_ssl_renewed → EN: "SSL cert renewed", ES: "Certificado SSL renovado"
    - activity_auto_scaling → EN: "Auto-scaling up", ES: "Auto-escalado activo"

    Same pattern: store StringResource in the data class, resolve at render time.

    === TIER CONTENT FILES ===
    For EACH of the 5 tier content files, read the file first, then:
    
    1. Add stringResource import and Res import
    2. Replace EVERY hardcoded string with stringResource(Res.string.{key})
    3. Add all new keys to profile strings.xml EN and ES

    Use naming convention: tier_{tierName}_{section}_{item}
    E.g.: tier_free_usage_limits, tier_free_api_calls, tier_paid_team_access, tier_poweradmin_platform_stats

    For each tier file, there are typically:
    - Card titles (2-6 per tier)
    - Card descriptions/subtitles
    - Table headers
    - List item labels
    - Button labels
    - Alert messages

    ALL must be string resources with EN and ES values.

    IMPORTANT: This is a lot of strings. Focus on getting the pattern right for FreeTierContent first, then apply consistently to the other 4. If a tier content file has >30 strings, that's expected — add them all.

    CRITICAL: Ensure every Res.string.{key} has a matching entry in BOTH values/strings.xml AND values-es/strings.xml.
  </action>
  <verify>Search each modified Kotlin file for remaining hardcoded visible strings (grep for quoted strings that aren't imports, package names, or resource keys). Every Text() composable should use stringResource(). Verify every Res.string.* reference has matching XML entries.</verify>
  <done>All 5 TierContent files, DashboardBottomNav, and DashboardMockData have zero hardcoded visible text. All new string keys exist in both EN and ES strings.xml files.</done>
</task>

</tasks>

<verification>
- grep for hardcoded English strings in all modified Kotlin files
- Every Text() composable uses stringResource()
- All Res.string.* references have matching XML entries in both EN and ES
- Profile and Dashboard modules each have a comprehensive strings.xml with all UI text
</verification>

<success_criteria>
Zero hardcoded visible text in ProfileSidebar.kt, all 5 TierContent files, DashboardBottomNav.kt, and DashboardMockData.kt. All strings come from resources with EN and ES translations.
</success_criteria>

<output>
After completion, create `.planning/phases/15-localization/15-09-SUMMARY.md`
</output>
