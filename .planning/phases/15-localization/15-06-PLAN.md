---
phase: 15-localization
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - app/profile/build.gradle.kts
  - app/profile/src/commonMain/composeResources/values/strings.xml
  - app/profile/src/commonMain/composeResources/values-es/strings.xml
  - app/profile/src/commonMain/kotlin/com/m2f/template/app/profile/StringKeyResolver.kt
  - app/profile/src/commonMain/kotlin/com/m2f/template/app/profile/ProfileScreen.kt
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Profile screen displays all UI strings via stringResource(Res.string.*) instead of hardcoded English"
    - "Profile screen displays error messages via resolveStringKey() instead of raw StringKey.code"
    - "Profile screen strings update when user switches locale to Spanish"
  artifacts:
    - path: "app/profile/src/commonMain/composeResources/values/strings.xml"
      provides: "Per-module English string resources for profile screen"
      contains: "<resources>"
    - path: "app/profile/src/commonMain/composeResources/values-es/strings.xml"
      provides: "Per-module Spanish string resources for profile screen"
      contains: "<resources>"
    - path: "app/profile/src/commonMain/kotlin/com/m2f/template/app/profile/StringKeyResolver.kt"
      provides: "Local resolveStringKey bridge for profile module"
      contains: "@Composable"
    - path: "app/profile/src/commonMain/kotlin/com/m2f/template/app/profile/ProfileScreen.kt"
      provides: "Profile screen using stringResource() for all UI text"
      contains: "stringResource"
  key_links:
    - from: "ProfileScreen.kt"
      to: "Res.string.*"
      via: "stringResource(Res.string.profile_*) calls"
      pattern: "stringResource\\(Res\\.string\\.profile_"
    - from: "ProfileScreen.kt"
      to: "StringKeyResolver.kt"
      via: "resolveStringKey(state.serverError) and resolveStringKey(fieldError)"
      pattern: "resolveStringKey\\("
---

<objective>
Localize the Profile module screen by adding per-module Compose resource infrastructure and replacing all hardcoded English strings with stringResource() calls, following the exact auth module pattern from Plan 02.

Purpose: Close the verification gap — profile screen strings don't update on locale switch because they're hardcoded English.
Output: Profile screen fully localized with per-module strings.xml (en + es) and resolveStringKey bridge.
</objective>

<execution_context>
@/Users/marc/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/marc/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-localization/15-02-SUMMARY.md

Reference files (the auth module pattern to replicate):
@app/auth/build.gradle.kts
@app/auth/src/commonMain/composeResources/values/strings.xml
@app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/StringKeyResolver.kt

Source strings to copy from:
@composeApp/src/commonMain/composeResources/values/strings.xml (lines 110-129 for profile, lines 179-231 for errors)
@composeApp/src/commonMain/composeResources/values-es/strings.xml (lines 110-129 for profile, lines 179-231 for errors)

Files to modify:
@app/profile/build.gradle.kts
@app/profile/src/commonMain/kotlin/com/m2f/template/app/profile/ProfileScreen.kt
@app/profile/src/commonMain/kotlin/com/m2f/template/app/profile/ProfileModel.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add compose.components.resources dependency and create per-module string resources + StringKeyResolver</name>
  <files>
    app/profile/build.gradle.kts
    app/profile/src/commonMain/composeResources/values/strings.xml
    app/profile/src/commonMain/composeResources/values-es/strings.xml
    app/profile/src/commonMain/kotlin/com/m2f/template/app/profile/StringKeyResolver.kt
  </files>
  <action>
    **Step 1: Add compose.components.resources to build.gradle.kts**

    In `app/profile/build.gradle.kts`, add `implementation(compose.components.resources)` to the `commonMain.dependencies` block, right after the existing `implementation(compose.foundation)` line. This is the exact same change made to `app/auth/build.gradle.kts` in Plan 02.

    **Step 2: Create per-module English strings.xml**

    Create `app/profile/src/commonMain/composeResources/values/strings.xml` containing:
    - All profile_* UI strings (copied from composeApp strings.xml lines 111-129): profile_command, profile_account_info, profile_account_info_subtitle, profile_name_label, profile_email_label, profile_user_id_label, profile_tier_label, profile_edit_button, profile_edit_title, profile_edit_subtitle, profile_edit_name_placeholder, profile_edit_email_placeholder, profile_save_button, profile_cancel_button, profile_save_success, profile_save_success_title, profile_error_title, profile_back, profile_logout
    - ALL error_* strings (copied from composeApp strings.xml lines 179-231) — the full set of 32 error strings that match StringKey entries. These are needed because resolveStringKey() has an exhaustive when over all StringKey values.

    **Step 3: Create per-module Spanish strings.xml**

    Create `app/profile/src/commonMain/composeResources/values-es/strings.xml` with the same structure — Spanish translations for all profile_* and error_* strings (copied from composeApp values-es/strings.xml lines 111-129 and 179-231).

    **Step 4: Create StringKeyResolver.kt**

    Create `app/profile/src/commonMain/kotlin/com/m2f/template/app/profile/StringKeyResolver.kt` — an EXACT copy of `app/auth/src/commonMain/kotlin/com/m2f/template/app/auth/StringKeyResolver.kt` with only the package and import changes:
    - Package: `com.m2f.template.app.profile`
    - Import: `template.app.profile.generated.resources.Res` (instead of `template.app.auth.generated.resources.Res`)
    - All `template.app.auth.generated.resources.*` imports become `template.app.profile.generated.resources.*`
    - The `resolveStringKey` function body stays exactly the same (exhaustive when over all 32 StringKey entries).
    - Keep it `internal fun`.
  </action>
  <verify>
    Run `./gradlew :app:profile:compileKotlinMetadata` — should compile without errors (validates that Compose Resources generates Res.string.* accessors from the per-module strings.xml).
  </verify>
  <done>
    Profile module has compose.components.resources dependency, per-module strings.xml with English and Spanish, and a local resolveStringKey bridge that compiles.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace all hardcoded strings in ProfileScreen.kt with stringResource() and resolveStringKey()</name>
  <files>
    app/profile/src/commonMain/kotlin/com/m2f/template/app/profile/ProfileScreen.kt
  </files>
  <action>
    Add these imports to ProfileScreen.kt:
    ```
    import org.jetbrains.compose.resources.stringResource
    import template.app.profile.generated.resources.Res
    import template.app.profile.generated.resources.*
    ```

    Then replace EVERY hardcoded string with the corresponding stringResource call. Here is the exact mapping based on current code:

    **In DesktopProfile (line 159) and MobileProfile (line 217):**
    - `"< back"` → `stringResource(Res.string.profile_back)`

    **In MobileProfile (line 223):**
    - `"$ logout"` → `stringResource(Res.string.profile_logout)`

    **In ProfileHeader (line 277):**
    - `"$ user_profile"` → `stringResource(Res.string.profile_command)`

    **In ProfileInfoCard:**
    - `"Profile updated successfully."` (line 299) → `stringResource(Res.string.profile_save_success)`
    - `"saved"` (line 301, title param) → `stringResource(Res.string.profile_save_success_title)`
    - `state.serverError.code` (line 308, message param) → `resolveStringKey(state.serverError)`  (CRITICAL: use resolveStringKey bridge, NOT .code)
    - `"error"` (line 310, title param) → `stringResource(Res.string.profile_error_title)`
    - `"account_info"` (line 316, title param) → `stringResource(Res.string.profile_account_info)`
    - `"// personal details"` (line 317, description param) → `stringResource(Res.string.profile_account_info_subtitle)`
    - `"name"` (line 323, InfoRow label) → `stringResource(Res.string.profile_name_label)`
    - `"email"` (line 324, InfoRow label) → `stringResource(Res.string.profile_email_label)`
    - `"user_id"` (line 325, InfoRow label) → `stringResource(Res.string.profile_user_id_label)`
    - `"tier"` (line 326, InfoRow label) → `stringResource(Res.string.profile_tier_label)`
    - `"> edit profile"` (line 331) → `stringResource(Res.string.profile_edit_button)`

    **In EditProfileSection:**
    - `"edit_profile"` (line 371, title param) → `stringResource(Res.string.profile_edit_title)`
    - `"// modify name and email"` (line 372, description param) → `stringResource(Res.string.profile_edit_subtitle)`
    - `state.serverError.code` (line 380, message param) → `resolveStringKey(state.serverError)`  (CRITICAL: use resolveStringKey bridge)
    - `"error"` (line 382, title param) → `stringResource(Res.string.profile_error_title)`
    - `"name"` (line 389, label param) → `stringResource(Res.string.profile_name_label)`
    - `"Enter your name"` (line 390, placeholder param) → `stringResource(Res.string.profile_edit_name_placeholder)`
    - `state.fieldErrors["name"]?.code` (line 392, errorMessage param) → `state.fieldErrors["name"]?.let { resolveStringKey(it) }`  (CRITICAL: use resolveStringKey bridge)
    - `"email"` (line 398, label param) → `stringResource(Res.string.profile_email_label)`
    - `"Enter your email"` (line 399, placeholder param) → `stringResource(Res.string.profile_edit_email_placeholder)`
    - `state.fieldErrors["email"]?.code` (line 401, errorMessage param) → `state.fieldErrors["email"]?.let { resolveStringKey(it) }`  (CRITICAL: use resolveStringKey bridge)
    - `"> save"` (line 408) → `stringResource(Res.string.profile_save_button)`
    - `"cancel"` (line 413) → `stringResource(Res.string.profile_cancel_button)`

    IMPORTANT: For the `.code` replacements, the pattern is:
    - `state.serverError.code` → `resolveStringKey(state.serverError)` (the non-null assertion is safe because it's inside an `if (state.serverError != null)` block)
    - `state.fieldErrors["name"]?.code` → `state.fieldErrors["name"]?.let { resolveStringKey(it) }` (safe-call chain)

    Do NOT change any hardcoded strings in the tier content files (FreeTierContent.kt, PaidTierContent.kt, etc.) — those are out of scope for this gap closure.
  </action>
  <verify>
    1. Run `./gradlew :app:profile:compileKotlinMetadata` — must compile cleanly.
    2. Run `grep -r '\.code' app/profile/src/commonMain/kotlin/com/m2f/template/app/profile/ProfileScreen.kt` — should return ZERO matches (all .code usages replaced with resolveStringKey).
    3. Run `grep -c 'stringResource' app/profile/src/commonMain/kotlin/com/m2f/template/app/profile/ProfileScreen.kt` — should return 19+ (one per UI string replaced).
    4. Run `grep -c 'resolveStringKey' app/profile/src/commonMain/kotlin/com/m2f/template/app/profile/ProfileScreen.kt` — should return 4 (2 serverError + 2 fieldErrors).
  </verify>
  <done>
    ProfileScreen.kt has zero hardcoded English strings — all UI text uses stringResource(Res.string.*) and all error display uses resolveStringKey(). Strings will update when user switches locale.
  </done>
</task>

</tasks>

<verification>
1. `./gradlew :app:profile:compileKotlinMetadata` compiles cleanly
2. No hardcoded English strings remain in ProfileScreen.kt (grep for quoted strings shows only non-UI strings like key names "name", "email" used as map keys)
3. All `.code` usages in ProfileScreen.kt replaced with resolveStringKey()
4. Per-module strings.xml exists in both values/ and values-es/ directories
5. StringKeyResolver.kt has exhaustive when over all 32 StringKey entries
</verification>

<success_criteria>
Profile screen renders all UI text via stringResource(Res.string.*) and all error messages via resolveStringKey(). When a user switches locale from English to Spanish, all profile screen strings update to Spanish translations.
</success_criteria>

<output>
After completion, create `.planning/phases/15-localization/15-06-SUMMARY.md`
</output>
