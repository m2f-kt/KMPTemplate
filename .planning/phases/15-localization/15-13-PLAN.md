---
phase: 15-localization
plan: 13
type: execute
wave: 1
depends_on: []
files_modified:
  - app/dashboard/src/commonMain/kotlin/com/m2f/template/app/dashboard/DashboardViewModel.kt
  - app/dashboard/src/commonMain/kotlin/com/m2f/template/app/dashboard/DashboardModel.kt
  - app/dashboard/src/commonTest/kotlin/com/m2f/template/app/dashboard/DashboardViewModelTest.kt
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "System Admin/PowerAdmin users see the admin panel nav item even without group memberships"
    - "Admin panel navigation works for system admins without group memberships"
    - "Users with group admin role still see admin panel as before (no regression)"
  artifacts:
    - path: "app/dashboard/src/commonMain/kotlin/com/m2f/template/app/dashboard/DashboardViewModel.kt"
      provides: "Dual-role admin check (UserRole + GroupRole)"
      contains: "getProfile"
    - path: "app/dashboard/src/commonMain/kotlin/com/m2f/template/app/dashboard/DashboardModel.kt"
      provides: "DashboardModel with system admin state"
    - path: "app/dashboard/src/commonTest/kotlin/com/m2f/template/app/dashboard/DashboardViewModelTest.kt"
      provides: "Tests for system admin visibility"
      contains: "system admin"
  key_links:
    - from: "app/dashboard/src/commonMain/kotlin/com/m2f/template/app/dashboard/DashboardViewModel.kt"
      to: "sdk.getProfile()"
      via: "UserApi call in LoadDashboard"
      pattern: "sdk\\.getProfile"
---

<objective>
Fix admin panel visibility for system-level Admin/PowerAdmin users who have no group memberships.

Purpose: DashboardViewModel currently checks ONLY GroupRole from memberships to determine admin panel visibility. A system Admin/PowerAdmin without group memberships gets an empty membership list → isAdmin stays false → admin nav item never renders. Need to also check the user's system-level UserRole via getProfile().
Output: System admins see the admin panel regardless of group membership. Admin panel click works even without a groupId.
</objective>

<execution_context>
@/Users/marc/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/marc/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/debug/admin-panel-not-visible.md
@app/dashboard/src/commonMain/kotlin/com/m2f/template/app/dashboard/DashboardViewModel.kt
@app/dashboard/src/commonMain/kotlin/com/m2f/template/app/dashboard/DashboardModel.kt
@app/dashboard/src/commonMain/kotlin/com/m2f/template/app/dashboard/DashboardEvent.kt
@app/dashboard/src/commonMain/kotlin/com/m2f/template/app/dashboard/DashboardIntent.kt
@app/dashboard/src/commonMain/kotlin/com/m2f/template/app/dashboard/DashboardMutation.kt
@app/dashboard/src/commonMain/kotlin/com/m2f/template/app/dashboard/DashboardSidebar.kt
@app/dashboard/src/commonMain/kotlin/com/m2f/template/app/dashboard/DashboardBottomNav.kt
@app/dashboard/src/commonTest/kotlin/com/m2f/template/app/dashboard/DashboardViewModelTest.kt
@core/models/src/commonMain/kotlin/com/m2f/template/models/UserRole.kt
@core/models/src/commonMain/kotlin/com/m2f/template/models/dto/UserDtos.kt
@core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/api/UserApi.kt
@core/testing/src/commonMain/kotlin/com/m2f/template/core/testing/fakes/FakeUserApiBuilder.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dual-role admin check to DashboardViewModel</name>
  <files>
    app/dashboard/src/commonMain/kotlin/com/m2f/template/app/dashboard/DashboardViewModel.kt
    app/dashboard/src/commonMain/kotlin/com/m2f/template/app/dashboard/DashboardModel.kt
    app/dashboard/src/commonMain/kotlin/com/m2f/template/app/dashboard/DashboardMutation.kt
    app/dashboard/src/commonMain/kotlin/com/m2f/template/app/dashboard/DashboardEvent.kt
  </files>
  <action>
**DashboardModel.kt** — Add a `isSystemAdmin` field (Boolean, default false). Keep existing `isAdmin`, `groupId`, `groupName` fields as-is. The UI sidebar/bottomnav already uses `isAdmin` to gate the admin nav item, so `isAdmin` should be true if EITHER system role is Admin/PowerAdmin OR group role is Admin/Owner.

**DashboardMutation.kt** — Add a new mutation `SetSystemAdmin(val isSystemAdmin: Boolean)` alongside the existing `SetMembership`.

**DashboardViewModel.kt** — In the `LoadDashboard` handler, BEFORE the existing `getMyMemberships()` call, also call `sdk.getProfile()`:

```kotlin
// Check system-level role
sdk.getProfile().onRight { user ->
    val isSystemAdmin = user.role.level >= UserRole.Admin.level
    sendMutation(DashboardMutation.SetSystemAdmin(isSystemAdmin))
}
```

Import `com.m2f.template.models.UserRole`.

Update the `reduce` function to handle `SetSystemAdmin`:
```kotlin
is DashboardMutation.SetSystemAdmin -> model.copy(
    isSystemAdmin = mutation.isSystemAdmin,
    isAdmin = mutation.isSystemAdmin || model.isAdmin,
)
```

And update `SetMembership` reduce to also account for system admin:
```kotlin
is DashboardMutation.SetMembership -> model.copy(
    isAdmin = mutation.isAdmin || model.isSystemAdmin,
    groupId = mutation.groupId,
    groupName = mutation.groupName,
)
```

This ensures `isAdmin` is true if EITHER role check passes, regardless of order.

**DashboardEvent.kt** — Update `NavigateToAdmin` to accept nullable groupId: `data class NavigateToAdmin(val groupId: String?)`. System admins without group memberships will navigate to admin panel with null groupId.

**DashboardViewModel.kt** — Update `AdminPanelClicked` handler to remove the null guard on groupId. Just emit the event:
```kotlin
is DashboardIntent.AdminPanelClicked -> {
    sendEvent(DashboardEvent.NavigateToAdmin(model.value.groupId))
}
```

This allows system admins to reach the admin panel. When groupId is null, the admin panel can show a "no group" state or prompt group creation.
  </action>
  <verify>
`./gradlew app:dashboard:compileKotlinJvm` compiles successfully.
  </verify>
  <done>DashboardViewModel checks both system UserRole (via getProfile) and group role (via getMyMemberships). isAdmin is true for system Admin/PowerAdmin even without group memberships. AdminPanelClicked no longer requires non-null groupId.</done>
</task>

<task type="auto">
  <name>Task 2: Update DashboardViewModel tests and AppNavHost for nullable groupId</name>
  <files>
    app/dashboard/src/commonTest/kotlin/com/m2f/template/app/dashboard/DashboardViewModelTest.kt
    composeApp/src/commonMain/kotlin/com/m2f/template/navigation/AppNavHost.kt
    composeApp/src/commonMain/kotlin/com/m2f/template/navigation/Routes.kt
  </files>
  <action>
**DashboardViewModelTest.kt** — Add new test cases AND update existing ones:

1. **New test: "LoadDashboard with system admin role sets isAdmin true even without group memberships"**
   - Configure fake: `user { getProfile { Either.Right(UserResponse(id = "1", email = "admin@test.com", name = "Admin", role = UserRole.Admin)) } }` — do NOT configure getMyMemberships (defaults to Left → error, silently ignored).
   - Dispatch LoadDashboard, expect final model with `isAdmin = true, isSystemAdmin = true, groupId = null`.

2. **New test: "AdminPanelClicked emits NavigateToAdmin with null groupId for system admin"**
   - Configure fake with admin profile, no memberships.
   - Dispatch LoadDashboard, then AdminPanelClicked.
   - Expect `event(DashboardEvent.NavigateToAdmin(null))`.

3. **New test: "LoadDashboard with system admin AND group admin membership gets groupId"**
   - Configure fake with admin profile AND admin group membership.
   - Dispatch LoadDashboard, expect final model with `isAdmin = true, isSystemAdmin = true, groupId = "group-1"`.

4. **Update existing tests**: The existing tests use `fakeSdk()` with default profile (returns Left). Since getProfile failure is silently ignored (onRight), existing tests that don't configure getProfile should continue working — `isSystemAdmin` stays false. Verify the existing test models include `isSystemAdmin = false` in their expected DashboardModel() constructors (it's a default-false field, so `DashboardModel()` already matches). If any existing test breaks due to new intermediate states from the getProfile call, update the expected model sequence.

**Routes.kt** — Change `AdminPanelRoute(val groupId: String)` to `AdminPanelRoute(val groupId: String? = null)` so navigation can work without a group.

**AppNavHost.kt** — Where `NavigateToAdmin` event triggers navigation, it should still call `navController.navigate(AdminPanelRoute(groupId = event.groupId))`. No change needed since groupId is now nullable in both places. Check that the `composable<AdminPanelRoute>` block handles a null `route.groupId` gracefully — if it passes groupId to AdminPanelViewModel via LaunchedEffect, ensure the ViewModel handles null (shows empty state or group selector). If the AdminPanelViewModel already guards on null groupId, this is fine.

**Important:** Use kotest assertion style consistently with the rest of the test file. The existing tests use the MVI test DSL (`intent()`, `model()`, `event()`) — keep using that pattern.
  </action>
  <verify>
`./gradlew app:dashboard:allTests` passes — all existing tests still pass, new tests pass. `./gradlew composeApp:compileKotlinJvm` compiles (Routes/AppNavHost changes).
  </verify>
  <done>Tests verify: (1) system admin without groups sees isAdmin=true, (2) AdminPanelClicked works with null groupId, (3) combined system+group admin gets groupId. Existing tests unbroken. Routes accept nullable groupId.</done>
</task>

</tasks>

<verification>
- `./gradlew app:dashboard:allTests` — all tests pass including new system admin tests
- `./gradlew composeApp:compileKotlinJvm` — compiles with nullable groupId route changes
- System admin user (no group memberships) → isAdmin=true in DashboardModel
- Group admin user (with admin membership) → isAdmin=true (regression check)
- Regular user → isAdmin=false (no change)
</verification>

<success_criteria>
Admin/PowerAdmin users see the admin panel nav item regardless of group memberships. AdminPanelClicked navigates even without a groupId. All existing dashboard tests continue passing.
</success_criteria>

<output>
After completion, create `.planning/phases/15-localization/15-13-SUMMARY.md`
</output>
