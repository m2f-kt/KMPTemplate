---
phase: 19-structured-ai-rag-pipeline
plan: 05
type: execute
wave: 3
depends_on: ["19-03"]
files_modified:
  - core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/api/DocumentApi.kt
  - core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/api/DocumentApiImpl.kt
  - core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/Sdk.kt
  - core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/di/SdkModule.kt
  - core/testing/src/commonMain/kotlin/com/m2f/template/testing/FakeDocumentApiBuilder.kt
  - core/testing/src/commonMain/kotlin/com/m2f/template/testing/FakeSdkBuilder.kt
  - composeApp/src/commonMain/kotlin/com/m2f/template/app/documents/DocumentsViewModel.kt
  - composeApp/src/commonMain/kotlin/com/m2f/template/app/documents/DocumentsScreen.kt
  - composeApp/src/commonMain/kotlin/com/m2f/template/app/documents/DocumentsIntent.kt
  - composeApp/src/commonMain/kotlin/com/m2f/template/app/documents/DocumentsModel.kt
  - composeApp/src/commonMain/kotlin/com/m2f/template/app/documents/DocumentsMutation.kt
  - composeApp/src/commonMain/kotlin/com/m2f/template/app/documents/DocumentsEvent.kt
  - composeApp/src/commonMain/kotlin/com/m2f/template/navigation/Routes.kt
  - composeApp/src/commonMain/kotlin/com/m2f/template/navigation/AppNavHost.kt
  - composeApp/src/commonMain/kotlin/com/m2f/template/di/AppModule.kt
autonomous: true
requirements: [RAG-04]

must_haves:
  truths:
    - "DocumentApi SDK interface exists with uploadDocument, listDocuments, getDocument, deleteDocument methods"
    - "FakeDocumentApiBuilder exists for ViewModel testing"
    - "Members can see a Documents screen listing their personal documents"
    - "Members can upload .txt/.md files from the Documents screen"
    - "Members can delete their own personal documents"
    - "Documents show status labels (pending, processing, indexed, failed)"
  artifacts:
    - path: "core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/api/DocumentApi.kt"
      provides: "SDK document operations interface"
      contains: "interface DocumentApi"
    - path: "composeApp/src/commonMain/kotlin/com/m2f/template/app/documents/DocumentsViewModel.kt"
      provides: "MVI ViewModel for documents feature"
      contains: "DocumentsViewModel"
    - path: "composeApp/src/commonMain/kotlin/com/m2f/template/app/documents/DocumentsScreen.kt"
      provides: "Documents list and upload UI"
      contains: "DocumentsScreen"
  key_links:
    - from: "DocumentsViewModel"
      to: "Sdk.uploadDocument"
      via: "SDK call for document upload"
      pattern: "sdk.*uploadDocument"
    - from: "AppNavHost"
      to: "DocumentsScreen"
      via: "DocumentsRoute navigation"
      pattern: "composable.*DocumentsRoute"
---

<objective>
Build the client SDK DocumentApi and the member-facing Documents UI screen.

Purpose: Give members a dedicated screen to upload, view, and manage their personal documents. This completes the user-facing write path for RAG document ingestion.

Output: DocumentApi (SDK), FakeDocumentApiBuilder (testing), DocumentsViewModel (MVI), DocumentsScreen (Compose), navigation wiring.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-structured-ai-rag-pipeline/19-CONTEXT.md
@.planning/phases/19-structured-ai-rag-pipeline/19-03-SUMMARY.md
@core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/api/FileApi.kt
@core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/api/FileApiImpl.kt
@core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/Sdk.kt
@core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/di/SdkModule.kt
@core/models/src/commonMain/kotlin/com/m2f/template/models/dto/DocumentDtos.kt
@core/models/src/commonMain/kotlin/com/m2f/template/models/routes/ApiRoutes.kt
@composeApp/src/commonMain/kotlin/com/m2f/template/navigation/Routes.kt
@composeApp/src/commonMain/kotlin/com/m2f/template/navigation/AppNavHost.kt
@composeApp/src/commonMain/kotlin/com/m2f/template/di/AppModule.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: DocumentApi SDK + FakeDocumentApiBuilder + Sdk facade update</name>
  <files>
    core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/api/DocumentApi.kt
    core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/api/DocumentApiImpl.kt
    core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/Sdk.kt
    core/sdk/src/commonMain/kotlin/com/m2f/template/sdk/di/SdkModule.kt
    core/testing/src/commonMain/kotlin/com/m2f/template/testing/FakeDocumentApiBuilder.kt
    core/testing/src/commonMain/kotlin/com/m2f/template/testing/FakeSdkBuilder.kt
  </files>
  <action>
    1. Create `DocumentApi` interface following FileApi pattern:
       ```kotlin
       interface DocumentApi {
           suspend fun uploadDocument(groupId: String, scope: String, fileName: String, fileBytes: ByteArray, contentType: String, assignedToUserId: String? = null): Either<AppError, DocumentResponse>
           suspend fun listDocuments(groupId: String, scope: String? = null): Either<AppError, DocumentListResponse>
           suspend fun getDocument(documentId: String): Either<AppError, DocumentResponse>
           suspend fun deleteDocument(documentId: String): Either<AppError, Unit>
       }
       ```

    2. Create `DocumentApiImpl` following FileApiImpl pattern:
       - `uploadDocument`: Use `submitFormWithBinaryData` for multipart upload to `Documents.Upload` resource. Include form fields for groupId, scope, and optional assignedToUserId alongside the file part.
       - `listDocuments`: GET to `Documents.ListByGroup(groupId = groupId, scope = scope)`. Wrapped in `apiCall<DocumentListResponse>`.
       - `getDocument`: GET to `Documents.ById(documentId = documentId)`. Wrapped in `apiCall<DocumentResponse>`.
       - `deleteDocument`: POST to `Documents.Delete(documentId = documentId)`. Wrapped in `apiCall<Unit>`.

    3. Update `Sdk` facade class:
       - Add `documentApi: DocumentApi` constructor parameter
       - Add `DocumentApi by documentApi` delegation

    4. Update `SdkModule.kt`:
       - Register `single<DocumentApi> { DocumentApiImpl(get()) }`
       - Pass `documentApi = get()` to Sdk constructor

    5. Create `FakeDocumentApiBuilder` following FakeFileApiBuilder pattern:
       - Default stubs for all methods returning success
       - DSL methods: `uploadDocument { ... }`, `listDocuments { ... }`, etc.

    6. Update `FakeSdkBuilder` to include `document { ... }` DSL block
  </action>
  <verify>
    <automated>cd /Users/marc/IdeaProjects/Template && ./gradlew :core:sdk:compileKotlinMetadata :core:testing:compileKotlinMetadata 2>&1 | tail -5</automated>
    <manual>Verify DocumentApi interface matches server endpoints and FakeDocumentApiBuilder provides testing support</manual>
  </verify>
  <done>DocumentApi interface + impl follow established SDK patterns; Sdk facade delegates to DocumentApi; SdkModule wires DocumentApi; FakeDocumentApiBuilder + FakeSdkBuilder updated for testing</done>
</task>

<task type="auto">
  <name>Task 2: Documents MVI ViewModel + Screen + Navigation for members</name>
  <files>
    composeApp/src/commonMain/kotlin/com/m2f/template/app/documents/DocumentsIntent.kt
    composeApp/src/commonMain/kotlin/com/m2f/template/app/documents/DocumentsModel.kt
    composeApp/src/commonMain/kotlin/com/m2f/template/app/documents/DocumentsMutation.kt
    composeApp/src/commonMain/kotlin/com/m2f/template/app/documents/DocumentsEvent.kt
    composeApp/src/commonMain/kotlin/com/m2f/template/app/documents/DocumentsViewModel.kt
    composeApp/src/commonMain/kotlin/com/m2f/template/app/documents/DocumentsScreen.kt
    composeApp/src/commonMain/kotlin/com/m2f/template/navigation/Routes.kt
    composeApp/src/commonMain/kotlin/com/m2f/template/navigation/AppNavHost.kt
    composeApp/src/commonMain/kotlin/com/m2f/template/di/AppModule.kt
  </files>
  <action>
    1. Create MVI type files following the established 4-file pattern:

       **DocumentsIntent** (sealed interface):
       - `data class LoadDocuments(val groupId: String) : DocumentsIntent`
       - `data class UploadFile(val groupId: String, val fileName: String, val fileBytes: ByteArray, val contentType: String) : DocumentsIntent`
       - `data class DeleteDocument(val documentId: String) : DocumentsIntent`
       - `data object RefreshDocuments : DocumentsIntent`

       **DocumentsModel** (data class):
       - `isLoading: Boolean = false`
       - `documents: List<DocumentResponse> = emptyList()`
       - `isUploading: Boolean = false`
       - `error: StringKey? = null`
       - `groupId: String? = null`

       **DocumentsMutation** (sealed interface):
       - `SetLoading`, `SetDocuments(docs)`, `SetUploading`, `AddDocument(doc)`, `RemoveDocument(id)`, `SetError(error)`

       **DocumentsEvent** (sealed interface):
       - `data object UploadSuccess : DocumentsEvent`

    2. Create `DocumentsViewModel` extending `MviViewModel<DocumentsIntent, DocumentsModel, DocumentsMutation, DocumentsEvent>`:
       - Constructor takes `Sdk`
       - `resolve()`: LoadDocuments -> call sdk.listDocuments, map to SetDocuments. UploadFile -> call sdk.uploadDocument, emit UploadSuccess event. DeleteDocument -> call sdk.deleteDocument, emit RemoveDocument mutation. RefreshDocuments -> reload.
       - `reduce()`: apply mutations to model
       - Follow the established LoginViewModel/AdminPanelViewModel patterns

    3. Create `DocumentsScreen` composable:
       - User decision: "single list with labels indicating personal vs group (no tabs)"
       - Show a list of documents using TerminalCard or TerminalList
       - Each document shows: name, scope label ("Personal" / "Group"), status badge (TerminalBadge with variant based on status)
       - Upload button at top (opens file picker -- use expect/actual for file picking or a simple text input for file path as MVP)
       - Delete action on each document (with confirmation or simple click)
       - Loading state with TerminalProgress
       - Empty state with helpful message ("No documents yet. Upload .txt or .md files to enhance your AI chat.")
       - Claude's discretion on exact layout per user decision

    4. Add `DocumentsRoute` to Routes.kt:
       `@Serializable data class DocumentsRoute(val groupId: String) : Route`

    5. Wire in AppNavHost:
       - Add `composable<DocumentsRoute>` block
       - DocumentsViewModel with koinViewModel
       - LaunchedEffect to dispatch LoadDocuments with route.groupId
       - Wire all screen callbacks

    6. Add nav item to DashboardSidebar/BottomNav (or access from a relevant place):
       - Add "Documents" nav item in dashboard sidebar for members
       - On click, navigate to DocumentsRoute with the user's current groupId
       - Need to know which group the user is in -- use memberships from DashboardViewModel state

    7. Register DocumentsViewModel in AppModule: `viewModelOf(::DocumentsViewModel)`
  </action>
  <verify>
    <automated>cd /Users/marc/IdeaProjects/Template && ./gradlew :composeApp:compileKotlinDesktop 2>&1 | tail -5</automated>
    <manual>Verify Documents screen shows document list with status labels and upload/delete actions</manual>
  </verify>
  <done>DocumentsScreen shows member's personal documents with status labels; upload triggers multipart upload via SDK; delete removes document; navigation wired from dashboard; MVI pattern followed</done>
</task>

</tasks>

<verification>
- `./gradlew :core:sdk:compileKotlinMetadata` passes (SDK compiles)
- `./gradlew :composeApp:compileKotlinDesktop` passes (UI compiles)
- DocumentApi SDK methods match server document routes
- FakeDocumentApiBuilder provides test doubles
- Documents screen shows document list with personal/group labels
- Upload flow sends multipart to server
- Delete flow removes document and triggers cleanup
- Navigation from dashboard to documents works
</verification>

<success_criteria>
Members can access a Documents screen from the dashboard, upload .txt/.md files, see document status (pending/indexed/failed), and delete their personal documents. All interactions go through the SDK.
</success_criteria>

<output>
After completion, create `.planning/phases/19-structured-ai-rag-pipeline/19-05-SUMMARY.md`
</output>
