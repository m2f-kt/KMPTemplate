---
phase: 19-structured-ai-rag-pipeline
plan: 06
type: execute
wave: 3
depends_on: ["19-03", "19-04"]
files_modified:
  - server/ai/src/test/kotlin/com/m2f/server/ai/StructuredOutputTest.kt
  - server/ai/src/test/kotlin/com/m2f/server/ai/DocumentRoutesTest.kt
  - server/ai/src/test/kotlin/com/m2f/server/ai/TestHelpers.kt
autonomous: true
requirements: [DEBT-03, AISTR-01, AISTR-02, AISTR-03]

must_haves:
  truths:
    - "Integration test verifies structured output returns typed Kotlin data class"
    - "Integration test verifies document upload creates record in database"
    - "Integration test verifies document list returns correct documents per user scope"
    - "Integration test verifies document deletion cascades to embeddings"
    - "Tests use Testcontainers PostgreSQL with pgvector extension"
  artifacts:
    - path: "server/ai/src/test/kotlin/com/m2f/server/ai/StructuredOutputTest.kt"
      provides: "Structured output integration tests"
      contains: "StructuredOutputTest"
    - path: "server/ai/src/test/kotlin/com/m2f/server/ai/DocumentRoutesTest.kt"
      provides: "Document routes integration tests"
      contains: "DocumentRoutesTest"
    - path: "server/ai/src/test/kotlin/com/m2f/server/ai/TestHelpers.kt"
      provides: "Shared test utilities for AI module"
      contains: "aiTestApp"
  key_links:
    - from: "StructuredOutputTest"
      to: "StructuredOutputService"
      via: "tests executeStructured with real Gemini API"
      pattern: "structuredOutputService.*execute"
    - from: "DocumentRoutesTest"
      to: "DocumentRoutes"
      via: "HTTP client calls to document endpoints"
      pattern: "client.*post.*documents"
---

<objective>
Create integration tests for the structured output endpoint and document management routes.

Purpose: Verify that structured output returns typed Kotlin data classes (DEBT-03) and that document CRUD operations work correctly with proper scoping and cascade deletion.

Output: Integration test files with comprehensive coverage of structured output and document management.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-structured-ai-rag-pipeline/19-RESEARCH.md
@.planning/phases/19-structured-ai-rag-pipeline/19-03-SUMMARY.md
@.planning/phases/19-structured-ai-rag-pipeline/19-04-SUMMARY.md
@server/files/src/test/kotlin/com/m2f/server/files/FileRoutesTest.kt
@server/groups/src/test/kotlin/com/m2f/server/groups/GroupRoutesTest.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test helpers + DocumentRoutesTest for document CRUD and scoping</name>
  <files>
    server/ai/src/test/kotlin/com/m2f/server/ai/TestHelpers.kt
    server/ai/src/test/kotlin/com/m2f/server/ai/DocumentRoutesTest.kt
  </files>
  <action>
    1. Create `TestHelpers.kt` following `server/files/src/test/kotlin/.../TestHelpers.kt` and `server/groups/src/test/kotlin/.../TestHelpers.kt` patterns:
       - `fun aiTestApp(block: suspend ApplicationTestBuilder.() -> Unit)` -- sets up Ktor testApplication with:
         - Koin modules (configurationModule, aiModule, authModule, groupModule, fileModule with mock S3)
         - Auth migrations, group migrations, AI migrations, vector migrations
         - Security, resources, content negotiation, websockets
         - Document routes registration
       - `suspend fun ApplicationTestBuilder.createTestToken(userId: String, role: String = "User"): String` -- create JWT for authenticated requests
       - `suspend fun ApplicationTestBuilder.createTestUser(email: String): String` -- register user, return userId
       - Use Testcontainers PostgreSQL with pgvector extension: `PostgreSQLContainer("pgvector/pgvector:pg15")` (matching the Docker Compose image from Phase 17)

    2. Create `DocumentRoutesTest.kt` with kotest assertions:
       - **Test: "POST /api/documents/upload creates document record"**
         - Create test user, create test group, add user as member
         - Upload a .txt file via multipart to /api/documents/upload with groupId and scope="personal"
         - Assert response status 200 (or 201)
         - Assert DocumentResponse has correct name, scope, status (initially "pending")

       - **Test: "GET /api/documents/list returns user's personal documents"**
         - Create user, group, upload 2 personal documents
         - GET /api/documents/list?groupId=X
         - Assert returns 2 documents with correct scope labels

       - **Test: "GET /api/documents/list respects group scoping"**
         - Create user1 in group1, user2 in group2
         - Upload doc to group1 as user1
         - GET /api/documents/list?groupId=group2 as user2
         - Assert returns 0 documents (cross-group isolation, RAG-06)

       - **Test: "POST /api/documents/{id}/delete removes document and embeddings"**
         - Upload document, wait for ingestion (or mock ingestion)
         - Delete document
         - Assert document no longer returned by list
         - Verify embeddings cleaned up (optional: direct DB query)

       - **Test: "member cannot upload group-scoped documents"**
         - Create user with Member role in group
         - Attempt upload with scope="group"
         - Assert 403 Forbidden

       Use kotest `shouldBe`, `shouldContain`, `shouldHaveSize` assertions per project conventions.
  </action>
  <verify>
    <automated>cd /Users/marc/IdeaProjects/Template && ./gradlew :server:ai:compileTestKotlin 2>&1 | tail -5</automated>
    <manual>Verify test file compiles and covers document CRUD, scoping, and deletion cascade</manual>
  </verify>
  <done>DocumentRoutesTest covers document upload, listing, group isolation, deletion cascade, and scope authorization; TestHelpers provide aiTestApp with pgvector Testcontainers</done>
</task>

<task type="auto">
  <name>Task 2: StructuredOutputTest for typed response verification</name>
  <files>
    server/ai/src/test/kotlin/com/m2f/server/ai/StructuredOutputTest.kt
  </files>
  <action>
    Create `StructuredOutputTest.kt` for DEBT-03:

    Since structured output requires a real LLM API call (Google Gemini), these tests verify the StructuredOutputService end-to-end. However, the test environment may not have API keys. Handle this with two approaches:

    **Approach A (preferred): Conditional test with real API:**
    - Read `AI_GOOGLE_API_KEY` from environment
    - If absent, mark test as skipped/pending with message "Set AI_GOOGLE_API_KEY to run structured output tests"
    - If present, test actual structured output:

    ```kotlin
    // Test: "executeStructured returns typed RelevanceCheck"
    // Create a real GoogleLLMClient + SingleLLMPromptExecutor
    // Create StructuredOutputService with real executor
    // Call execute<RelevanceCheck>(prompt) with a simple query
    // Assert result is Either.Right with valid RelevanceCheck fields
    // This verifies AISTR-01 (typed JSON response), AISTR-02 (@Serializable + @LLMDescription), AISTR-03 (Either wrapper)
    ```

    **Approach B (always runs): Unit test for service wiring:**
    - Test that StructuredOutputService can be instantiated
    - Test that RelevanceCheck data class has correct @Serializable annotation
    - Test that RelevanceDetector falls back to `needsContext = false` on error

    Include both approaches:
    1. Integration test gated by API key presence (real LLM call)
    2. Unit tests that always run (no LLM dependency)

    Use kotest assertions: `shouldBe`, `shouldBeRight`, `shouldBeLeft` (Arrow kotest assertions).
  </action>
  <verify>
    <automated>cd /Users/marc/IdeaProjects/Template && ./gradlew :server:ai:compileTestKotlin 2>&1 | tail -5</automated>
    <manual>Verify StructuredOutputTest has both conditional integration test and always-running unit tests</manual>
  </verify>
  <done>StructuredOutputTest verifies typed structured output with real Gemini API (conditional) and service wiring (always runs); covers AISTR-01, AISTR-02, AISTR-03, DEBT-03</done>
</task>

</tasks>

<verification>
- `./gradlew :server:ai:compileTestKotlin` passes (test classes compile)
- DocumentRoutesTest uses Testcontainers with pgvector
- DocumentRoutesTest verifies group isolation (RAG-06)
- StructuredOutputTest verifies typed response (DEBT-03)
- Both test files use kotest assertions per project conventions
- Tests follow established patterns from FileRoutesTest and GroupRoutesTest
</verification>

<success_criteria>
Integration tests compile and cover: (1) document CRUD with proper scoping, (2) group isolation for RAG, (3) deletion cascade, and (4) structured output typed response. Tests can be run with `./gradlew :server:ai:test`.
</success_criteria>

<output>
After completion, create `.planning/phases/19-structured-ai-rag-pipeline/19-06-SUMMARY.md`
</output>
